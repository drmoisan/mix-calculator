# Policy Compliance Audit: schema-builder-ux-overhaul (Issue #72)

**Audit Date:** 2026-06-16
**Code Under Test:** 32 changed Python files (20 production under `src/`, 12 test files under `tests/`). No TypeScript, PowerShell, C#, Bash, or JSON files changed in the branch diff `0a47fef..c6a9f95`.

**Coverage Metrics by Language:**

| Language | Files Changed | Tests | Test Result | Baseline Coverage | Post-Change Coverage | New Code Coverage |
|----------|--------------|-------|-------------|-------------------|---------------------|-------------------|
| Python | 32 files (20 src, 12 test) | 1085 tests | ✅ 1085 pass, 0 fail | 99.08% lines, 94.10% branches | 99.04% lines, 93.43% branches | 100% / 100% / 92-100% per new module (lowest new module 85% line / 75% branch) |

**Note:** Only Python files changed in the branch diff. No other language rows are applicable (zero changed files in those languages).

### Coverage Evidence Checklist

- TypeScript baseline coverage artifact: `N/A - no TypeScript files changed in branch diff`
- TypeScript post-change coverage artifact: `N/A - no TypeScript files changed in branch diff`
- PowerShell baseline coverage artifact: `N/A - no PowerShell files changed in branch diff`
- PowerShell post-change coverage artifact: `N/A - no PowerShell files changed in branch diff`
- Per-language comparison summary: see section 1.2.1 below; Python artifact `artifacts/python/lcov.info` (regenerated by reviewer full-suite run) and `docs/features/active/2026-06-16-schema-builder-ux-overhaul-72/evidence/qa-gates/coverage-delta.md`

**Non-negotiable verdict rule:** This audit reports numeric baseline and post-change coverage for the only in-scope language (Python), plus per-new/changed-module coverage. Languages with zero changed files are not in scope.

**Fail-closed rule:** All required baseline, QA, and coverage-comparison artifacts are present and were independently re-verified by the reviewer.

---

## Executive Summary

Issue #72 is a usability follow-up on the drag-and-drop Schema Builder (issues #50/#62). The branch delivers eleven acceptance criteria (AC-1..AC-11) across the dialog resize behavior, Identity, Derived, Columns, Key, Dedup, and Preview tabs. The change touches 20 production and 12 test Python files; no other language is involved.

The reviewer independently re-ran the full Python toolchain against branch head `c6a9f95`:

- Black `--check .`: pass (255 files unchanged).
- Ruff `check .`: pass (all checks passed).
- Pyright: pass (0 errors, 0 warnings).
- Pytest with branch coverage (`QT_QPA_PLATFORM=offscreen`): 1085 passed, exit 0; line 99.04%, branch 93.43%.

The three resolved design decisions were verified against the diff: D-1 (formula grammar unchanged; bracket handling isolated to a UI display helper), D-2 (`KeySpec`/`KeyPart` model and loader key resolution unchanged), and D-3 (`DedupPolicy` invariant relaxed only for the new `auto` mode; `collapse`/`aggregate` and the LE explicit `select_from` path preserved). AC-9 has a real production call site (`install_preview_refresh_handler` -> `presenter.update_preview`, wired from `open_schema_builder`), driven by an integration test through the real tab-change seam, not only a unit test of the method. All 32 changed Python files are under the 500-line cap (largest: `schema_builder_dialog.py` at 494). Committed test fixtures use synthetic masked values only. No new suppressions were introduced.

No blocking findings. One non-blocking informational item: `src/gui/_columns_tab_protocol.py` is a `typing.Protocol`-only module that the coverage tool reports as not imported (0%); the concrete implementers are fully covered, so this is an informational PARTIAL on the metric, not a code defect.

**Policy documents evaluated:**
- ✅ `general-code-change.md`
- ✅ `general-unit-test.md`

**Language-specific policies evaluated:**
- ✅ `python.md` + `python-suppressions.md`
- N/A `powershell.md` (no PowerShell files changed)
- N/A Bash (no Bash files changed)
- N/A JSON (no governed JSON files changed)

**Temporary artifacts cleanup:**
- ✅ No temporary/one-time scripts were created during this review.
- ✅ No ongoing tooling scripts were added by this review.
- No scripts created during review.

---

## Rejected Scope Narrowing

None. The caller prompt supplied the full branch diff scope (base `main`, merge-base `0a47fef`, head `c6a9f95`) and did not attempt to narrow the audit to a plan, task, phase, file subset, or to mark any language as out of scope. The reviewer audited the full feature-vs-base diff.

---

## Evidence Location Compliance

A git-diff scan of the branch for files written under `artifacts/baselines/`, `artifacts/qa/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/regression-testing/`, or `artifacts/post-change/` returned no matches. The canonical `validate_evidence_locations.py` script is absent from this repo (recorded in reviewer memory), so a git-diff scan was used:

- Command: `git diff --name-status 0a47fef..c6a9f95 | grep -iE 'artifacts/(baselines|baseline|qa|qa-gates|evidence|coverage|regression-testing|post-change)/'`
- Result: `NO_NONCANONICAL_EVIDENCE_PATHS`

All executor evidence is under the canonical `docs/features/active/2026-06-16-schema-builder-ux-overhaul-72/evidence/<kind>/` scheme (`baseline/`, `qa-gates/`). No `EVIDENCE_LOCATION_OVERRIDE_REJECTED` entries were required.

---

## 1. General Unit Test Policy Compliance

### 1.1 Core Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Independence** - Tests run in any order | ✅ PASS | Full suite passes under standard pytest collection order; tests use per-test fixtures (`qtbot`, locally constructed `PreviewSlice`/service fakes) and no shared mutable module state. |
| **Isolation** - Each test targets single behavior | ✅ PASS | New test files (`test_schema_builder_dialog_live.py`, `test_schema_builder_dialog_dedup.py`, `test_schema_loader_dedup.py`, `test_schema_builder_derived_format.py`) name one behavior per test (e.g. `test_live_preview_tab_renders_table_from_masked_slice`). |
| **Fast Execution** - Tests complete quickly | ✅ PASS | Full suite 1085 tests in ~39s; targeted re-run ~35s. |
| **Determinism** - Consistent results | ✅ PASS | GUI tests run under `QT_QPA_PLATFORM=offscreen`; fixtures are in-memory, no network/DB/temp-file I/O; values are fixed literals. |
| **Readability & Maintainability** - Clear structure | ✅ PASS | Arrange/Act/Assert comments present in the new live and dedup tests; descriptive `test_` names. |

### 1.2 Coverage and Scenarios

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Baseline Coverage Documented** | ✅ PASS | **Baseline (pre-development):** 99.08% lines, 94.10% branches. **Command:** `QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch --cov-report=term-missing`. **Timestamp:** 2026-06-16 (Phase 0). Recorded in `evidence/baseline/baseline-pytest.md` and `evidence/qa-gates/coverage-delta.md`. |
| **No Coverage Regression** | ✅ PASS | **Post-change coverage:** 99.04% lines, 93.43% branches. **Change:** -0.04% lines, -0.67% branches. **Status:** No regression on changed lines; the small aggregate delta is dilution from new well-tested code. Example: "Baseline: 99.08% → Post-change: 99.04% (-0.04%) ✅ PASS (>= 85% threshold)". |
| **New Code Coverage** | ✅ PASS | **New modules:** `_schema_loader_auto_dedup.py` 100%, `_schema_model_specs.py` 100%, `_columns_tab_drop_row.py` 100%, `_schema_dedup_discriminator.py` 100%, `_schema_dialog_surfaces.py` 100%, `_schema_builder_derived_format.py` 95%, `_key_multiselect_widget.py` 96%, `_columns_tab_source_token.py` 90%, `_schema_preview_table.py` 85% line / 75% branch. All meet the uniform >= 85% line / >= 75% branch threshold. |
| **Comprehensive Coverage** | ✅ PASS | New behaviors exercised: window flags, identity multi-line round-trip, derived `=` parse/render, bracket strip/add (incl. names with spaces), double-click insert, columns row chooser + masked value, key multi-select -> `KeySpec`, dedup `auto` model + loader groupby/sum + GUI, preview table render + missing-input messages, AC-9 wired production path. Untested: protocol-only `_columns_tab_protocol.py` (`...`-bodied methods, no logic). |
| **Positive Flows** - Valid inputs | ✅ PASS | e.g. `test_live_preview_tab_renders_table_from_masked_slice`, `test_dedup_auto_assembles_without_discriminator`, `test_key_multiselect_round_trips_through_assemble_schema`. |
| **Negative Flows** - Invalid inputs | ✅ PASS | `test_live_preview_shows_no_source_message_for_blank_slice`, `test_live_preview_shows_specific_missing_input_message`, `test_aggregate_still_requires_discriminator_after_auto_added`, `apply_auto_dedup` raises `ValueError` when no measures (loader tests). |
| **Edge Cases** - Boundary conditions | ✅ PASS | Names with spaces in bracket round-trip; `auto` dedup with no discriminator, no-measure, no-dimension edges; row-chooser bounds; None preview slice. |
| **Error Handling** - Error paths | ✅ PASS | `SchemaValidationError` surfaced as a specific Preview message; `apply_auto_dedup` fail-fast `ValueError`. |
| **Concurrency** - If applicable | N/A | GUI dialog/loader transforms are single-threaded; no concurrency surface introduced. |
| **State Transitions** - If applicable | ✅ PASS | Preview tab-change seam (`QTabWidget.currentChanged`) drives `update_preview`; columns row-chooser state transitions covered. |

### 1.2.1 Per-Language Coverage Comparison

- Python: Baseline: 99.08% line (94.10% branch) -> Post-change: 99.04% line (93.43% branch). Change: -0.04% line (-0.67% branch). New/changed-code coverage: 85%-100% per module, all >= 85% line / >= 75% branch. Disposition: PASS. Evidence: `artifacts/python/lcov.info` (reviewer full-suite run, exit 0), `docs/features/active/2026-06-16-schema-builder-ux-overhaul-72/evidence/qa-gates/coverage-delta.md`, `evidence/qa-gates/final-pytest.md`.
- TypeScript: Baseline: N/A - out of scope -> Post-change: N/A - out of scope. Change: N/A - out of scope. New/changed-code coverage: N/A - out of scope. Disposition: N/A. Evidence: N/A - out of scope (no TypeScript files in branch diff).
- PowerShell: Baseline: N/A - out of scope -> Post-change: N/A - out of scope. Change: N/A - out of scope. New/changed-code coverage: N/A - out of scope. Disposition: N/A. Evidence: N/A - out of scope (no PowerShell files in branch diff).

### 1.3 Test Structure and Diagnostics

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clear Failure Messages** | ✅ PASS | Tests assert concrete strings (e.g. "No source data available to preview") and widget population, producing actionable failures. |
| **Arrange-Act-Assert Pattern** | ✅ PASS | New live/dedup tests label Arrange/Act/Assert phases explicitly. |
| **Document Intent** | ✅ PASS | Descriptive `test_` names plus docstrings on the live integration tests. |

### 1.4 External Dependencies and Environment

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Avoid External Dependencies** | ✅ PASS | No network/DB/external-process dependencies; in-memory `PreviewSlice` and fake services. |
| **Use Mocks/Stubs** | ✅ PASS | Fake schema service and minimal presenter stubs; real pure code paths preferred for loader/format helpers. |
| **Environment Stability** | ✅ PASS | `QT_QPA_PLATFORM=offscreen`; no temporary files created in tests (confirmed by inspection of new test files). |

### 1.5 Policy Audit Requirement

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Pre-submission Review** | ✅ PASS | This document is the required policy review for the change. |

---

## 2. General Code Change Policy Compliance

### 2.1 Before Making Changes

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clarify the objective** | ✅ PASS | Objective captured in `issue.md`, `spec.md`, `user-story.md` (Issue #72). |
| **Read existing change plans** | ✅ PASS | `plan.2026-06-16T14-25.md` present; Phase 0 read-evidence at `evidence/baseline/phase0-instructions-read.md`. |
| **Document the plan** | ✅ PASS | Plan and AC traceability documented in the feature folder. |

### 2.2 Design Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Simplicity first** | ✅ PASS | `apply_auto_dedup` is a single pure pandas transform; bracket handling isolated to one UI format helper. |
| **Reusability** | ✅ PASS | Extracted helpers (`_columns_tab_drop_row.py`, `_columns_tab_source_token.py`, `_schema_dialog_surfaces.py`) reused by the dialog. |
| **Extensibility** | ✅ PASS | `auto` added to `DEDUP_MODES` without breaking existing modes; `ColumnsTabViewProtocol` allows multiple view implementations. |
| **Separation of concerns** | ✅ PASS | Loader transform (`_schema_loader_auto_dedup.py`) separated from GUI; display-only bracket logic separated from formula grammar. |

### 2.3 Module & File Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Cohesive modules** | ✅ PASS | Each new module has a single purpose documented in its module docstring. |
| **Under 500 lines** | ✅ PASS | Reviewer-independent `awk NR` scan of all 32 changed files: max is `schema_builder_dialog.py` at 494 lines; no file exceeds 500. |
| **Public vs internal** | ✅ PASS | Internal helpers are `_`-prefixed; `__all__` declared on new modules. |
| **No circular dependencies** | ✅ PASS | Auto-dedup uses `TYPE_CHECKING`-guarded imports of `SchemaDefinition`; Pyright passes with no circular-import errors. |

### 2.4 Naming, Docs, and Comments

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Descriptive names** | ✅ PASS | `install_preview_refresh_handler`, `apply_auto_dedup`, `preview_rows_from_slice` are descriptive. |
| **Docs/docstrings** | ✅ PASS | New classes/functions carry Google-style docstrings with Args/Returns/Raises/Side effects. |
| **Comment why, not what** | ✅ PASS | Branch/loop intent comments present (e.g. dimension/measure collection in `apply_auto_dedup`). |

### 2.5 After Making Changes - Toolchain Execution

| Requirement | Status | Evidence |
|------------|--------|----------|
| **1. Formatting** | ✅ PASS | **Command:** `poetry run black --check .` **Result:** 255 files unchanged. |
| **2. Linting** | ✅ PASS | **Command:** `poetry run ruff check .` **Result:** All checks passed. |
| **3. Type checking** | ✅ PASS | **Command:** `poetry run pyright` **Result:** 0 errors, 0 warnings, 0 informations. |
| **4. Testing** | ✅ PASS | **Command:** `QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch --cov-report=term-missing` **Result:** 1085 passed, exit 0. |
| **Full toolchain loop** | ✅ PASS | All four stages green in a single reviewer pass. |
| **Explicit reporting** | ✅ PASS | Commands and results recorded here and in `evidence/qa-gates/final-*.md`. |

### 2.6 Summarize and Document

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Summarize changes** | ✅ PASS | Summarized in this audit and `evidence/qa-gates/ac-traceability.md`. |
| **Design choices explained** | ✅ PASS | D-1/D-2/D-3 documented in `spec.md` and verified against the diff (section 3A). |
| **Update supporting documents** | ✅ PASS | Feature folder docs and evidence updated. |
| **Provide next steps** | ✅ PASS | See section 10 Recommendation. |

---

## 3. Language-Specific Code Change Policy Compliance

### Section 3A: Python Code Change Policy Compliance

#### 3A.1 Tooling & Baseline

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Formatting with Black** | ✅ PASS | **Command:** `poetry run black --check .` **Result:** 255 files unchanged. |
| **Linting with Ruff** | ✅ PASS | **Command:** `poetry run ruff check .` **Result:** All checks passed. |
| **Type checking with Pyright** | ✅ PASS | **Command:** `poetry run pyright` **Result:** 0 errors. |
| **Testing with Pytest** | ✅ PASS | **Command:** `QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch` **Result:** 1085 passed. |

#### 3A.2 Python Design & Typing

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Strong typing** | ✅ PASS | Pyright clean; no `Any` introduced in the diff; `apply_auto_dedup` fully annotated. |
| **Dataclasses for value objects** | ✅ PASS | `DedupPolicy` remains a dataclass with `__post_init__` invariant enforcement. |
| **Protocols/ABCs for interfaces** | ✅ PASS | `ColumnsTabViewProtocol` is a `@runtime_checkable typing.Protocol`. |
| **Avoid utility classes** | ✅ PASS | New behavior is module functions (`apply_auto_dedup`, `preview_rows_from_slice`), not static-only classes. |

#### 3A.3 Python Error Handling

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Specific exceptions** | ✅ PASS | `apply_auto_dedup` raises `ValueError` with a specific message; `DedupPolicy` raises `SchemaValidationError`; no broad catches added. |
| **Logging over print** | ✅ PASS | No `print` statements added in the diff. |
| **Invariants at construction** | ✅ PASS | `DedupPolicy.__post_init__` validates mode and discriminator requirement. |

### Design-Decision Verification (D-1, D-2, D-3)

- **D-1 (formula grammar unchanged; bracket handling isolated):** PASS. No formula-engine/`asteval`/`FormulaEvaluator` source file is in the diff. The only `_derived_formula_dialog.py` change adds a UI double-click slot that inserts the display-only `[Name]` form; its docstring confirms the bracket is stripped to `col("Name")` on store/validate. Bracket round-trip logic lives in the new UI helper `_schema_builder_derived_format.py`.
- **D-2 (KeySpec model/loader unchanged):** PASS. `git diff` on `src/_schema_model_specs.py` shows zero changes to the `KeySpec`/`KeyPart` classes; the only edits in that file are the `DEDUP_MODES`/`DedupPolicy` changes for D-3. Key tab is a multi-select that composes ordered `column-ref` parts through the unchanged model.
- **D-3 (DedupPolicy invariant relaxed only for `auto`; LE explicit path preserved):** PASS. The diff adds `"auto"` to `DEDUP_MODES` and narrows the discriminator-required guard to `{"collapse", "aggregate"}` only. `collapse`/`aggregate` still require a discriminator (`test_aggregate_still_requires_discriminator_after_auto_added`); the LE explicit `select_from` path is asserted unchanged (`tests/test_schema_loader_dedup.py::test_le_explicit_select_from_dedup_unchanged`).

---

## 4. Language-Specific Unit Test Policy Compliance

### Section 4A: Python Unit Test Policy Compliance

#### 4A.1 Framework and Scope

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Use Pytest** | ✅ PASS | All tests are pytest; `qtbot` fixture for GUI. |
| **Coverage expectation** | ✅ PASS | Repo-wide 99.04% line / 93.43% branch; new modules 85%-100% line, all >= 75% branch (uniform tier thresholds). |

#### 4A.2 Test Style and Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Focused unit tests** | ✅ PASS | One behavior per test in new files. |
| **Mocking sparingly** | ✅ PASS | Fakes only at the service boundary; pure loader/format helpers tested directly. |
| **Organization** | ✅ PASS | Tests mirror code (`tests/test_schema_loader_dedup.py` for loader; `tests/gui/...` for GUI). |

#### 4A.3 Naming and Readability

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Naming conventions** | ✅ PASS | Descriptive `test_` names. |
| **Docstrings/comments** | ✅ PASS | Integration tests carry docstrings describing the driven seam. |

#### 4A.4 Running the Toolchain

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Use Pytest** | ✅ PASS | **Command:** `QT_QPA_PLATFORM=offscreen poetry run pytest` **Result:** 1085 passed. |
| **No Alternative Test Runners** | ✅ PASS | Pytest only. |

---

## 5. Test Coverage Detail

### Auto-dedup loader (`src/_schema_loader_auto_dedup.py`)

| Test Name | Scenario Type | Lines Covered | Status |
|-----------|--------------|---------------|--------|
| `test_auto_dedup_groups_by_dimensions_sums_measures` | Positive | groupby/sum path | ✅ |
| `test_auto_dedup_no_measures_raises` | Negative | `ValueError` guard | ✅ |
| `test_auto_dedup_no_dimensions_resets_index` | Edge Case | no-dimension branch | ✅ |

**Coverage:** 100% of module.

**Not covered:** None.

### DedupPolicy model (`src/_schema_model_specs.py`)

| Test Name | Scenario Type | Lines Covered | Status |
|-----------|--------------|---------------|--------|
| `test_aggregate_mode_is_a_dedup_mode` | Positive | mode vocabulary | ✅ |
| `test_aggregate_still_requires_discriminator_after_auto_added` | Negative | discriminator guard | ✅ |

**Coverage:** 100% of module.

**Not covered:** None.

### Preview table (`src/gui/widgets/_schema_preview_table.py`)

| Test Name | Scenario Type | Lines Covered | Status |
|-----------|--------------|---------------|--------|
| `test_live_preview_tab_renders_table_from_masked_slice` | Positive | render path | ✅ |
| `test_live_preview_shows_no_source_message_for_blank_slice` | Negative | empty-slice message | ✅ |

**Coverage:** 85% line / 75% branch (meets threshold).

**Not covered:** Minor display-only branches (lines 82, 86, 90); non-blocking.

---

## 6. Test Execution Metrics

| Metric | Value | Status |
|--------|-------|--------|
| Total Tests | 1085 | ✅ |
| Tests Passed | 1085 (100%) | ✅ |
| Tests Failed | 0 | ✅ |
| Execution Time | ~39s total | ✅ Fast |
| Average Time per Test | ~36ms | ✅ Fast |
| Discovery Time | included in run | ✅ |
| Functions/Classes Tested | new modules covered per section 5 | ✅ |
| Test File Size | all changed test files <= 482 lines | ✅ Maintainable |
| Code Coverage | 99.04% lines, 93.43% branches | ✅ |

---

## 7. Code Quality Checks

**For Python:**

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Black Formatting | `poetry run black --check .` | 255 files unchanged | ✅ |
| Ruff Linting | `poetry run ruff check .` | All checks passed | ✅ |
| Pyright Type Checking | `poetry run pyright` | 0 errors | ✅ |
| Pytest Tests | `QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch` | 1085 passed | ✅ |

**Notes:**
No pre-existing failures. `src/gui/_columns_tab_protocol.py` reports as not-imported (0%) under coverage because it is a `typing.Protocol`-only module with `...`-bodied methods; the concrete implementers are covered. This is an informational metric artifact, not a code-quality failure.

---

## 8. Gaps and Exceptions

### Identified Gaps
- **Protocol-only coverage metric:** `src/gui/_columns_tab_protocol.py` reports 0% (module-not-imported) under coverage. Informational only; the module carries no executable logic and its implementers are covered. Not a policy violation.

### Approved Exceptions
**None.** No suppressions were added; no exceptions to policy were required.

### Removed/Skipped Tests
**None.** No tests were removed or skipped; the suite grew from 1049 to 1085.

---

## 9. Summary of Changes

### Commits in This PR/Branch

Branch `feat/schema-builder-ux-overhaul-72` at head `c6a9f955355ac7cc9866fc8ba11f718aa8e7a5ae`, base `main` at merge-base `0a47fef2869b97d8a290d33570ebeee834c80987`. Range `0a47fef..c6a9f95`.

### Files Modified

51 files changed total (4147 insertions, 814 deletions): 32 Python code files plus 19 docs/evidence/feature-folder files. Key production modules:

1. **`src/_schema_loader_auto_dedup.py`** (NEW) — D-3 `auto` dedup transform (dimension groupby, measure sum).
2. **`src/_schema_model_specs.py`** (MODIFIED) — added `auto` to `DEDUP_MODES`; relaxed discriminator invariant for `auto` only.
3. **`src/gui/_schema_open_helpers.py`** (MODIFIED) — `install_preview_refresh_handler` wiring the AC-9 production preview path.
4. **`src/gui/_schema_wiring.py`** (MODIFIED) — `open_schema_builder` installs the preview refresh handler.
5. **`src/gui/widgets/_key_multiselect_widget.py`** (NEW) — D-2 Key tab multi-select.
6. **`src/gui/widgets/_schema_builder_derived_format.py`** (NEW) — D-1 display-only bracket round-trip helper.
7. **`src/gui/widgets/_schema_preview_table.py`** (NEW) — AC-9 tabular preview render.
8. **`src/gui/widgets/_derived_formula_dialog.py`** (MODIFIED) — AC-5 double-click insert slot.

---

## 10. Compliance Verdict

### Overall Status: ✅ FULLY COMPLIANT

All four Python toolchain stages pass in a single reviewer pass; coverage meets the uniform >= 85% line / >= 75% branch thresholds with no regression on changed lines; all 32 changed files are under the 500-line cap; the three resolved design decisions are honored; and AC-9 has a verified production call site driven by an integration test. No blocking findings.

**Fail-closed reminder:** No required artifact is missing; all baseline, QA, and coverage-comparison evidence was independently re-verified.

---

### Policy-by-Policy Summary

#### General Code Change Policy (Section 2)
- ✅ Before Making Changes: documented in feature folder
- ✅ Design Principles: simplicity, reusability, extensibility, separation all met
- ✅ Module & File Structure: all files under 500 lines
- ✅ Naming, Docs, Comments: descriptive, docstrings present
- ✅ Toolchain Execution: all four stages green
- ✅ Summarize & Document: feature folder and evidence updated

#### Language-Specific Code Change Policy (Section 3)

**For Python:**
- ✅ Tooling & Baseline: Black/Ruff/Pyright/Pytest green
- ✅ Python Design & Typing: strong typing, dataclass, protocol usage
- ✅ Error Handling: specific exceptions, invariants at construction

#### General Unit Test Policy (Section 1)
- ✅ Core Principles: independent, isolated, fast, deterministic, readable
- ✅ Coverage & Scenarios: thresholds met, scenarios complete
- ✅ Test Structure: AAA, clear diagnostics
- ✅ External Dependencies: none; fakes used
- ✅ Policy Audit: this document

#### Language-Specific Unit Test Policy (Section 4)

**For Python:**
- ✅ Framework & Scope: pytest, coverage thresholds met
- ✅ Test Style & Structure: focused, mirrored organization
- ✅ Naming & Readability: descriptive names, docstrings
- ✅ Toolchain: pytest only

---

### Metrics Summary

- ✅ 1085/1085 tests passing (100%)
- ✅ New modules covered 85%-100% line, all >= 75% branch
- ✅ 99.04% line coverage / 93.43% branch coverage
- ✅ Proper file organization: tests mirror code; all files under 500 lines
- ✅ All code quality checks passing (Black, Ruff, Pyright)
- ✅ Test execution time: ~39 seconds (fast)

---

### Recommendation

**Ready for merge.**

The change satisfies all in-scope policy requirements with independently verified toolchain evidence. The single informational item (protocol-only 0% coverage metric) requires no action. No remediation is required.

---

## Appendix A: Test Inventory

Representative new/updated tests (full suite: 1085 tests):

- `tests/gui/test_schema_builder_dialog_live.py::test_live_preview_tab_renders_table_from_masked_slice`
- `tests/gui/test_schema_builder_dialog_live.py::test_live_preview_shows_no_source_message_for_blank_slice`
- `tests/gui/test_schema_builder_dialog_live.py::test_live_preview_shows_specific_missing_input_message`
- `tests/gui/test_schema_builder_dialog_live.py::test_live_key_tab_is_multiselect_with_seeded_selection`
- `tests/gui/test_schema_builder_dialog_dedup.py::test_dedup_default_mode_is_auto`
- `tests/gui/test_schema_builder_dialog_dedup.py::test_dedup_auto_assembles_without_discriminator`
- `tests/test_schema_loader_dedup.py::test_le_explicit_select_from_dedup_unchanged`
- `tests/test_schema_loader_dedup.py::test_aggregate_still_requires_discriminator_after_auto_added`
- `tests/gui/test_schema_builder_derived_format.py` (bracket round-trip incl. names with spaces)
- `tests/gui/test_derived_formula_dialog.py::test_double_click_inserts_bracketed_name`
- `tests/gui/test_columns_tab_presenter.py::test_set_preview_row_*`

## Appendix B: Toolchain Commands Reference

**For Python:**
```bash
# Formatting
poetry run black --check .

# Linting
poetry run ruff check .

# Type checking
poetry run pyright

# Testing (GUI offscreen + coverage)
QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch --cov-report=term-missing

# File-size scan (reviewer, awk NR over changed files)
git diff --name-status 0a47fef..c6a9f95 | grep -E '\.py$'
```

---

**Audit Completed By:** feature-review agent
**Audit Date:** 2026-06-16
**Policy Version:** Current (as of audit date)
