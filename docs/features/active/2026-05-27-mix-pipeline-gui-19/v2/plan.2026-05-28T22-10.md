# Atomic Implementation Plan — mix-pipeline-gui (Issue #19) v2

- **Issue:** #19
- **Version:** 2.0
- **Status:** Draft
- **Feature folder:** `docs/features/active/2026-05-27-mix-pipeline-gui-19/`
- **Work Mode:** full-feature (resolved from `v2/issue.md` metadata marker `- Work Mode: full-feature`)
- **Plan generated:** 2026-05-28T22-10
- **Requirements sources:** `v2/spec.md`, `v2/user-story.md`, `v2/issue.md`, `artifacts/research/mix-pipeline-gui-v2-implementation.2026-05-28T14-30.md`
- **Implementing worker:** `atomic-executor`
- **Per-batch budget (hard):** at most 3 production files AND 3 test files per phase
- **Language in scope:** Python (Black -> Ruff -> Pyright strict -> Pytest with coverage)
- **Coverage gate:** line >= 85%, branch >= 75%, no regression on changed lines

## Evidence Location Invariant

All v2 evidence artifacts for this plan are written under the canonical v2 evidence root:
`docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/<kind>/` per
`.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. The `v2/evidence/`
prefix keeps v1 and v2 artifacts cleanly separated under the same feature folder.
Non-canonical locations (`artifacts/baselines/`, `artifacts/baseline/`,
`artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`,
`artifacts/coverage/`) are forbidden. Any supplied non-canonical path is rejected
and replaced with the canonical path; the override is recorded as
`EVIDENCE_LOCATION_OVERRIDE_REJECTED: <supplied> replaced with <canonical>`.

## Reused Modules (do not duplicate, do not modify)

v2 is additive over v1. No v1 commit on `feature/mix-pipeline-gui-19` is reverted.
The following v1 modules are the working base; v2 modifies several in place and
adds one new module. The pure-transform stack is unchanged.

Unchanged transform stack:

- `src/mix_pipeline_run.py` — `run_transforms(aop_vs_le, sku_lu, mix_base, le_raw)`.
- `src/mix_lookups.py` — `build_customer_lu`, `build_aop_norm`, `build_le_norm`, `build_aop_vs_le`, `build_mix_base`.
- `src/mix_transforms.py` — `pivot_le`, `pivot_aop`.
- `src/normalize_le.py` — `load_source`, `normalize`, `validate_tieouts`.
- `src/load_aop.py` — `load_aop(source, *, sheet="AOP1")`.
- `src/load_skulu.py` — `load_skulu(source, *, sheet="SKU_LU")`.
- `src/pandas_io.py` — `read_excel_sheet`, `read_table`, `write_table`.

v1 GUI modules touched by v2 (modified in place):

- `src/gui/protocols.py` — extend `PipelineViewProtocol` with four `set_*_button_enabled` methods.
- `src/gui/main_window.py` — promote `_import_le_btn`, `_import_aop_btn`, `_import_skulu_btn`, `_import_all_btn` (and the existing `run`/`save`/`open`/`export` buttons) to public attributes addressable from the adapter and behavioral tests.
- `src/gui/widgets/source_input_widget.py` — add `_on_tab_changed` slot for the Render Tab re-render path.
- `src/gui/widgets/export_dialog.py` — remove the format combo; format choice now lives in the Save dialog filter.
- `src/gui/exporters/csv_exporter.py` — implement the `<base>_<table>.csv` name-mangling rule.
- `src/gui/presenters/source_selection_presenter.py` — add optional `preview_sink` constructor parameter.
- `src/gui/presenters/pipeline_presenter.py` — add `_last_imported_path`, `on_file_path_changed(key, path)`, `working_tables` property, derived-table invalidation rule, button-state pushes via the extended view protocol.
- `src/gui/app.py` — extend `MainWindowPipelineView`, rewire signals, reorder `_handle_export` (`set_available_tables` before `dialog.exec`), rewrite `default_export_runner` (filter `"Excel (*.xlsx);;CSV (*.csv)"`), accept optional `runner` and chooser parameters, expose `runner` on `WiredApplication`.

v1 GUI modules unchanged:

- `src/gui/pipeline_service.py`, `src/gui/services/workbook_reader.py`, `src/gui/services/db_service.py`, `src/gui/widgets/preview_widget.py`, `src/gui/widgets/source_input_widget.py` (except for the `_on_tab_changed` addition), `src/gui/widgets/progress_dialog.py`, `src/gui/exporters/base.py`, `src/gui/exporters/registry.py`, `src/gui/exporters/excel_exporter.py`, `src/gui/presenters/export_presenter.py`, `src/gui/workers/pipeline_worker.py`.

v2 GUI module added:

- `src/gui/runners.py` — `RunnerProtocol`, `ThreadedRunner`, `SynchronousRunner`. Classified T2 in `quality-tiers.yml`.

v1 fixtures reused by v2 tests (do not duplicate):

- `tests/le_fixtures.py` — `build_workbook` (LE workbook) and `PersistentConnection`/`patch_connect` `sqlite3.connect(":memory:")` round-trip pattern.
- `tests/aop_fixtures.py` — `build_aop_workbook` (AOP workbook).
- `tests/gui/fakes/fake_views.py`, `tests/gui/fakes/fake_services.py`, `tests/gui/fakes/fake_exporters.py` — extended in v2 only to add recording surfaces for the four new view-protocol methods.

No shared SKU_LU workbook builder exists in the test suite. Any v2 test needing a
SKU_LU workbook or a combined LE+AOP+SKU_LU single-workbook fixture must fabricate
it in-file with header on Excel row 1 (`SKU`, `SKU Description`, `Category`,
`International`) and fabricated values only.

## Confidentiality Invariant (per-phase acceptance check)

No real `SKU Description` or `Category` values, customer names, SKU numbers,
prices, or discounts may appear in any new source file, test file, fixture, or
doc. Fabricated values only (for example `Acme Foods`, `SKU-1001`, `Example-A`,
`Category X`). The `Country` values `US` and `Canada` are not secret.

## Dependency and Tier Invariants

- v2 adds no new dependency. PySide6 (`^6.11.1`) and `pytest-qt` are already declared (v1).
- The single new module `src/gui/runners.py` is classified T2 in `quality-tiers.yml` in Phase 1, the phase that creates it. Every other v2 change is to a v1 module already classified.
- Headless Qt tests run under `QT_QPA_PLATFORM=offscreen`, already provided by `tests/gui/conftest.py`.

## Toolchain Loop (run per implementation phase, in order)

`poetry run black .` -> `poetry run ruff check .` -> `poetry run pyright` ->
`QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch --cov-report=term-missing`.
Restart from Black if any step fails or changes files. Per
`.claude/rules/general-unit-test.md`, the banned APIs `time.sleep`,
`QThread.sleep`, `QTest.qWait`, and `qtbot.waitUntil` must not appear in v2
behavioral test code; `qtbot.waitSignal` is reserved for the isolated worker test
where genuinely required.

---

### Phase 0 — Baseline Capture and Policy Reads

Budget: 0 production files, 0 test files (capture only).

- [x] [P0-T1] Read repository policy files in the required order and record an evidence artifact at `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/other/phase0-instructions-read.2026-05-28T22-10.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read: `CLAUDE.md`, `.claude/rules/general-code-change.md`, `.claude/rules/general-unit-test.md`, `.claude/rules/python.md`, `.claude/rules/python-suppressions.md`, `.claude/rules/quality-tiers.md`, `.claude/rules/self-explanatory-code-commenting.md`, `.claude/rules/tonality.md`. Acceptance: artifact exists with all three fields populated.
- [x] [P0-T2] Capture the Black baseline by running `poetry run black --check .` on the current branch HEAD and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/baseline/black.2026-05-28T22-10.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `Output Summary:` states pass/fail and count of files that would be reformatted.
- [x] [P0-T3] Capture the Ruff baseline by running `poetry run ruff check .` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/baseline/ruff.2026-05-28T22-10.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `Output Summary:` records the error count.
- [x] [P0-T4] Capture the Pyright baseline by running `poetry run pyright` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/baseline/pyright.2026-05-28T22-10.md` with the four schema fields. Acceptance: `Output Summary:` records error and warning counts.
- [x] [P0-T5] Capture the Pytest coverage baseline by running `QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch --cov-report=term-missing` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/baseline/pytest-coverage.2026-05-28T22-10.md` with the four schema fields. Acceptance: `Output Summary:` records numeric baseline line-coverage % and branch-coverage % and the passed/failed test counts.
- [x] [P0-T6] Confirm the Qt runtime preconditions by running `poetry run python -c "import PySide6; print(PySide6.__version__)"` and `$env:QT_QPA_PLATFORM='offscreen'; poetry run python -c "from PySide6.QtWidgets import QApplication; import sys; app = QApplication(sys.argv); print('offscreen-ok')"`, then write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/baseline/qt-runtime.2026-05-28T22-10.md` with the four schema fields. Acceptance: `Output Summary:` records the PySide6 version and that a `QApplication` constructed under `QT_QPA_PLATFORM=offscreen`. If the `offscreen` plugin does not load, the phase outcome is remediation-required, not PASS.

---

### Phase 1 — Runner Abstraction and View-Protocol Extensions

Budget: 2 production files (`src/gui/runners.py`, `src/gui/protocols.py`), 1 test file (`tests/gui/test_runners.py`). DAG layer: protocol seam for AC-6 deterministic dispatch and presenter button control. Per research Q3 and Q6.

- [x] [P1-T1] Create `src/gui/runners.py` defining (a) `RunnerProtocol` with method `run(task: Callable[[], dict[str, pd.DataFrame]], on_success: Callable[[dict[str, pd.DataFrame]], None], on_error: Callable[[str], None]) -> None` (b) class `ThreadedRunner` whose `run` constructs `PipelineWorker(task)`, moves it to a `QThread`, wires `worker.finished` → `on_success`, `worker.error` → `on_error`, and starts the thread (the production default per spec section 4 / research Q6); (c) class `SynchronousRunner` whose `run` invokes `task()` directly, calls `on_success(result)` on return and `on_error(str(exc))` on exception (the test seam per Decision 3). Full type hints; Google-style docstrings; the broad `except Exception` in `SynchronousRunner.run` is a defined boundary that re-routes to `on_error`. Add `quality-tiers.yml` entry `src/gui/runners.py: T2`. Acceptance: file exists, <= 500 lines, exposes the three named symbols with the listed signatures; `quality-tiers.yml` lists the module; `poetry run pyright src/gui/runners.py` exits 0.
- [x] [P1-T2] Update `src/gui/protocols.py` to add four new methods to `PipelineViewProtocol`: `set_import_button_enabled(key: str, enabled: bool) -> None`, `set_run_button_enabled(enabled: bool) -> None`, `set_save_button_enabled(enabled: bool) -> None`, `set_export_button_enabled(enabled: bool) -> None`. Update the existing module-level `__all__` to include any new names if added. No Qt imports. Full type hints; Google-style docstrings explaining the contract per research Q3. Acceptance: `src/gui/protocols.py` is <= 500 lines; `grep -E "set_import_button_enabled|set_run_button_enabled|set_save_button_enabled|set_export_button_enabled" src/gui/protocols.py` matches four method signatures inside `PipelineViewProtocol`; the file still has no Qt import; `poetry run pyright src/gui/protocols.py` exits 0.
- [x] [P1-T3] Create `tests/gui/test_runners.py` (no `QApplication` for the `SynchronousRunner` cases) covering: (a) `SynchronousRunner.run` with a success task calls `on_success` with the task result and does not call `on_error`; (b) `SynchronousRunner.run` with a task that raises `ValueError("boom")` calls `on_error("boom")` and does not call `on_success`; (c) `SynchronousRunner.run` with a task that raises a non-`ValueError` exception still routes to `on_error` with the stringified message; (d) a structural check that `ThreadedRunner` implements `RunnerProtocol` (via `isinstance` against a `runtime_checkable` Protocol, or a direct `hasattr` check on `run`). Use only deterministic in-memory callables; no `time.sleep`, `QThread.sleep`, `QTest.qWait`, or `qtbot.waitUntil`. Arrange-Act-Assert. Acceptance: file exists, <= 500 lines, all four test cases pass; `poetry run pytest tests/gui/test_runners.py -q` exits 0.
- [x] [P1-T4] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase1-qa.2026-05-28T22-10.md` (per-command schema). Order: Black -> Ruff -> Pyright -> `QT_QPA_PLATFORM=offscreen` Pytest with coverage. Restart from Black on any failure/change. Acceptance: all four exit 0 in one pass; coverage >= 85% line / >= 75% branch with no regression on changed lines; no unauthorized suppressions; confidentiality check passes.

---

### Phase 2 — MainWindow Button Promotion and MainWindowPipelineView Extension

Budget: 2 production files (`src/gui/main_window.py`, `src/gui/app.py`), 2 test files (`tests/gui/test_main_window.py`, `tests/gui/test_app_wiring.py`). DAG layer: addressable button surface and adapter implementation of the four new protocol methods per research Q3.

- [x] [P2-T1] Update `src/gui/main_window.py` to promote the four import buttons from private to public attributes: rename `_import_le_btn` → `import_le_btn`, `_import_aop_btn` → `import_aop_btn`, `_import_skulu_btn` → `import_skulu_btn`, `_import_all_btn` → `import_all_btn`. Confirm `run_btn`, `save_btn`, `open_btn`, `export_btn` are already public (or promote them in this task if still private). Update all in-file references. No new behavior. Acceptance: `grep -E "self\._import_(le|aop|skulu|all)_btn" src/gui/main_window.py` returns no matches; `grep -E "self\.import_(le|aop|skulu|all)_btn" src/gui/main_window.py` returns the four button assignments; file is <= 500 lines.
- [x] [P2-T2] Update `src/gui/app.py::MainWindowPipelineView` to implement the four new methods from P1-T2. (a) `set_import_button_enabled(key, enabled)` maps `"LE"` → `self._window.import_le_btn`, `"aop"` → `self._window.import_aop_btn`, `"sku_lu"` → `self._window.import_skulu_btn` and calls `setEnabled(enabled)`; after each update it recomputes `import_all_btn.setEnabled(any(btn.isEnabled() for btn in [import_le_btn, import_aop_btn, import_skulu_btn]))`; (b) `set_run_button_enabled(enabled)` routes to `self._window.run_btn.setEnabled`; (c) `set_save_button_enabled(enabled)` routes to `self._window.save_btn.setEnabled`; (d) `set_export_button_enabled(enabled)` routes to `self._window.export_btn.setEnabled`. Full type hints; Google-style docstrings. Acceptance: `MainWindowPipelineView` now has four new methods with the listed bodies; `poetry run pyright src/gui/app.py` exits 0; file remains <= 500 lines.
- [x] [P2-T3] Update `tests/gui/test_main_window.py` (or create if absent) with a `qtbot`-driven test asserting the four import buttons are addressable as public attributes on a constructed `MainWindow`: instantiate `MainWindow(qt_app=qtbot.qapp)` (or whatever v1 constructor is), `qtbot.addWidget(window)`, then assert `hasattr(window, "import_le_btn") and window.import_le_btn.isEnabled() is True` (initial state), and equivalent for `import_aop_btn`, `import_skulu_btn`, `import_all_btn`, `run_btn`, `save_btn`, `open_btn`, `export_btn`. No `qtbot.waitUntil`. Acceptance: file exists, <= 500 lines, the public-attribute assertions pass under `QT_QPA_PLATFORM=offscreen`.
- [x] [P2-T4] Update `tests/gui/test_app_wiring.py` (existing) to add unit coverage of the four new `MainWindowPipelineView` methods using the in-test `MainWindow`: assert `set_import_button_enabled("LE", False)` disables `import_le_btn`; `set_import_button_enabled("LE", True)` re-enables it; setting one key recomputes `import_all_btn` to `any(...)`; `set_run_button_enabled(False)` disables `run_btn`; same for save and export. No interaction with presenters in these tests. Acceptance: existing tests in `test_app_wiring.py` continue to pass; new tests cover the four routing methods; file remains <= 500 lines.
- [x] [P2-T5] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase2-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch, no regression on changed lines.

---

### Phase 3 — Render Tab Re-render and Preview Sink

Budget: 2 production files (`src/gui/widgets/source_input_widget.py`, `src/gui/presenters/source_selection_presenter.py`), 2 test files (`tests/gui/test_source_input_widget.py`, `tests/gui/test_source_selection_presenter.py`). DAG layer: AC-1. Per research Q1.

- [x] [P3-T1] Update `src/gui/widgets/source_input_widget.py` to add a private slot `_on_tab_changed(self, sheet: str) -> None` connected to `self._tab_combo.currentTextChanged` in `__init__`. The body, per spec section 1: `if self._render_checkbox.isChecked() and self._path and sheet: self.render_tab_requested.emit(self._path, sheet)`. No new signal; reuse `render_tab_requested`. `_on_render_toggled` is unchanged from v1 — it emits `render_tab_requested` on `checked=True` with a valid path and sheet, and is a no-op on `checked=False`. The new `_on_tab_changed` slot re-emits `render_tab_requested` when the checkbox is already checked and the user picks a different tab. The uncheck-clears-preview path is NOT wired inside the widget; it is wired in `build_application` per P7-T1 paragraph (d) by connecting each `SourceInputWidget._render_checkbox.toggled` to a closure that calls `window.preview_widget.show_preview([])` when `checked` is False. Expose `_render_checkbox` via a public read-only accessor `render_checkbox` (a property returning the underlying checkbox) so the composition root can wire the toggled signal without reaching into private state. Document the rationale in a Google-style docstring referencing AC-1 tab-change re-render. Acceptance: `grep -n "_on_tab_changed" src/gui/widgets/source_input_widget.py` finds the new connection and slot; `SourceInputWidget` exposes a public `render_checkbox` accessor returning the internal `QCheckBox` so the composition root can connect to its `toggled` signal; the file is <= 500 lines; `poetry run pyright src/gui/widgets/source_input_widget.py` exits 0.
- [x] [P3-T2] Update `src/gui/presenters/source_selection_presenter.py::SourceSelectionPresenter.__init__` to add a keyword-only optional parameter `preview_sink: SourceSelectionViewProtocol | None = None` and store it on `self._preview_sink`. Update `on_render_tab(path, sheet)` to additionally call `self._preview_sink.show_preview(rows)` after `self._view.show_preview(rows)` when `self._preview_sink is not None`. Add a new method `on_clear_preview(self) -> None` (or extend the existing clear path) that calls `self._view.show_preview([])` and, when set, `self._preview_sink.show_preview([])`, used when the checkbox is unchecked. Full type hints; Google-style docstrings citing research Q1 Option A. Acceptance: the constructor signature has the keyword-only `preview_sink` parameter with default `None`; `on_render_tab` calls `preview_sink.show_preview` when set; the file remains <= 500 lines; existing tests in `tests/gui/test_source_selection_presenter.py` still pass without modification (parameter defaults to `None`).
- [x] [P3-T3] Extend `tests/gui/test_source_input_widget.py` with three new tests (Qt; `qtbot`, offscreen): (a) when the checkbox is checked and a path is set, changing `self._tab_combo.setCurrentIndex(1)` emits `render_tab_requested` with `(path, sheet1)`; (b) when the checkbox is unchecked and a path is set, changing the tab does NOT emit `render_tab_requested`; (c) when the checkbox is checked but no path is set, changing the tab does NOT emit (signal capture via `qtbot.waitSignal(..., timeout=100, raising=False)` with negative assertion is permitted ONLY here because the test inspects emission state without polling external systems; prefer a `Signal`-capture list captured before the action and asserted after). Acceptance: file exists, <= 500 lines, all new tests pass under `QT_QPA_PLATFORM=offscreen`; no `qtbot.waitUntil` or `QTest.qWait`.
- [x] [P3-T4] Extend `tests/gui/test_source_selection_presenter.py` with three new tests (no `QApplication`): (a) when `preview_sink` is provided, `on_render_tab(path, sheet)` calls `preview_sink.show_preview(rows)` exactly once with the same rows pushed to the primary `view`; (b) `on_clear_preview()` calls `preview_sink.show_preview([])`; (c) when `preview_sink is None`, `on_render_tab` does not raise and only calls `view.show_preview`. Use a `FakeSourceSelectionView` for both view and sink. Acceptance: all three new tests pass; the file remains <= 500 lines.
- [x] [P3-T5] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase3-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 4 — PipelinePresenter file-path tracking, working_tables, derived invalidation

Budget: 1 production file (`src/gui/presenters/pipeline_presenter.py`), 1 test file (`tests/gui/test_pipeline_presenter.py`). DAG layer: AC-2/3/4/6/7/9 presenter contract per research Q2 and Q3.

- [x] [P4-T1] Update `src/gui/presenters/pipeline_presenter.py` to make the following additive changes: (a) declare `self._last_imported_path: dict[str, str | None] = {"LE": None, "aop": None, "sku_lu": None}` in `__init__`; (b) add a public `@property working_tables(self) -> dict[str, pd.DataFrame]: return self._derived_tables if self._derived_tables else self._imported_tables` per spec section "working_tables property"; (c) add `on_file_path_changed(self, key: str, path: str) -> None` that, per spec section 2, calls `self._view.set_import_button_enabled(key, True)` when `path != self._last_imported_path[key]` (no-op when equal, including the `"db:<path>"` sentinel case); (d) update `on_import_one(name)` and `on_import_all(spec)` so that on success they set `self._last_imported_path[key] = path` for each successfully-imported key, call `self._view.set_import_button_enabled(key, False)`, and invalidate derived tables by setting `self._derived_tables = {}` (per spec "Derived-table invalidation rule"); on `ValueError` they call `self._view.show_error(...)` and leave button state unchanged; (e) update `on_open_db(path)` so that for every key in `{"LE", "aop", "sku_lu"}` whose name appears in the loaded set it calls `self._view.set_import_button_enabled(key, False)` and sets `self._last_imported_path[key] = f"db:{path}"` (sentinel per research Q4), then calls `self._view.set_run_button_enabled(True)` when at least one key was loaded; (f) update `on_save(...)` and any export-related guards to use `self.working_tables`; update Save/Export enable pushes so `set_save_button_enabled(True)` and `set_export_button_enabled(True)` are called when `self.working_tables` is non-empty and `False` when empty (transitions: after import success, after open success, after run success, after a Save/Export that does not modify state; on derived invalidation if working set is still non-empty via imported_tables, leave enabled); (g) update `can_run(self) -> bool: return bool(self.working_tables)` so Open also enables Run per spec section 4; (h) add `on_run_success(self, derived: dict[str, pd.DataFrame]) -> None` that delegates to `self.apply_run_result(derived)` and then calls `self._view.set_run_button_enabled(True)`; (i) add `on_run_error(self, message: str) -> None` that calls `self._view.show_error(message)`, calls `self._set_running(False)`, and calls `self._view.set_run_button_enabled(True)`. Full type hints; Google-style docstrings updated for each modified method; decision-logic comments on the sentinel and invalidation rules. Acceptance: file exists, <= 500 lines, the listed members are present with the listed bodies (including `on_run_success` and `on_run_error`); `poetry run pyright src/gui/presenters/pipeline_presenter.py` exits 0; no Qt import.
- [x] [P4-T2] Extend `tests/gui/test_pipeline_presenter.py` with the following new tests using the v1 `FakePipelineService`/`FakePipelineView` (extended in P4-T1 dependencies; the fakes already have recording surfaces from v1 — extend `FakePipelineView` in `tests/gui/fakes/fake_views.py` ONLY to add the four `set_*_button_enabled` recording methods, counted under this phase's test-file count): (a) `on_import_one("LE", spec)` success calls `view.set_import_button_enabled("LE", False)` and stores `_last_imported_path["LE"] == spec.le_path`; subsequent `on_file_path_changed("LE", spec.le_path)` does NOT call `set_import_button_enabled` (same path); subsequent `on_file_path_changed("LE", "different.xlsx")` DOES call `view.set_import_button_enabled("LE", True)`; (b) same for `"aop"` and `"sku_lu"`; (c) `on_import_all(spec)` full-success calls `set_import_button_enabled(key, False)` for all three keys; partial-success (one loader raises `ValueError`) calls `show_error` and only disables successful keys; (d) derived-table invalidation: after `on_run()` populates `_derived_tables`, a subsequent `on_import_one("LE", spec)` success resets `_derived_tables == {}`; (e) `working_tables` returns `_derived_tables` when non-empty, else `_imported_tables`; (f) `on_open_db("test.db")` success with a loaded set of `{"LE": ..., "aop": ..., "sku_lu": ...}` calls `set_import_button_enabled(key, False)` for each, sets `_last_imported_path[key] == "db:test.db"` per key, calls `set_run_button_enabled(True)`; subsequent `on_file_path_changed("LE", "new.xlsx")` re-enables only LE (calls `set_import_button_enabled("LE", True)`); (g) at least one `hypothesis` property test for `on_file_path_changed` (T2 density): for any non-empty string `p` and any starting `last`, calling `on_file_path_changed(key, p)` calls `set_import_button_enabled(key, True)` iff `p != last`; (h) `on_run_success(derived)` records `_derived_tables == derived` (via `apply_run_result`) and calls `view.set_run_button_enabled(True)` exactly once; (i) `on_run_error("boom")` calls `view.show_error("boom")`, clears the running state (`_set_running(False)`), and calls `view.set_run_button_enabled(True)` exactly once. Arrange-Act-Assert; fabricated data only. Acceptance: file remains <= 500 lines (split into a sibling test module if it would exceed; the split file counts against this phase's test-file budget); all new tests pass; existing tests pass.
- [x] [P4-T3] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase4-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 5 — ExportDialog Simplification and default_export_runner Rewrite

Budget: 2 production files (`src/gui/widgets/export_dialog.py`, `src/gui/app.py`), 2 test files (`tests/gui/test_export_dialog.py`, `tests/gui/test_app_wiring_defaults.py`). DAG layer: AC-9 Defects 1 and 2 per spec section 7 and research Q5.

- [x] [P5-T1] Update `src/gui/widgets/export_dialog.py` to remove the format combo (`QComboBox`). Format selection is moved to the Save dialog filter per Decision 2. Retain the per-table checklist (`QListWidget`) and the Export-All control. Drop any constructor or method parameter that referenced the registry to populate the combo. Update `ExportViewProtocol` adherence: `ExportDialog` no longer exposes a format-selection method. Update the Google-style docstring to cite Decision 2. Acceptance: `grep -n "QComboBox\|format" src/gui/widgets/export_dialog.py` shows no remaining format-combo references; the file remains <= 500 lines; the dialog still implements `set_table_list`, `get_selected_names`, and `select_all_tables`.
- [x] [P5-T2] Update `src/gui/app.py`. (a) In `_handle_export`: call `export_presenter.set_available_tables(list(pipeline_presenter.working_tables.keys()))` BEFORE invoking `export_dialog_runner(export_dialog)` (Defect 1 fix). (b) Rewrite `default_export_runner(export_dialog: ExportDialog) -> tuple[str, str] | None` per spec section 7: first call `export_dialog.exec()`; if it returns the rejected code, return `None`; otherwise call `path, selected_filter = QFileDialog.getSaveFileName(export_dialog, "Export Destination", "", "Excel (*.xlsx);;CSV (*.csv)")`; if `path` is the empty string, return `None`; otherwise return `("Excel", path)` when `"*.xlsx" in selected_filter` and `("CSV", path)` when `"*.csv" in selected_filter`. (c) Confirm `wire_control_signals` passes `pipeline_presenter.working_tables, format_name, destination_path` into `export_presenter.on_export(tables, format_name, destination_path)`. Do not introduce a new presenter signature; the v1 signature is correct and stays. Full type hints; Google-style docstrings citing Decision 2 / research Q5. Acceptance: `_handle_export` calls `set_available_tables` before the dialog; `default_export_runner` matches the listed body; `poetry run pyright src/gui/app.py` exits 0; file remains <= 500 lines.
- [x] [P5-T3] Update `tests/gui/test_export_dialog.py` to remove every assertion about the format combo (Defect 2 fix; combo is gone). Add positive tests that `set_table_list(["LE", "aop", "sku_lu"])` populates three checklist items; checking the first two then `get_selected_names() == ["LE", "aop"]`; `select_all_tables()` checks all three. Acceptance: tests pass under `QT_QPA_PLATFORM=offscreen`; the file remains <= 500 lines; `grep -n "QComboBox\|format" tests/gui/test_export_dialog.py` shows no remaining combo assertions.
- [x] [P5-T4] Update `tests/gui/test_app_wiring_defaults.py` to replace any prior dialog.exec assertions with the v2 filter-string parse contract. Add four tests: (a) `default_export_runner` with a stub `ExportDialog.exec()` returning the accepted code and a stub `QFileDialog.getSaveFileName` returning `("C:/out.xlsx", "Excel (*.xlsx)")` returns `("Excel", "C:/out.xlsx")`; (b) same stubs returning `("C:/out.csv", "CSV (*.csv)")` return `("CSV", "C:/out.csv")`; (c) `ExportDialog.exec()` returning the rejected code returns `None`; (d) `getSaveFileName` returning `("", "")` returns `None`. Use `monkeypatch.setattr` on `QFileDialog.getSaveFileName` to stub the static call deterministically; no live file dialog. Acceptance: all four tests pass; file is <= 500 lines.
- [x] [P5-T5] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase5-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 6 — CsvExporter Name-Mangling Rule

Budget: 1 production file (`src/gui/exporters/csv_exporter.py`), 1 test file (`tests/gui/test_csv_exporter.py`). DAG layer: AC-9 Defect 3 per spec section 7 and Decision 1.

- [x] [P6-T1] Update `src/gui/exporters/csv_exporter.py::CsvExporter.export(tables, selected_names, destination_path)` to implement the name-mangling rule per spec section 7 and Decision 1. Compute `base` as the filename portion of `destination_path` with a trailing `.csv` (case-insensitive) stripped; compute `directory` as `os.path.dirname(destination_path)` (or use `pathlib.PurePath`). For each table name in `selected_names`, write the table's DataFrame as `<directory>/<base>_<table>.csv`. If `directory` is empty, write to the current directory. Wrap the `to_csv` call in the typed-boundary style (no per-call type suppression). Full type hints; Google-style docstring citing Decision 1; intent comment on the per-table loop. Acceptance: file remains <= 500 lines; `CsvExporter.format_name == "CSV"`; `poetry run pyright src/gui/exporters/csv_exporter.py` exits 0.
- [x] [P6-T2] Update `tests/gui/test_csv_exporter.py` to cover the name-mangling rule using an injected in-memory write seam (a per-table writer callable injected via constructor or a monkeypatched `to_csv` that records `(path, frame)` per call; no temp files). Add tests: (a) `destination_path == "C:/out/results.csv"`, `selected_names == ["LE"]` writes one frame to `C:/out/results_LE.csv`; (b) `selected_names == ["LE", "aop", "sku_lu"]` writes three files `C:/out/results_LE.csv`, `C:/out/results_aop.csv`, `C:/out/results_sku_lu.csv`; (c) `destination_path == "results.csv"` (no directory) writes `results_LE.csv`; (d) `destination_path == "C:/out/results"` (no `.csv` extension) writes `C:/out/results_LE.csv` (base equals the unmodified filename); (e) `selected_names` is empty: no write occurs (presenter is responsible for empty-selection rejection but the exporter must not crash); (f) `CsvExporter.format_name == "CSV"`. Use fabricated data only. Acceptance: all six tests pass; file remains <= 500 lines; no temp files; no confidential values.
- [x] [P6-T3] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase6-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 7 — wire_control_signals and build_application Rewiring

Budget: 1 production file (`src/gui/app.py`), 2 test files (`tests/gui/test_app_wiring.py`, `tests/gui/test_app_composition.py`). DAG layer: composition-root wiring for AC-1 preview sink, AC-2/3/4/5 per-input file-path tracking, AC-6 runner dispatch, AC-7 Save, AC-9 Export. Per research Q3, Q4, Q6.

- [x] [P7-T1] Update `src/gui/app.py` with the following composition changes. (a) `build_application(..., runner: RunnerProtocol | None = None, save_path_chooser: Callable[[], str | None] | None = None, open_path_chooser: Callable[[], str | None] | None = None, export_dialog_runner: Callable[[ExportDialog], tuple[str, str] | None] | None = None, pipeline_service: PipelineServiceProtocol | None = None, workbook_reader: WorkbookReaderProtocol | None = None)`: when `runner is None`, default to `ThreadedRunner()` per spec section 4; when each chooser is `None`, use the production default (the existing v1 `QFileDialog` wrapper for save/open, and `default_export_runner` for export); when `pipeline_service is None`, default to `PipelineService(db_service=DbService())` (the existing v1 wiring); when `workbook_reader is None`, default to `WorkbookReader()`. The existing local construction lines are replaced by `pipeline_service = pipeline_service if pipeline_service is not None else PipelineService(db_service=DbService())` and the analogous `workbook_reader = workbook_reader if workbook_reader is not None else WorkbookReader()`. The injected `pipeline_service` and `workbook_reader` are consumed by `build_application` itself and are used by P8-T3 and P9-T2 to inject `FakePipelineService` and a fake `WorkbookReader` respectively. Store `runner` on `WiredApplication` so behavioral tests can address it. Per-task acceptance check: `src/gui/app.py` is 404 lines at HEAD and must stay <= 500 lines after the P7 changes; if any task in this phase pushes it over the cap, P7 must split a helper (for example, the `_handle_*` closures) into `src/gui/_wiring.py` within the same phase so both files remain under cap. (b) When constructing each `SourceSelectionPresenter`, pass `preview_sink=window.preview_widget` per spec section 1 / research Q1 Option A. (c) Wire each `SourceInputWidget.file_selected` (the existing v1 signal carrying the selected path) to a closure `lambda p, k=key: pipeline_presenter.on_file_path_changed(k, p)` for `key in ("LE", "aop", "sku_lu")` per research Q3. (d) Connect each of `le_widget.render_checkbox.toggled`, `aop_widget.render_checkbox.toggled`, `skulu_widget.render_checkbox.toggled` to a closure that calls `window.preview_widget.show_preview([])` when the new checked state is False, per spec section 1. (e) Update `_handle_run` to dispatch through `self._runner.run(task=pipeline_presenter.make_run_task(), on_success=pipeline_presenter.on_run_success, on_error=pipeline_presenter.on_run_error)` so AC-6 runs through the injected runner (the v1 `make_run_task()` builder is reused; `on_run_success` and `on_run_error` are added in P4-T1). (f) Update `_handle_save` to use `pipeline_presenter.working_tables` and the injected `save_path_chooser`. (g) Update `_handle_export` so `set_available_tables` is invoked BEFORE the dialog (per P5-T2) and so the closure first reads `pipeline_presenter.working_tables` and then passes it as the first argument to `export_presenter.on_export(tables, format_name, destination_path)` (the v1 three-argument signature stays). Full type hints; Google-style docstrings citing the four research questions. Acceptance: `build_application` signature accepts the six new keyword-only parameters (`runner`, `save_path_chooser`, `open_path_chooser`, `export_dialog_runner`, `pipeline_service`, `workbook_reader`) each with `None` defaults; `WiredApplication` exposes `runner`; the four new connections exist; `poetry run pyright src/gui/app.py` exits 0; file remains <= 500 lines (helper split into `src/gui/_wiring.py` performed within this phase if required to stay under cap).
- [x] [P7-T2] Extend `tests/gui/test_app_wiring.py` with the following new tests using fakes for choosers and `SynchronousRunner`: (a) `build_application(runner=SynchronousRunner())` returns a `WiredApplication` whose `runner` attribute is the injected `SynchronousRunner`; (b) when `runner is None`, the resulting `WiredApplication.runner` is an instance of `ThreadedRunner`; (c) each `SourceSelectionPresenter` constructed in `build_application` has its `preview_sink` set to the `window.preview_widget`; (d) emitting `file_selected("path/le.xlsx")` on `window.le_widget` triggers `pipeline_presenter.on_file_path_changed("LE", "path/le.xlsx")` (use a recording fake or a spy installed on the presenter method); (e) toggling the Render Tab checkbox to `False` on `window.le_widget` triggers the preview-clear path (preview_widget model row count goes to 0); (f) `build_application(pipeline_service=fake_service, workbook_reader=fake_reader)` returns a `WiredApplication` whose internal collaborators are the injected fakes (asserted by retrieving the constructed presenter's `_service` attribute and the source-selection presenter's reader). No `qtbot.waitUntil`. Acceptance: all six new tests pass; existing tests still pass; file remains <= 500 lines.
- [x] [P7-T3] Extend `tests/gui/test_app_composition.py` (existing) with a `qtbot`-driven smoke test that constructs `wired = build_application(qt_app=qtbot.qapp, runner=SynchronousRunner(), save_path_chooser=lambda: None, open_path_chooser=lambda: None, export_dialog_runner=lambda dialog: None)`, calls `qtbot.addWidget(wired.window)`, and asserts: (a) `wired.runner` is a `SynchronousRunner`; (b) all four import buttons are enabled before any action; (c) `wired.window.preview_widget.model().rowCount() == 0` initially; (d) the `ExporterRegistry` still reports `["Excel", "CSV"]`. Acceptance: file remains <= 500 lines; the smoke test passes under `QT_QPA_PLATFORM=offscreen`.
- [x] [P7-T4] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase7-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 8 — Behavioral Integration Tests (AC-1 through AC-5)

Budget: 0 production files, 3 test files (`tests/gui/integration/__init__.py`, `tests/gui/integration/test_behavioral_preview.py`, `tests/gui/integration/test_behavioral_import_buttons.py`). DAG layer: AC-1, AC-2, AC-3, AC-4, AC-5 button-driven behavioral checks per spec Definition of Done. Per research Q1 model-based assertions and Q3 per-input mechanics.

- [x] [P8-T1] Create `tests/gui/integration/__init__.py` as an empty package marker (module docstring only). Acceptance: file exists, <= 500 lines, package collects under `pytest tests/gui/integration`.
- [x] [P8-T2] Create `tests/gui/integration/test_behavioral_preview.py` covering AC-1: (a) construct `wired = build_application(qt_app=qtbot.qapp, runner=SynchronousRunner(), save_path_chooser=lambda: None, open_path_chooser=lambda: None, export_dialog_runner=lambda d: None)`; `qtbot.addWidget(wired.window)`; load a two-tab LE workbook via the existing v1 file-selection test seam (use the `BytesIO` LE builder; install via the v1 fake `WorkbookReader` path or via direct presenter call `source_presenter.on_file_selected(le_path)`); (b) emit the Render Tab checkbox `toggled(True)` and assert `wired.window.preview_widget.model().rowCount() > 0` (per research Q1 model-based assertion, deterministic under offscreen — no pixel comparison, no wait primitive); (c) change `wired.window.le_widget._tab_combo.setCurrentIndex(1)`; assert `wired.window.preview_widget.model().item(0, 0).text()` changed from the prior value; (d) emit `toggled(False)`; assert `wired.window.preview_widget.model().rowCount() == 0`; (e) repeat the toggle-on / tab-change / toggle-off cycle for `aop_widget` and `skulu_widget`. Use only direct model inspection — NO `qtbot.waitUntil`, NO `qtbot.waitSignal`, NO `QTest.qWait`, NO `time.sleep`. Fabricated values only. Acceptance: file exists, <= 500 lines, all per-widget tests pass under `QT_QPA_PLATFORM=offscreen`; banned APIs grep returns no matches.
- [x] [P8-T3] Create `tests/gui/integration/test_behavioral_import_buttons.py` covering AC-2, AC-3, AC-4, AC-5: (a) construct `wired` as in P8-T2 but with a `FakePipelineService` injected via `build_application` so the loader paths are deterministic; (b) AC-2: select an LE file/sheet, `qtbot.mouseClick(wired.window.import_le_btn, Qt.LeftButton)`; assert `wired.window.import_le_btn.isEnabled() is False` immediately (synchronous via `SynchronousRunner` path or direct presenter slot); assert `"LE" in wired.pipeline_presenter._imported_tables`; emit `file_selected` again with the SAME path on `le_widget`; assert `import_le_btn` stays disabled; emit `file_selected("different.xlsx")`; assert `import_le_btn.isEnabled() is True`; (c) AC-3 and AC-4: same pattern for `import_aop_btn` / `aop_widget` (key `"aop"`) and `import_skulu_btn` / `skulu_widget` (key `"sku_lu"`); (d) AC-2 negative: configure `FakePipelineService.import_le` to raise `ValueError("bad LE")`; click `import_le_btn`; assert `import_le_btn.isEnabled() is True`; assert the status surface (via the fake or via `pipeline_view` recording) recorded the error; (e) AC-5 full-success: select all three inputs, click `wired.window.import_all_btn`; assert all four import buttons are disabled (`import_le_btn`, `import_aop_btn`, `import_skulu_btn`, `import_all_btn`); change LE path via `file_selected("new_le.xlsx")`; assert only `import_le_btn` and `import_all_btn` are re-enabled; (f) AC-5 partial-success: configure `FakePipelineService.import_aop` to raise `ValueError`; click `import_all_btn`; assert `import_le_btn` and `import_skulu_btn` disabled, `import_aop_btn` enabled, `import_all_btn` enabled. No polling primitives. Fabricated values only. Acceptance: file exists, <= 500 lines, all listed behavioral checks pass under `QT_QPA_PLATFORM=offscreen`; banned APIs grep returns no matches.
- [x] [P8-T4] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase8-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 9 — Behavioral Integration Tests (AC-6 through AC-9)

Budget: 0 production files, 3 test files (`tests/gui/integration/test_behavioral_pipeline_run.py`, `tests/gui/integration/test_behavioral_dialogs.py`, `tests/gui/integration/test_behavioral_composition.py`). DAG layer: AC-6 Run, AC-7 Save, AC-8 Open, AC-9 Export, AC-10 composition. Per spec Definition of Done and Decision 3.

- [x] [P9-T1] Create `tests/gui/integration/test_behavioral_pipeline_run.py` covering AC-6: construct `wired = build_application(qt_app=qtbot.qapp, runner=SynchronousRunner(), save_path_chooser=lambda: None, open_path_chooser=lambda: None, export_dialog_runner=lambda d: None)`; populate `pipeline_presenter._imported_tables` with a fabricated three-key dict; assert `wired.window.run_btn.isEnabled() is True`; `qtbot.mouseClick(wired.window.run_btn, Qt.LeftButton)`; immediately (no wait primitive — `SynchronousRunner.run` returns synchronously per spec section 4) assert `wired.pipeline_presenter._derived_tables != {}`; assert the result was reported via the `view.show_result` recording surface. Negative flow: configure the run task to raise; click; assert `_imported_tables` is preserved and the error surfaced. Run-disabled state: with `_imported_tables == {}` and no Open, assert `wired.window.run_btn.isEnabled() is False`. Fabricated values only; no polling primitives. Acceptance: file exists, <= 500 lines, all behavioral checks pass under `QT_QPA_PLATFORM=offscreen`.
- [x] [P9-T2] Create `tests/gui/integration/test_behavioral_dialogs.py` covering AC-7, AC-8, AC-9: (a) AC-7 Save: construct `wired` with `save_path_chooser=lambda: "results.db"`; populate `_imported_tables` with fabricated tables; assert `wired.window.save_btn.isEnabled() is True`; `qtbot.mouseClick(wired.window.save_btn, Qt.LeftButton)`; assert the injected fake `PipelineService.save_to_db` recorded one call with the working tables and `"results.db"`; (b) AC-7 cancel: with `save_path_chooser=lambda: None`, click Save; assert no `save_to_db` call was recorded; (c) AC-7 disabled-when-empty: with no imports and no open, assert `wired.window.save_btn.isEnabled() is False`; (d) AC-8 Open: construct `wired` with `open_path_chooser=lambda: "test.db"` and a fake `open_db` returning `{"LE": frame, "aop": frame, "sku_lu": frame}`; `qtbot.mouseClick(wired.window.open_btn, Qt.LeftButton)`; assert all four import buttons are disabled (`import_le_btn`, `import_aop_btn`, `import_skulu_btn`, `import_all_btn`); assert `wired.window.run_btn.isEnabled() is True`; emit `file_selected("new.xlsx")` on `le_widget`; assert only `import_le_btn` and `import_all_btn` re-enable; (e) AC-9 Export checklist-populated-before-display: construct `wired` with `export_dialog_runner=lambda dialog: ("Excel", "out.xlsx")` and a recording fake; populate `_imported_tables`; click `wired.window.export_btn`; capture that `export_presenter.set_available_tables` was called with `["LE", "aop", "sku_lu"]` BEFORE the dialog runner was invoked (Defect 1 fix; record call order via a sequence counter on the fake); (f) AC-9 CSV name-mangling end-to-end: with `export_dialog_runner=lambda dialog: ("CSV", "C:/out/results.csv")`, populate `_imported_tables` with three keys; click `export_btn`; assert the fake exporter's recorded `export` call resolved as `CsvExporter` and wrote `results_LE.csv`, `results_aop.csv`, `results_sku_lu.csv` at directory `C:/out/`; (g) AC-9 disabled-when-empty: with empty `working_tables`, assert `wired.window.export_btn.isEnabled() is False`. No polling primitives. Fabricated values only. Acceptance: file exists, <= 500 lines, all behavioral checks pass under `QT_QPA_PLATFORM=offscreen`; banned APIs grep returns no matches.
- [x] [P9-T3] Create `tests/gui/integration/test_behavioral_composition.py` covering AC-10 (button-driven composition smoke): `wired = build_application(qt_app=qtbot.qapp, runner=SynchronousRunner(), save_path_chooser=lambda: None, open_path_chooser=lambda: None, export_dialog_runner=lambda d: None)`; assert each of `import_le_btn`, `import_aop_btn`, `import_skulu_btn`, `import_all_btn`, `run_btn`, `save_btn`, `open_btn`, `export_btn` is reachable as a public attribute of `wired.window`; assert clicking each one (`qtbot.mouseClick(...)`) at least invokes the wired presenter slot through the recording fake surface (smoke check that every button has a non-stub handler). Assert `wired.runner` is the injected `SynchronousRunner`. No polling primitives. Acceptance: file exists, <= 500 lines, the composition smoke check passes under `QT_QPA_PLATFORM=offscreen`.
- [x] [P9-T4] Run the toolchain loop and persist evidence to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/phase9-qa.2026-05-28T22-10.md` (per-command schema). Acceptance: all four commands exit 0 in one pass; coverage >= 85% line / >= 75% branch.

---

### Phase 10 — Quality-Tier Classification Refresh and Verification

Budget: 0 production files, 0 test files (verification only; `quality-tiers.yml` was already updated in Phase 1 when `src/gui/runners.py` was created).

- [x] [P10-T1] Verify `quality-tiers.yml` classifies every `src/gui/**` module including the new `src/gui/runners.py: T2` entry registered in P1-T1. Run the repository tier-classification check (or `poetry run python -c` enumeration of `src/gui/**` against the YAML) and confirm no `src/gui` module is unclassified. Record the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/other/tier-classification.2026-05-28T22-10.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (list of `src/gui/**` modules and their tiers, with `src/gui/runners.py` shown at T2 and the v1 module set unchanged). Acceptance: artifact exists; `Output Summary:` confirms `src/gui/runners.py: T2` is present; every other `src/gui/**` module retains its v1 classification; no unclassified module remains; `EXIT_CODE: 0`.

---

### Phase 11 — Final QA Loop, Coverage Delta, and DoD Traceability

Budget: 0 production files, 0 test files (verification and coverage-delta only).

- [x] [P11-T1] Run `poetry run black .` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/final-black.2026-05-28T22-10.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. Acceptance: artifact exists with all four fields; `EXIT_CODE: 0`; `Output Summary:` reports no files reformatted (a clean pass after the loop).
- [x] [P11-T2] Run `poetry run ruff check .` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/final-ruff.2026-05-28T22-10.md` with the four schema fields. Acceptance: `EXIT_CODE: 0`; `Output Summary:` reports 0 errors; any suppression present matches a pre-authorized pattern in `.claude/rules/python-suppressions.md`.
- [x] [P11-T3] Run `poetry run pyright` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/final-pyright.2026-05-28T22-10.md` with the four schema fields. Acceptance: `EXIT_CODE: 0`; `Output Summary:` reports 0 errors and 0 warnings; no `Any` introduced and no strictness reduction.
- [x] [P11-T4] Run `QT_QPA_PLATFORM=offscreen poetry run pytest --cov --cov-branch --cov-report=term-missing` and write the result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/final-pytest-coverage.2026-05-28T22-10.md` with the four schema fields. Acceptance: `EXIT_CODE: 0`; `Output Summary:` records numeric post-change line-coverage % and branch-coverage % and the passed/failed test counts; line >= 85% and branch >= 75%; test count is at least the v1 baseline.
- [x] [P11-T5] Compute the coverage delta and write `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/qa-gates/coverage-delta.2026-05-28T22-10.md` reporting v2 baseline coverage, post-change coverage, and new/changed-code (`src/gui/runners.py` plus the v1 modules modified by v2) coverage. Baseline values are sourced exclusively from `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/baseline/pytest-coverage.2026-05-28T22-10.md` (produced by P0-T5). The coverage-delta artifact records baseline line %, baseline branch %, post-change line %, post-change branch %, and the test-count delta, all read from on-disk evidence; no hard-coded numbers appear in the plan. Acceptance: artifact exists with `Timestamp:`, baseline line/branch %, post-change line/branch %, and changed-code line/branch %; confirms line >= 85% / branch >= 75% repository-wide and no regression on changed lines. If any required coverage value is unavailable, the outcome is remediation-required, not PASS.
- [x] [P11-T6] Verify every AC-1 through AC-12 in `v2/issue.md` maps to at least one passing test and write the traceability result to `docs/features/active/2026-05-27-mix-pipeline-gui-19/v2/evidence/other/dod-traceability.2026-05-28T22-10.md` with `Timestamp:` and a table mapping each AC to its verifying unit/integration test file(s) AND its verifying behavioral integration test file(s) under `tests/gui/integration/`, with pass status for each. Acceptance: artifact exists; every AC-1 through AC-12 maps to at least one passing test at both the unit/integration level AND (where applicable per spec DoD) the button-driven behavioral level; the confidentiality invariant (no `SKU Description`/`Category` values, no real customer/SKU data) is confirmed across all new source, test, fixture, and doc files; no DoD item is unverified.

---

## Acceptance-Criteria Coverage Map (v2/issue.md)

| AC | Title | Phase(s) | Verifying unit/integration test(s) | Verifying behavioral test (tests/gui/integration/) |
|---|---|---|---|---|
| AC-1 | Render Tab renders an image of the selected worksheet | P3, P8 | `test_source_selection_presenter` (preview_sink), `test_source_input_widget` (`_on_tab_changed`), `test_preview_widget` (unchanged) | `test_behavioral_preview.py` |
| AC-2 | Import LE wires to pipeline and disables on success | P4, P8 | `test_pipeline_presenter` (LE path tracking + button push), `test_pipeline_service` (unchanged) | `test_behavioral_import_buttons.py` |
| AC-3 | Import AOP wires to pipeline and disables on success | P4, P8 | `test_pipeline_presenter` (AOP path tracking + button push), `test_pipeline_service` (unchanged) | `test_behavioral_import_buttons.py` |
| AC-4 | Import SKU_LU wires to pipeline and disables on success | P4, P8 | `test_pipeline_presenter` (SKU_LU path tracking + button push), `test_pipeline_service` (unchanged) | `test_behavioral_import_buttons.py` |
| AC-5 | Import All runs all three imports and disables all four | P4, P8 | `test_pipeline_presenter` (full-success and partial-success) | `test_behavioral_import_buttons.py` |
| AC-6 | Run executes transformation end-to-end | P1, P4, P7, P9 | `test_runners` (`SynchronousRunner`), `test_pipeline_presenter` (run dispatch), `test_pipeline_worker` (unchanged off-thread proof), `test_pipeline_service` (unchanged) | `test_behavioral_pipeline_run.py` |
| AC-7 | Save persists working tables to SQLite `.db` | P4, P7, P9 | `test_pipeline_presenter` (working_tables, save flow), `test_app_wiring` (chooser injection) | `test_behavioral_dialogs.py` |
| AC-8 | Open loads tables and reflects load state on import buttons | P4, P7, P9 | `test_pipeline_presenter` (sentinel + button pushes), `test_app_wiring` (open chooser injection) | `test_behavioral_dialogs.py` |
| AC-9 | Export opens file dialog with Excel/CSV format choices and exports through registry | P5, P6, P7, P9 | `test_export_dialog` (combo removed), `test_app_wiring_defaults` (filter parse), `test_csv_exporter` (name-mangling), `test_export_presenter` (unchanged) | `test_behavioral_dialogs.py` |
| AC-10 | Composition root wires all signals to behavioral handlers | P7, P9 | `test_app_wiring`, `test_app_composition` | `test_behavioral_composition.py` and the full `tests/gui/integration/` suite |
| AC-11 | Presentation logic remains testable without a live Qt event loop | P1, P3, P4, P5, P6 | every `test_*_presenter.py` runs without a `QApplication`; `test_runners.py` `SynchronousRunner` cases run without a `QApplication` | n/a (unit invariant) |
| AC-12 | Full toolchain passes and coverage thresholds hold | P11 | `final-black`, `final-ruff`, `final-pyright`, `final-pytest-coverage`, `coverage-delta` | n/a (gate) |

## Per-Phase File-Budget Check (hard cap: 3 production AND 3 test files)

| Phase | Production files | Test/non-code files | Within 3+3 cap |
|---|---|---|---|
| P0 | 0 | 0 (evidence only) | Yes |
| P1 | 2 (`src/gui/runners.py`, `src/gui/protocols.py`) | 1 (`tests/gui/test_runners.py`) | Yes |
| P2 | 2 (`src/gui/main_window.py`, `src/gui/app.py`) | 2 (`tests/gui/test_main_window.py`, `tests/gui/test_app_wiring.py`) | Yes |
| P3 | 2 (`src/gui/widgets/source_input_widget.py`, `src/gui/presenters/source_selection_presenter.py`) | 2 (`tests/gui/test_source_input_widget.py`, `tests/gui/test_source_selection_presenter.py`) | Yes |
| P4 | 1 (`src/gui/presenters/pipeline_presenter.py`) | 1 (`tests/gui/test_pipeline_presenter.py` + extension of `tests/gui/fakes/fake_views.py` recording-only; the fakes extension is metadata-only on an existing v1 test-support module and does not change the test-file count for this phase) | Yes |
| P5 | 2 (`src/gui/widgets/export_dialog.py`, `src/gui/app.py`) | 2 (`tests/gui/test_export_dialog.py`, `tests/gui/test_app_wiring_defaults.py`) | Yes |
| P6 | 1 (`src/gui/exporters/csv_exporter.py`) | 1 (`tests/gui/test_csv_exporter.py`) | Yes |
| P7 | 1 (`src/gui/app.py`) | 2 (`tests/gui/test_app_wiring.py`, `tests/gui/test_app_composition.py`) | Yes |
| P8 | 0 | 3 (`tests/gui/integration/__init__.py`, `test_behavioral_preview.py`, `test_behavioral_import_buttons.py`) | Yes |
| P9 | 0 | 3 (`test_behavioral_pipeline_run.py`, `test_behavioral_dialogs.py`, `test_behavioral_composition.py`) | Yes |
| P10 | 0 | 0 (verification only) | Yes |
| P11 | 0 | 0 (evidence only) | Yes |

Note on `quality-tiers.yml`: the only new entry in v2 is `src/gui/runners.py: T2`,
registered in P1-T1. Every other v2 change is to an existing v1 module already
classified, so `quality-tiers.yml` carries one new line in v2. The recording-only
extension of `tests/gui/fakes/fake_views.py` in P4 adds four recording methods to
an existing v1 test-support module without changing its file identity or count.
The `src/gui/app.py` file is modified in P2, P5, and P7; each edit is incremental
and the file remains <= 500 lines at every phase boundary.

## Test Plan

- **Unit (no Qt event loop):** `tests/gui/test_runners.py` (`SynchronousRunner` cases), `tests/gui/test_pipeline_presenter.py` (extended for v2 file-path tracking, working_tables, invalidation, button pushes; T2 `hypothesis` property test for `on_file_path_changed`), `tests/gui/test_source_selection_presenter.py` (extended for `preview_sink`), `tests/gui/test_csv_exporter.py` (extended for name-mangling), `tests/gui/test_export_presenter.py` (unchanged), `tests/gui/test_pipeline_service.py` (unchanged), other v1 `test_*_presenter.py` files unchanged.
- **Widget (pytest-qt, `QT_QPA_PLATFORM=offscreen`):** `tests/gui/test_main_window.py` (new public-attribute coverage), `tests/gui/test_source_input_widget.py` (extended for `_on_tab_changed`), `tests/gui/test_export_dialog.py` (extended; format combo removed), `tests/gui/test_preview_widget.py` (unchanged), `tests/gui/test_app_wiring.py` and `tests/gui/test_app_wiring_defaults.py` (extended for runner injection, chooser injection, filter-string parse), `tests/gui/test_app_composition.py` (extended for `SynchronousRunner` smoke).
- **Worker (Qt; `qtbot.waitSignal` allowed):** `tests/gui/test_pipeline_worker.py` (unchanged from v1; the only test where `qtbot.waitSignal` is retained, per spec section 4).
- **Behavioral integration (Qt; `QT_QPA_PLATFORM=offscreen`; NO `qtbot.waitUntil`, NO `QTest.qWait`, NO `time.sleep`):** the new `tests/gui/integration/` directory with five files covering AC-1 (preview), AC-2/3/4/5 (import buttons), AC-6 (`SynchronousRunner` pipeline run), AC-7/8/9 (Save/Open/Export dialogs), AC-10 (composition).
- **Coverage evidence:** v2 baseline `v2/evidence/baseline/pytest-coverage.2026-05-28T22-10.md`; post-change `v2/evidence/qa-gates/final-pytest-coverage.2026-05-28T22-10.md`; comparison `v2/evidence/qa-gates/coverage-delta.2026-05-28T22-10.md`.

## Open Questions / Notes

None — spec is fully resolved by Decisions 1-3 (CSV name-mangling, ExportDialog
format-combo removal + Save dialog filter, `RunnerProtocol` injection seam) and
research Q1-Q8. The only v2-specific risks are (a) Pyright-strict typing of the
new `RunnerProtocol`/`SynchronousRunner`/`ThreadedRunner` interactions with
`PipelineWorker` (mitigated in P1-T1 by reusing the v1 typed-Protocol-view
pattern already proven for `Signal` typing) and (b) the `offscreen` plugin
availability for the expanded behavioral suite (verified in P0-T6 and exercised
by every test in `tests/gui/integration/`).
