# Atomic Plan — App Rename and Real Icon (Issue #33)

- Feature folder: `docs/features/active/2026-05-29-app-rename-and-real-icon-33/`
- Work Mode: minor-audit (small path)
- AC source: `docs/features/active/2026-05-29-app-rename-and-real-icon-33/issue.md` (AC1-AC11)
- Plan generated: 2026-05-29
- Evidence root: `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/`

This plan delivers AC1-AC11 from the issue.md. The plan is phased; every task is
atomic with a single binary outcome and an explicit verifiable artifact. Evidence
paths conform to the canonical `<FEATURE>/evidence/<kind>/` scheme.

---

### Phase 0 — Compliance and baseline capture

- [x] [P0-T1] Read `.claude/rules/general-code-change.md` and confirm precedence over agent defaults. Record reading in evidence file `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:`, and the explicit list of files read.
- [x] [P0-T2] Read `.claude/rules/general-unit-test.md` and append confirmation line to the same `phase0-instructions-read.md` artifact (no real temp files in tests; coverage >= 85% line / >= 75% branch; Arrange-Act-Assert structure).
- [x] [P0-T3] Read `.claude/rules/quality-tiers.md` and append confirmation line. Note repository tier classification source `quality-tiers.yml`. Modules in scope are T4 (build scripts) and T3 (GUI wiring); coverage thresholds uniform.
- [x] [P0-T4] Read `.claude/rules/python.md` and append confirmation line. Confirm the seven-stage Python toolchain: Black -> Ruff -> Pyright -> Pytest with restart-on-change semantics.
- [x] [P0-T5] Read `.claude/rules/python-suppressions.md` and append confirmation line. Confirm only pre-authorized `# noqa` patterns may be used; new suppressions outside that list require explicit user approval.
- [x] [P0-T6] Read `.claude/rules/self-explanatory-code-commenting.md` and append confirmation line. Confirm mandatory docstrings (purpose/args/returns/raises/side-effects), intent comments on loops/comprehensions, decision-logic comments on non-trivial branching.
- [x] [P0-T7] Read `.claude/rules/tonality.md` and append confirmation line. Confirm professional tone for all human-readable strings, doc updates, and READMEs (no hyperbole, no jokes, no metaphor unless utilitarian).
- [x] [P0-T8] Capture baseline Black state with `poetry run black --check .` Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/black.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail and count of would-reformat files).
- [x] [P0-T9] Capture baseline Ruff state with `poetry run ruff check .` Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/ruff.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (rule-violation count and list of distinct rule codes).
- [x] [P0-T10] Capture baseline Pyright state with `poetry run pyright`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/pyright.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error count, warning count, information count).
- [x] [P0-T11] Capture baseline Pytest state with coverage: `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/pytest.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass/fail counts AND numeric headline values: line coverage percent, branch coverage percent).
- [x] [P0-T12] Verify `artifacts/realgood_calculator_icon.svg` exists and parses as XML via `python -c "from xml.etree import ElementTree as ET; ET.parse('artifacts/realgood_calculator_icon.svg'); print('ok')"`. Capture the SHA256 of the file via `python -c "import hashlib,pathlib;print(hashlib.sha256(pathlib.Path('artifacts/realgood_calculator_icon.svg').read_bytes()).hexdigest())"`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/svg-source-fingerprint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (file size, SHA256, root tag).
- [x] [P0-T13] Capture baseline placeholder `packaging/velopack/icon.ico` SHA256 and first-four-byte magic via `python -c "import hashlib,pathlib;b=pathlib.Path('packaging/velopack/icon.ico').read_bytes();print(hashlib.sha256(b).hexdigest(),b[:4].hex())"`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/baseline/icon-placeholder-fingerprint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (SHA256 and magic bytes hex).

---

### Phase 1 — `src/gui/_icon.py` helper with test-first construction

- [x] [P1-T1] Add a new failing test `test_resolve_icon_path_prefers_compiled_mode` in `tests/gui/test_icon.py` that imports `resolve_icon_path` from `src.gui._icon`, injects a `path_exists` callable returning `True` for the compiled-mode probe path (`Path(sys.executable).parent / "icon.ico"`), and asserts the returned path equals that probe. Test must fail at first run because module does not yet exist.
- [x] [P1-T2] Add failing test `test_resolve_icon_path_falls_back_to_dev_mode` in the same file. Inject a `path_exists` callable returning `False` for the compiled-mode probe and `True` for the dev-mode probe (`Path(__file__).parents[2] / "packaging" / "velopack" / "icon.ico"` where `__file__` is the helper module). Assert returned path equals the dev-mode probe.
- [x] [P1-T3] Add failing test `test_resolve_icon_path_raises_when_no_probe_resolves` in the same file. Inject a `path_exists` callable that returns `False` for every input. Assert `FileNotFoundError` is raised.
- [x] [P1-T4] Create `src/gui/_icon.py` with a module docstring stating purpose, responsibilities, usage, and side-effects. Implement `resolve_icon_path(path_exists: Callable[[Path], bool] | None = None) -> Path` with full type hints, a Google-style docstring (Args/Returns/Raises), an intent comment above the probe iteration, and decision-logic comments distinguishing compiled-mode from dev-mode probes. Default `path_exists` to `Path.is_file`. File must not exceed 500 lines.
- [x] [P1-T5] Rerun `poetry run pytest tests/gui/test_icon.py -q` and capture evidence. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase1-icon-tests.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (three new tests pass).
- [x] [P1-T6] Run `poetry run black src/gui/_icon.py tests/gui/test_icon.py` and `poetry run ruff check src/gui/_icon.py tests/gui/test_icon.py`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase1-format-lint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` for each command.

---

### Phase 2 — Nuitka argv updates in `src/build_exe.py` (AC1)

- [x] [P2-T1] Add new failing test `test_resolve_nuitka_command_contains_icon_flags` in `tests/test_build_exe.py` asserting that `resolve_nuitka_command()` argv contains, in ordered membership: `--output-filename=MixCalculator.exe`, `--windows-icon-from-ico=<REPO_ROOT>/packaging/velopack/icon.ico`, and `--include-data-file=<REPO_ROOT>/packaging/velopack/icon.ico=icon.ico`. Use `str(REPO_ROOT / "packaging" / "velopack" / "icon.ico")` for the expected path string so Windows separators match.
- [x] [P2-T2] Update `tests/test_build_exe.py::test_main_dry_run_prints_argv_and_does_not_invoke_seam` to additionally assert `"--output-filename=MixCalculator.exe"`, `"--windows-icon-from-ico="`, and `"--include-data-file="` appear in `captured.out`.
- [x] [P2-T3] In `src/build_exe.py`, add a module-level constant `EXE_NAME: Final[str] = "MixCalculator.exe"` with a one-line comment explaining cross-file consistency (referenced by `src/build_velopack.py`).
- [x] [P2-T4] In `src/build_exe.py::resolve_nuitka_command()`, insert the three new flags into the returned argv list between `f"--output-dir={REPO_ROOT / 'dist' / 'nuitka'}"` and the trailing positional `str(REPO_ROOT / "src" / "gui" / "app.py")`. Order: `f"--output-filename={EXE_NAME}"`, then `f"--windows-icon-from-ico={REPO_ROOT / 'packaging' / 'velopack' / 'icon.ico'}"`, then `f"--include-data-file={REPO_ROOT / 'packaging' / 'velopack' / 'icon.ico'}=icon.ico"`. Update the function docstring to enumerate the new flags and explain the data-file destination semantics.
- [x] [P2-T5] Run `poetry run pytest tests/test_build_exe.py -q`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase2-build-exe-tests.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (all build_exe tests pass including new icon-flag test).

---

### Phase 3 — Velopack argv updates in `src/build_velopack.py` (AC2, AC3)

- [x] [P3-T1] Update `tests/test_build_velopack.py::test_resolve_pack_command_contains_required_argv` expected_pairs: change `("--packId", "mix-calculator")` to `("--packId", "MixCalculator")` and `("--mainExe", "app.exe")` to `("--mainExe", "MixCalculator.exe")`.
- [x] [P3-T2] Update `tests/test_build_velopack.py::test_main_dry_run_prints_argv_and_does_not_invoke_seam` token list: change `"mix-calculator"` to `"MixCalculator"`.
- [x] [P3-T3] Update `tests/test_build_velopack.py::test_resolve_upload_command_argv_shape` expected_pairs: change `("--releaseName", "mix-calculator 0.1.0")` to `("--releaseName", "Mix Calculator 0.1.0")`.
- [x] [P3-T4] In `src/build_velopack.py::resolve_pack_command()`, change the `--packId` value from `"mix-calculator"` to `"MixCalculator"` and the `--mainExe` value from `"app.exe"` to `"MixCalculator.exe"`. Update the surrounding intent comment to record the new branding.
- [x] [P3-T5] In `src/build_velopack.py::_app_exe_exists()` predicate seam, change the probed file name from `"app.exe"` to `"MixCalculator.exe"` so the pre-flight gate detects the renamed Nuitka output. Update the docstring accordingly.
- [x] [P3-T6] In `src/build_velopack.py::main()` error message for missing app exe, change the literal `"dist/nuitka/app.dist/app.exe is missing"` to `"dist/nuitka/app.dist/MixCalculator.exe is missing"`. Keep the remediation hint `Run \`poetry run build-exe\` first` intact.
- [x] [P3-T7] In `src/build_velopack.py::resolve_upload_command()`, change the `--releaseName` value from `f"mix-calculator {version}"` to `f"Mix Calculator {version}"`. Update the surrounding intent comment.
- [x] [P3-T8] Update module docstring header references from `mix-calculator-Setup.exe` and `mix-calculator-<version>-full.nupkg` to the case-aligned `MixCalculator-Setup.exe` and `MixCalculator-<version>-full.nupkg` where Velopack output filenames track packId.
- [x] [P3-T9] Run `poetry run pytest tests/test_build_velopack.py -q`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase3-build-velopack-tests.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (all build_velopack tests pass with new packId / mainExe / releaseName values).

---

### Phase 4 — Pillow dev dependency

- [x] [P4-T1] Edit `pyproject.toml` `[tool.poetry.group.dev.dependencies]` to add the line `pillow = "^12.0"` immediately after `pytest-qt = ">=4.4"`. Confirm the existing block layout is preserved.
- [x] [P4-T2] Regenerate the lockfile with `env -u VIRTUAL_ENV poetry lock`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase4-poetry-lock.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (lock succeeded; resolved Pillow version).
- [x] [P4-T3] Install with `env -u VIRTUAL_ENV poetry install`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase4-poetry-install.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (install succeeded).
- [x] [P4-T4] Verify `PIL.Image` importable via `poetry run python -c "from PIL import Image; print(Image.__version__)"`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase4-pil-import.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (Pillow version string printed).
- [x] [P4-T5] Probe whether Pillow ships a `py.typed` marker via `poetry run python -c "import importlib.resources as r,PIL,pathlib;print(pathlib.Path(PIL.__file__).parent / 'py.typed', (pathlib.Path(PIL.__file__).parent / 'py.typed').is_file())"`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase4-pil-typed-probe.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (boolean result). The result determines whether `# type: ignore[import-untyped]` is authorized for the PIL import in Phase 5.

---

### Phase 5 — `packaging/velopack/convert_icon.py` converter + tests (AC6)

- [x] [P5-T1] Add failing test `test_convert_icon_main_writes_multisize_ico` in `tests/test_convert_icon.py`. The test constructs an in-memory synthetic 256x256 SVG fixture as a `str` (single `<rect width="256" height="256" fill="#ff0000"/>` inside an `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256">` root), passes it to `convert_icon.convert_svg_bytes_to_ico_bytes(svg_bytes)`, and asserts the returned `bytes` starts with `b"\x00\x00\x01\x00"`, that `PIL.Image.open(io.BytesIO(result)).info` exposes the expected size, and that `Image.open(io.BytesIO(result))` can iterate frames at sizes `{(16,16),(32,32),(48,48),(256,256)}`. The test passes the SVG as bytes and reads ICO bytes; NO filesystem temp files are created.
- [x] [P5-T2] Add failing test `test_convert_icon_main_exits_zero_with_path_args` in the same file. Use `monkeypatch` to patch `convert_icon.read_svg_bytes` to return the synthetic SVG bytes and patch `convert_icon.write_ico_bytes` to a recorder. Call `convert_icon.main(["--input","x.svg","--output","y.ico"])` and assert returned int is `0` and the recorder received bytes starting with `b"\x00\x00\x01\x00"`.
- [x] [P5-T3] Add failing test `test_convert_icon_rejects_missing_qsvgrenderer_load` in the same file. Provide invalid SVG bytes (`b"not svg"`) and assert `convert_icon.convert_svg_bytes_to_ico_bytes` raises `ValueError` with a message mentioning "SVG" or "parse".
- [x] [P5-T4] Create `packaging/velopack/convert_icon.py` with a module docstring stating purpose (one-shot SVG to multi-size ICO converter), responsibilities (rasterize via QSvgRenderer at 16/32/48/256, assemble via Pillow), usage (`poetry run python packaging/velopack/convert_icon.py --input <svg> --output <ico>`), side effects (writes ICO file when not given seam overrides), and a NOTE that the script depends on PySide6 runtime dep and Pillow dev dep. File must not exceed 500 lines.
- [x] [P5-T5] In `packaging/velopack/convert_icon.py`, implement helper `_render_svg_to_qimage(svg_bytes: bytes, size: int) -> QImage`. Use `QSvgRenderer.load(QByteArray(svg_bytes))`; if the renderer reports `isValid() is False`, raise `ValueError` with message including "SVG could not be parsed at size <size>". Create `QImage(size, size, QImage.Format_ARGB32)`, fill transparent via `image.fill(Qt.GlobalColor.transparent)`, paint via `QPainter(image)` and call `painter.end()` explicitly before returning. Include intent comments above the QPainter block and a docstring with Args/Returns/Raises.
- [x] [P5-T6] In `packaging/velopack/convert_icon.py`, implement helper `_qimage_to_pil(image: QImage) -> Image.Image`. Convert format via `image.convertToFormat(QImage.Format_RGBA8888)`, read `image.bits()` with `image.sizeInBytes()` (or fall back to `bytesPerLine`), and assemble a PIL image with `Image.frombytes("RGBA", (width, height), bytes_buffer)`. Account for stride: if `image.bytesPerLine() != width * 4`, slice each scanline before passing to PIL. Include intent comment explaining the stride correction and a docstring with Args/Returns.
- [x] [P5-T7] In `packaging/velopack/convert_icon.py`, implement `convert_svg_bytes_to_ico_bytes(svg_bytes: bytes) -> bytes`. Ensure a `QCoreApplication` instance exists via `if QCoreApplication.instance() is None: QApplication([])` so subsequent calls in the same process do not double-construct. For each size in `(16, 32, 48, 256)` render via `_render_svg_to_qimage` and convert via `_qimage_to_pil`. Save the largest as the base, pass the remaining sizes via `images[0].save(buffer, format='ICO', sizes=[(16,16),(32,32),(48,48),(256,256)])` where `buffer` is `io.BytesIO()`. Return `buffer.getvalue()`. Include docstring with Args/Returns/Raises.
- [x] [P5-T8] In `packaging/velopack/convert_icon.py`, implement seams `read_svg_bytes(path: Path) -> bytes` and `write_ico_bytes(path: Path, data: bytes) -> None` and a `main(argv: Sequence[str] | None = None) -> int` that uses `argparse` with `--input` and `--output` value flags, calls the seams (so tests can patch them), and returns 0 on success.
- [x] [P5-T9] If Phase 4 P4-T5 indicated Pillow lacks `py.typed`, attach a single line-level `# type: ignore[import-untyped]` on the `from PIL import Image` import line with the rationale comment `# Pillow lacks py.typed marker`. If P4-T5 indicated `py.typed` is present, do not add the suppression.
- [x] [P5-T10] Run `poetry run pytest tests/test_convert_icon.py -q` and capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase5-convert-icon-tests.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (three new tests pass).
- [x] [P5-T11] Verify the new files are within size cap via `python -c "import pathlib;p=pathlib.Path('packaging/velopack/convert_icon.py');print(p,sum(1 for _ in p.open()))"` and the same for `tests/test_convert_icon.py`. Both must report under 500 lines. Append a note to `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase5-convert-icon-tests.md`.

---

### Phase 6 — Asset commits (AC4, AC5)

- [x] [P6-T1] Copy `artifacts/realgood_calculator_icon.svg` to `packaging/velopack/icon-source.svg` using `python -c "import shutil;shutil.copy('artifacts/realgood_calculator_icon.svg','packaging/velopack/icon-source.svg')"`. Verify file size matches baseline (47,010 bytes) and that the file starts with `<svg` or `<?xml`.
- [x] [P6-T2] Record the post-copy SHA256 of `packaging/velopack/icon-source.svg` and compare to the Phase 0 baseline. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase6-icon-source-fingerprint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (SHA256 equals baseline -> copy is byte-exact). AC4 verified by this artifact.
- [x] [P6-T3] Generate the multi-size ICO by running `poetry run python packaging/velopack/convert_icon.py --input packaging/velopack/icon-source.svg --output packaging/velopack/icon.ico`. The command must replace the placeholder ICO in place. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase6-icon-generate.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (exit 0, output file size in bytes).
- [x] [P6-T4] Verify the generated ICO. Run `poetry run python -c "import hashlib,pathlib;from PIL import Image,IcoImagePlugin;p=pathlib.Path('packaging/velopack/icon.ico');b=p.read_bytes();im=Image.open(p);sizes=sorted({(e.width,e.height) for e in im.ico.entry});print(hashlib.sha256(b).hexdigest(),b[:4].hex(),sizes)"`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase6-icon-fingerprint.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (SHA256, magic bytes `0x00 0x00 0x01 0x00` -> `00000100`, and the sorted set of frame sizes `[(16,16),(32,32),(48,48),(256,256)]`). AC5 verified by this artifact.

---

### Phase 7 — GUI window icon wiring in `src/gui/app.py` (AC8)

- [x] [P7-T1] In `tests/gui/test_app_composition.py`, add a failing test `test_main_sets_window_icon_on_qapplication` that patches `src.gui.app.QIcon` to a recorder, patches `src.gui.app.resolve_icon_path` to return `pathlib.Path("/fake/icon.ico")`, drives `app_module.main([])` under the existing `_existing_qapp`/`_instant_exec` monkeypatch pattern, and asserts (a) `QIcon` was called once with the string `"/fake/icon.ico"` (using `pathlib.PurePosixPath` or platform-neutral comparison) AND (b) `setWindowIcon` was called once on the returned QApplication with the QIcon recorder's return value.
- [x] [P7-T2] Add failing test `test_build_application_calls_set_window_icon_when_qt_app_constructed` in `tests/gui/test_app_composition.py`. Patch `src.gui.app.resolve_icon_path` to return a known path and patch `src.gui.app.QIcon` to a recorder. Call `build_application()` with `qt_app=None` so the function constructs a fresh QApplication via the pytest-qt managed instance. Assert that `qt_app.windowIcon().isNull()` is False after the call, OR assert via the QIcon recorder that the constructor was called with the resolved path string.
- [x] [P7-T3] In `src/gui/app.py`, add `from PySide6.QtGui import QIcon` to the imports block and `from src.gui._icon import resolve_icon_path` after the existing wiring imports.
- [x] [P7-T4] In `src/gui/app.py::build_application()`, after the line `application = qt_app` but before the `reader` resolution, insert a guarded `if qt_app is None: application = QApplication([])` so the function always has a QApplication to set the icon on (the existing default `application = qt_app` path was for tests passing an externally-managed QApplication; this guard makes the post-construction icon-set work in both paths). Update the function docstring to record the change.
- [x] [P7-T5] In `src/gui/app.py::build_application()`, after the QApplication is resolved/constructed and before any widget construction, insert: `icon_path = resolve_icon_path()` followed by `application.setWindowIcon(QIcon(str(icon_path)))`. Include an intent comment above the two lines explaining that the icon must be set on the QApplication so it propagates to the title bar, taskbar, and Alt-Tab preview. Wrap in `try/except FileNotFoundError: pass` ONLY if the planner determines a missing icon at dev-time would break unrelated tests; otherwise let the FileNotFoundError propagate so a missing ICO is caught at startup.
- [x] [P7-T6] In `src/gui/app.py::main()`, after the `qt_app = QApplication(args)` line, insert `qt_app.setWindowIcon(QIcon(str(resolve_icon_path())))` so `main()` independently calls `setWindowIcon` (covered by P7-T1). Add an intent comment.
- [x] [P7-T7] Run `poetry run pytest tests/gui/test_app_composition.py tests/gui/test_icon.py -q` and capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase7-gui-tests.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (all GUI composition tests pass, including the two new icon-set tests).

---

### Phase 8 — `packaging/velopack/README.md` documentation updates (AC9)

- [x] [P8-T1] Update the "Pack identity" table in `packaging/velopack/README.md`: change `--packId` value `mix-calculator` to `MixCalculator`, change `--mainExe` value `app.exe` to `MixCalculator.exe`, and update the inline note on `--packId` to reflect the case-corrected branding (still stable across releases).
- [x] [P8-T2] In the "Outputs" section, change `mix-calculator-Setup.exe` to `MixCalculator-Setup.exe`, `mix-calculator-<version>-full.nupkg` to `MixCalculator-<version>-full.nupkg`, `mix-calculator-<version>-delta.nupkg` to `MixCalculator-<version>-delta.nupkg`, `mix-calculator-Portable.zip` to `MixCalculator-Portable.zip`, and the release-name line `release name \`mix-calculator <version>\`` to `release name \`Mix Calculator <version>\``.
- [x] [P8-T3] Replace the "Icon" subsection. The new prose must record that the ICO is generated from `icon-source.svg` (committed alongside the ICO) via `packaging/velopack/convert_icon.py` and that the placeholder description is no longer accurate. Keep the magic-byte verification snippet but update the path commentary.
- [x] [P8-T4] Add a new top-level subsection titled `## Icon source and regeneration` documenting (a) the source file `packaging/velopack/icon-source.svg`, (b) the converter `packaging/velopack/convert_icon.py`, (c) the exact regeneration command `poetry run python packaging/velopack/convert_icon.py --input packaging/velopack/icon-source.svg --output packaging/velopack/icon.ico`, (d) that Pillow is required as a dev-only build-time dependency declared in `pyproject.toml`, and (e) that the produced ICO contains 16/32/48/256 frames with magic bytes `0x00 0x00 0x01 0x00`.
- [x] [P8-T5] Verify the updated README contains every required string token by `python -c "import pathlib;t=pathlib.Path('packaging/velopack/README.md').read_text();print(all(s in t for s in ['MixCalculator','MixCalculator.exe','Mix Calculator','icon-source.svg','convert_icon.py','Pillow']))"`. Capture in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase8-readme-tokens.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (expected output: `True`).

---

### Phase 9 — Final QC and AC verification (AC10, AC11)

- [x] [P9-T1] Run `poetry run black --check .` Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-black.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (pass; zero would-reformat files). If non-zero exit, run `poetry run black .` and restart the loop from this step.
- [x] [P9-T2] Run `poetry run ruff check .` Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-ruff.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (zero rule violations; zero new suppressions beyond pre-authorized patterns). On change or failure, restart from P9-T1.
- [x] [P9-T3] Run `poetry run pyright`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-pyright.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (zero errors and warnings on changed/new files). On change or failure, restart from P9-T1.
- [x] [P9-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Capture evidence in `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-pytest.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (numeric pass/fail counts AND total line coverage percent AND total branch coverage percent). Both percentages must be at least the Phase 0 baseline (and meet >= 85% line / >= 75% branch).
- [x] [P9-T5] Compute coverage delta for changed modules (`src/build_exe.py`, `src/build_velopack.py`, `src/gui/app.py`, `src/gui/_icon.py`, `packaging/velopack/convert_icon.py`) by running `poetry run pytest --cov=src.build_exe --cov=src.build_velopack --cov=src.gui.app --cov=src.gui._icon --cov-branch --cov-report=term-missing tests/`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-coverage-changed-modules.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (per-module line and branch percent for each changed module; all must remain >= 85% / >= 75%).
- [x] [P9-T6] Verify file-size cap for every new and modified file. Run `poetry run python -c "import pathlib;files=['src/build_exe.py','src/build_velopack.py','src/gui/app.py','src/gui/_icon.py','packaging/velopack/convert_icon.py','tests/test_build_exe.py','tests/test_build_velopack.py','tests/gui/test_app_composition.py','tests/gui/test_icon.py','tests/test_convert_icon.py'];[print(f,sum(1 for _ in pathlib.Path(f).open())) for f in files]"`. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-file-size.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (each file < 500 lines). AC11 verified by this artifact.
- [x] [P9-T7] Build the AC1-AC11 verification matrix. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-ac-verification.md` containing a table that maps each AC to the artifact path(s) that prove it: AC1->phase2-build-exe-tests.md; AC2->phase3-build-velopack-tests.md (packId/mainExe pairs); AC3->phase3-build-velopack-tests.md (releaseName); AC4->phase6-icon-source-fingerprint.md; AC5->phase6-icon-fingerprint.md; AC6->phase5-convert-icon-tests.md; AC7->phase4-poetry-lock.md + phase4-poetry-install.md + phase4-pil-import.md; AC8->phase7-gui-tests.md; AC9->phase8-readme-tokens.md; AC10->phase9-black.md + phase9-ruff.md + phase9-pyright.md + phase9-pytest.md + phase9-coverage-changed-modules.md; AC11->phase9-file-size.md. Include `Timestamp:`, the verification table, and an explicit `Status:` line per AC (PASS or FAIL with reason).
- [x] [P9-T8] Restart-on-change check. If any of P9-T1..P9-T4 reported non-zero exit OR modified files, the loop must be restarted from P9-T1 until a single clean pass is recorded. Write `docs/features/active/2026-05-29-app-rename-and-real-icon-33/evidence/qa-gates/phase9-loop-completion.md` with `Timestamp:`, `Command:` (the sequence used), `EXIT_CODE:` (0 only when single-pass green), `Output Summary:` (record of iterations and final clean pass).
