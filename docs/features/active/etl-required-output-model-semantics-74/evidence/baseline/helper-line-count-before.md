# `_schema_loader_helpers.py` Line Count — Before R1 (cycle 2)

Timestamp: 2026-06-17T13-10

Command: `poetry run python -c "print(sum(1 for _ in open('src/_schema_loader_helpers.py', encoding='utf-8')))"`

EXIT_CODE: 0

Output Summary: 464 (the file has no trailing newline on its last content line, so the
newline-count is 464; the file's last content line is line 465 in the editor). This is the
pre-change line count; the 500-line cap must hold after the R1 ordering change.
