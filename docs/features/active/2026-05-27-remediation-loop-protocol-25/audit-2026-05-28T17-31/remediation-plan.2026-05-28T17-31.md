# Remediation Plan — Issue #25 (Bug: remediation-loop-protocol) — Cycle 1

- **Issue:** #25
- **Issue URL:** https://github.com/drmoisan/mix-calculator/issues/25
- **Work Mode:** full-bug (source precedence: `docs/features/active/remediation-loop-protocol-25/issue.md` line 5)
- **Feature folder:** `docs/features/active/remediation-loop-protocol-25/`
- **Cycle entry timestamp (`<entry-ts>`):** `2026-05-28T17-31`
- **Cycle:** 1 (cycle-0 plan at `plan.2026-05-28T12-57.md` is complete and not modified by this plan)
- **Remediation inputs source:** `docs/features/active/remediation-loop-protocol-25/remediation-inputs.2026-05-28T17-31.md`
- **Languages in scope:** PowerShell (Pester test code only — no production hook edits planned), Markdown (one qa-gate doc append).
- **Languages NOT in scope:** Python, TypeScript, C#, JSON, agent definitions, policy documents.

**Fail-closed evidence rule:** Each evidence-producing task names the artifact path that closes it. Missing baseline, final-QA, or AC-verification artifacts force a BLOCKED or INCOMPLETE verdict; PASS is not permitted in their absence.

**Evidence accounting rule:** Every command-step artifact under `docs/features/active/remediation-loop-protocol-25/evidence/<kind>/` carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. For Pester command-step artifacts, `Output Summary:` MUST include numeric file-level line and branch coverage values for `.claude/hooks/validate-orchestrator-output.ps1`.

## Evidence-Path Substitution Notice

This planner inspected the delegation prompt. The prompt names canonical evidence paths under `docs/features/active/remediation-loop-protocol-25/evidence/<kind>/`. The cycle-1 sub-paths chosen are `evidence/baseline-c1/` and `evidence/qa-gates-c1/` to keep cycle-1 artifacts distinct from cycle-0 evidence (which is frozen). All paths resolve to the canonical `<FEATURE>/evidence/<kind>/` scheme. No non-canonical path was supplied; no substitution was required.

## Cycle-0 Baseline Freeze Notice

The delegation prompt explicitly prohibits regeneration of `evidence/baseline/*` artifacts. Cycle-0 baselines (`docs/features/active/remediation-loop-protocol-25/evidence/baseline/*.2026-05-28T12-57.md`) are frozen and are NOT re-captured by this plan. Cycle-1 baseline captures live under `evidence/baseline-c1/` with timestamp `2026-05-28T17-31` and describe the cycle-1 entry state only.

## Requirements Source

Primary requirements source is `docs/features/active/remediation-loop-protocol-25/remediation-inputs.2026-05-28T17-31.md`. The original ten ACs from `issue.md` remain authoritative; this cycle addresses the audit findings that map to AC#9 (Pester fixtures continue to satisfy the validator contract under the uniform 85% line / 75% branch threshold) and AC#6 (qa-gate evidence accurately documents the enforcement channel).

## Acceptance Criteria → Task Mapping

The two findings in `remediation-inputs.2026-05-28T17-31.md` map to plan tasks as follows. Both ACs are closed by Phase 2 final-QA verification tasks.

- AC#9 — Pester fixtures continue to satisfy the validator contract; the uniform 85% line / 75% branch coverage threshold (`.claude/rules/quality-tiers.md` Authoritative Decision #2; `.claude/rules/general-unit-test.md`) applies file-wide to `.claude/hooks/validate-orchestrator-output.ps1`. The new tests exercise `Invoke-OrchestratorOutputValidation` directly through the 13 payload/checkpoint scenarios enumerated in `remediation-inputs.2026-05-28T17-31.md`. **Tasks:** P1-T1 through P1-T15, P2-T3, P2-T4, P2-T5, P2-T6.
- AC#6 — qa-gate evidence accurately documents that AC#6 enforcement is realized via `.claude/schemas/orchestrator-state.schema.json` + `.claude/hooks/validate-orchestrator-output.ps1::Test-RemediationLoopShape`, while the MCP tool `mcp__drm-copilot__validate_orchestration_artifacts` retains its upstream-owned built-in schema unchanged. **Tasks:** P1-T16, P2-T7.

## Do Not Do (recorded from remediation-inputs)

- Do not lower the 85% coverage threshold in `.claude/rules/quality-tiers.md` or `.claude/rules/general-unit-test.md`.
- Do not modify the existing six Pester tests in `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1`.
- Do not edit any agent definition file under `.claude/agents/`.
- Do not edit policy documents under `.claude/rules/` or `.github/instructions/`.
- Do not edit any file under `docs/features/active/2026-05-27-mix-pipeline-gui-19/`.
- Do not introduce temporary files in tests. Inline JSON literals via heredoc strings are acceptable.
- Do not regenerate `evidence/baseline/*` artifacts (frozen at 2026-05-28T12-57).
- Do not edit `.claude/hooks/validate-orchestrator-output.ps1` (production hook is unchanged this cycle; coverage gap is closed by exercising the already-modified entrypoint through additional tests).
- Do not edit `.claude/schemas/orchestrator-state.schema.json` (cycle-0 artifact is unchanged this cycle).
- Do not alter the verification timestamp in `evidence/qa-gates/ac-6-schema-shape.2026-05-28T12-57.md`; append only.

---

### Phase 0 — Cycle-1 Baselines and Policy Capture

- [x] [P0-T1] Read `.claude/rules/general-code-change.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T2] Read `.claude/rules/powershell.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T3] Read `.claude/rules/general-unit-test.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T4] Read `.claude/rules/quality-tiers.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T5] Read `.claude/rules/tonality.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T6] Read `.claude/skills/atomic-plan-contract/SKILL.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T7] Read `.claude/skills/policy-compliance-order/SKILL.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T8] Read `.claude/skills/evidence-and-timestamp-conventions/SKILL.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T9] Read `docs/features/active/remediation-loop-protocol-25/remediation-inputs.2026-05-28T17-31.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T10] Read `docs/features/active/remediation-loop-protocol-25/policy-audit.2026-05-28T17-31.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T11] Read `.claude/hooks/validate-orchestrator-output.ps1` (unmodified cycle-0 production file) and record the line count and the absolute path in the `Files Read:` block of `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T12] Read `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1` (existing six-test file from cycle 0) and record the line count and the absolute path in the `Files Read:` block of `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T13] Write the Phase 0 evidence header (`Timestamp: 2026-05-28T17-31`, `Policy Order:` list, `Files Read:` list) to `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/phase0-instructions-read.2026-05-28T17-31.md`.
- [x] [P0-T14] Capture a cycle-1 baseline PoshQC format result by invoking `mcp__drm-copilot__run_poshqc_format` and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/poshqc-format.2026-05-28T17-31.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T15] Capture a cycle-1 baseline PoshQC analyze result by invoking `mcp__drm-copilot__run_poshqc_analyze` with `scan_folders=[".claude/hooks","tests/claude/hooks"]` and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/poshqc-analyze.2026-05-28T17-31.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T16] Capture a cycle-1 baseline PoshQC Pester test result by invoking `mcp__drm-copilot__run_poshqc_test` with `scan_folders=["tests/claude/hooks"]` and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/poshqc-test.2026-05-28T17-31.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the summary records the headline test pass/fail counts to confirm the existing six tests still pass at cycle-1 entry).
- [x] [P0-T17] Capture the cycle-1 entry file-level Pester coverage on `.claude/hooks/validate-orchestrator-output.ps1` using the verbatim invocation from `policy-audit.2026-05-28T17-31.md` Appendix B (`Invoke-Pester` with `CodeCoverage.Path = '.claude/hooks/validate-orchestrator-output.ps1'`, JaCoCo output to `artifacts/pester/targeted-cov.xml`) and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/targeted-coverage.2026-05-28T17-31.md` with fields `Timestamp:`, `Command:` (the exact PowerShell block), `EXIT_CODE:`, `Output Summary:` recording the entry numeric line covered/missed/total and branch (CommandsExecutedCount / CommandsAnalyzedCount) values for the file (expected near 42.1% LINE per `policy-audit.2026-05-28T17-31.md`).
- [x] [P0-T18] Confirm absence of the planned sibling test file `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1` and record the negative finding in `docs/features/active/remediation-loop-protocol-25/evidence/baseline-c1/sibling-test-absence.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, `SearchScope: tests/claude/hooks/`, `SearchPatterns: validate-orchestrator-output.invoke.Tests.ps1`, `SearchResult: none`.

### Phase 1 — Implement Sibling Pester Test File for `Invoke-OrchestratorOutputValidation` and AC#6 qa-gate Documentation Append

Each P1 task adds exactly one Pester `It` block (or its supporting scaffolding) to the new sibling file `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`. The file must remain under the 500-line cap; the planner sized the 13 scenarios plus scaffolding to fit in approximately 350 lines. The file dot-sources `.claude/hooks/validate-orchestrator-output.ps1` via the same `Join-Path` / `Resolve-Path` pattern used by the existing remediation-loop test file, and uses `Mock Get-CheckpointFileContent` to inject checkpoint content without filesystem writes.

- [x] [P1-T1] Create `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1` with the file header (`#Requires -Version 7.0`, `#Requires -Modules @{ ModuleName = 'Pester'; ModuleVersion = '5.0.0' }`, `<#.SYNOPSIS.DESCRIPTION#>` block referencing issue #25 cycle 1 and AC#9), the `BeforeAll` block that dot-sources `.claude/hooks/validate-orchestrator-output.ps1` via `Join-Path $PSScriptRoot '..\..\..\.claude\hooks\validate-orchestrator-output.ps1'` followed by `Resolve-Path -LiteralPath`, and a `script:New-ConformantCheckpointJson` helper that returns a string literal of a minimal valid `orchestrator-state.json` payload (keys: `objective`, `completed_steps`, `next_step`, `last_updated`, optional `remediation_loop`).
- [x] [P1-T2] In `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`, add a top-level `Describe 'Invoke-OrchestratorOutputValidation — payload rejection paths'` block and an `It 'returns Ok=false when CLAUDE_HOOK_INPUT is empty'` test invoking `Invoke-OrchestratorOutputValidation -RawPayload ''` and asserting `$result.Ok -eq $false` and `$result.Message -match 'CLAUDE_HOOK_INPUT is empty'` (scenario 1 of `remediation-inputs.2026-05-28T17-31.md` Implementation Guidance).
- [x] [P1-T3] In the same `Describe`, add `It 'returns Ok=false when payload is not JSON'` invoking `Invoke-OrchestratorOutputValidation -RawPayload 'not-json{'` and asserting `$result.Ok -eq $false` and `$result.Message -match 'failed to parse'` (scenario 2).
- [x] [P1-T4] In the same `Describe`, add `It 'returns Ok=false when payload output field is empty'` invoking `Invoke-OrchestratorOutputValidation -RawPayload '{ "output": "" }'` and asserting `$result.Ok -eq $false` and `$result.Message -match 'agent output is empty'` (scenario 3).
- [x] [P1-T5] In `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`, add a second `Describe 'Invoke-OrchestratorOutputValidation — checkpoint rejection paths'` block and an `It 'returns Ok=false when the checkpoint file does not exist'` test that uses `Mock Get-CheckpointFileContent { return @{ Exists = $false; Content = $null } }` and invokes `Invoke-OrchestratorOutputValidation -RawPayload '{ "output": "summary" }' -CheckpointPath 'artifacts/orchestration/orchestrator-state.json'` asserting `$result.Ok -eq $false` and `$result.Message -match 'does not exist'` (scenario 4).
- [x] [P1-T6] In the same `Describe`, add `It 'returns Ok=false when the checkpoint file is empty'` using `Mock Get-CheckpointFileContent { return @{ Exists = $true; Content = '' } }`; assert `$result.Ok -eq $false` and `$result.Message -match 'is empty'` (scenario 5).
- [x] [P1-T7] In the same `Describe`, add `It 'returns Ok=false when the checkpoint content is not valid JSON'` using `Mock Get-CheckpointFileContent { return @{ Exists = $true; Content = 'not-json{' } }`; assert `$result.Ok -eq $false` and `$result.Message -match 'is not valid JSON'` (scenario 6).
- [x] [P1-T8] In the same `Describe`, add `It 'returns Ok=false when checkpoint is missing required fields'` using `Mock Get-CheckpointFileContent { return @{ Exists = $true; Content = '{ "objective": "x" }' } }`; assert `$result.Ok -eq $false` and `$result.Message -match 'missing required field\(s\)'` (scenario 7).
- [x] [P1-T9] In the same `Describe`, add `It 'returns Ok=false when checkpoint objective is empty'` using `Mock Get-CheckpointFileContent` to return a JSON string with `objective=""` but all other required fields populated; assert `$result.Ok -eq $false` and `$result.Message -match "empty 'objective' field"` (scenario 8).
- [x] [P1-T10] In `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`, add a third `Describe 'Invoke-OrchestratorOutputValidation — checkpoint acceptance paths'` block and an `It 'returns Ok=true when checkpoint has no remediation_loop field'` using `Mock Get-CheckpointFileContent` to return the conformant checkpoint JSON without a `remediation_loop` key; assert `$result.Ok -eq $true` and `$result.Message -eq $null` (scenario 9).
- [x] [P1-T11] In the same `Describe`, add `It 'returns Ok=true when checkpoint has a conformant remediation_loop'` using `Mock Get-CheckpointFileContent` to return checkpoint JSON whose `remediation_loop` contains one well-formed cycle (`plan_path` set, `preflight.final_status='clear'`, `execution_status='complete'`, `blocking_count=0`, `exit_condition_met=true`); assert `$result.Ok -eq $true` and `$result.Message -eq $null` (scenario 10).
- [x] [P1-T12] In `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`, add a fourth `Describe 'Invoke-OrchestratorOutputValidation — remediation_loop rejection paths'` block and an `It 'returns Ok=false when a cycle is missing plan_path'` using `Mock Get-CheckpointFileContent` to return checkpoint JSON whose `remediation_loop.cycles[0]` omits `plan_path`; assert `$result.Ok -eq $false` and `$result.Message -match "missing required field 'plan_path'"` (scenario 11).
- [x] [P1-T13] In the same `Describe`, add `It 'returns Ok=false when a cycle has exit_condition_met true and blocking_count 2'` using `Mock Get-CheckpointFileContent` to return checkpoint JSON whose `remediation_loop.cycles[0]` has `exit_condition_met=true` and `blocking_count=2`; assert `$result.Ok -eq $false` and `$result.Message -match "'exit_condition_met' is true"` and `$result.Message -match "blocking_count' is '2'"` (scenario 12).
- [x] [P1-T14] In the same `Describe`, add `It 'returns Ok=false when execution_status in_progress and preflight pending'` using `Mock Get-CheckpointFileContent` to return checkpoint JSON whose `remediation_loop.cycles[0]` has `execution_status='in_progress'` and `preflight.final_status='pending'` and `plan_path` set; assert `$result.Ok -eq $false` and `$result.Message -match "'execution_status' is 'in_progress'"` and `$result.Message -match "'preflight.final_status' is 'pending'"` (scenario 13).
- [x] [P1-T15] Verify that `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1` totals fewer than 500 lines by reading the file and writing the line count to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/test-file-size.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the line count and the 500-line budget remaining. If the file is at or above 500 lines, this task fails and the plan must split scenarios across two sibling files before re-running.
- [x] [P1-T16] Append an `## Enforcement Channel` subsection to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-6-schema-shape.2026-05-28T12-57.md` (without altering existing content or the original `Timestamp: 2026-05-28T12-57` line) stating verbatim: AC#6 enforcement is realized via `.claude/schemas/orchestrator-state.schema.json` + `.claude/hooks/validate-orchestrator-output.ps1::Test-RemediationLoopShape`. The MCP tool `mcp__drm-copilot__validate_orchestration_artifacts` retains its upstream-owned built-in schema unchanged. The append MUST contain the literal token `Enforcement realized via repo-local` so that the verification grep in `remediation-inputs.2026-05-28T17-31.md` Finding 2 returns one hit.

### Phase 2 — Final QA Loop and Acceptance-Criterion Verification (Cycle 1)

The final QA loop runs the PowerShell toolchain in order (format → analyze → test) per `.claude/rules/powershell.md`. Any auto-modification or failure restarts the loop from P2-T1.

- [x] [P2-T1] Run `mcp__drm-copilot__run_poshqc_format` on the working tree and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/poshqc-format-final.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If files are auto-modified, restart from P2-T1.
- [x] [P2-T2] Run `mcp__drm-copilot__run_poshqc_analyze` with `scan_folders=[".claude/hooks","tests/claude/hooks"]` and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/poshqc-analyze-final.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If failures occur or files are auto-modified, restart from P2-T1.
- [x] [P2-T3] Run `mcp__drm-copilot__run_poshqc_test` with `scan_folders=["tests/claude/hooks"]` and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/poshqc-test-final.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing the new test count (13 additional `It` blocks expected) and the aggregate pass/fail totals across both sibling test files. If failures occur or files are auto-modified, restart from P2-T1.
- [x] [P2-T4] Run the targeted file-level Pester coverage command (verbatim from `policy-audit.2026-05-28T17-31.md` Appendix B, but with `Run.Path` widened to include both `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1` and `tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`, keeping `CodeCoverage.Path = '.claude/hooks/validate-orchestrator-output.ps1'`, JaCoCo output to `artifacts/pester/targeted-cov.xml`) and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/targeted-coverage-final.2026-05-28T17-31.md` with `Timestamp:`, `Command:` (exact PowerShell block), `EXIT_CODE:`, `Output Summary:` reporting numeric LINE covered/missed/total, computed LINE % (must be >= 85%), and BRANCH (CommandsExecutedCount / CommandsAnalyzedCount) % (must be >= 75%). If LINE < 85% or BRANCH < 75%, this task fails and the plan must add scenarios to `validate-orchestrator-output.invoke.Tests.ps1` until thresholds are met, restarting the loop from P2-T1.
- [x] [P2-T5] Verify AC#9 closure by writing `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/ac-9-coverage-threshold.2026-05-28T17-31.md` with `Timestamp:`, `Command: Read evidence/qa-gates-c1/targeted-coverage-final.2026-05-28T17-31.md`, `EXIT_CODE: 0`, `Output Summary:` quoting the final LINE and BRANCH percentages from P2-T4, the entry percentages from P0-T17, and explicitly stating that the file-level threshold (>= 85% LINE, >= 75% BRANCH) from `.claude/rules/quality-tiers.md` Authoritative Decision #2 is satisfied on `.claude/hooks/validate-orchestrator-output.ps1`.
- [x] [P2-T6] Verify AC#9 test inventory by writing `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/ac-9-test-inventory.2026-05-28T17-31.md` with `Timestamp:`, `Command: Read tests/claude/hooks/validate-orchestrator-output.invoke.Tests.ps1`, `EXIT_CODE: 0`, `Output Summary:` listing the 13 `It` names from P1-T2 through P1-T14 and confirming the existing six `Test-RemediationLoopShape` tests in the sibling cycle-0 test file are unmodified (compare against the cycle-0 baseline file size recorded at P0-T12).
- [x] [P2-T7] Verify AC#6 documentation closure by running a grep for the literal token `Enforcement realized via repo-local` against `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-6-schema-shape.2026-05-28T12-57.md` and writing the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/ac-6-enforcement-channel.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming exactly one hit and that the original `Timestamp: 2026-05-28T12-57` line at the head of the qa-gate file is unchanged.
- [x] [P2-T8] Confirm no out-of-scope edits by running `git diff --name-only main..HEAD` and writing the file list to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates-c1/scope-check.2026-05-28T17-31.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` enumerating the cycle-1 changed files and confirming that none fall under `docs/features/active/2026-05-27-mix-pipeline-gui-19/`, `.claude/agents/`, `.claude/rules/`, `.github/instructions/`, `.claude/hooks/validate-orchestrator-output.ps1`, `.claude/schemas/orchestrator-state.schema.json`, or `evidence/baseline/` (cycle-0 frozen baselines).

---

## Plan-Level Preflight Self-Check

The planner ran the following self-checks against this plan before returning the preflight signal:

1. **Plan-path continuity:** This file is written exactly once at the path supplied by the caller (`docs/features/active/remediation-loop-protocol-25/remediation-plan.2026-05-28T17-31.md`). The cycle-0 plan at `plan.2026-05-28T12-57.md` is not modified.
2. **Atomic task contract:** Every task starts with `- [ ] [P#-T#]`, uses sequential numbering inside its phase (P0-T1..P0-T18, P1-T1..P1-T16, P2-T1..P2-T8), and has a binary outcome with one explicit artifact path or one explicit file edit.
3. **Phase 0 baseline contract:** Phase 0 reads the required policy/skill files in the order defined by `policy-compliance-order` and captures cycle-1 baselines under `evidence/baseline-c1/` without overwriting cycle-0 baselines.
4. **Coverage evidence contract:** Phase 0 and Phase 2 both capture numeric file-level LINE/BRANCH coverage on `.claude/hooks/validate-orchestrator-output.ps1` via the verbatim Appendix B command from the policy audit. P2-T4 fails closed if LINE < 85% or BRANCH < 75%.
5. **Final QA loop:** P2-T1 through P2-T4 execute format → analyze → test → targeted coverage in order. Restart-on-change is encoded in each step's restart clause.
6. **No-SKIPPED rule:** No P2 task contains a SKIPPED branch; every command-bearing task is unconditional.
7. **Evidence path invariant:** Every artifact path resolves to `docs/features/active/remediation-loop-protocol-25/evidence/<kind>/` where `<kind>` is `baseline-c1` or `qa-gates-c1`. No `artifacts/baselines/`, `artifacts/qa/`, `artifacts/coverage/`, or `artifacts/evidence/` paths appear.
8. **Do-not-do compliance:** No task edits `.claude/rules/`, `.claude/agents/`, `.claude/hooks/validate-orchestrator-output.ps1`, `.claude/schemas/orchestrator-state.schema.json`, the existing six-test cycle-0 Pester file, `docs/features/active/2026-05-27-mix-pipeline-gui-19/`, `evidence/baseline/*` cycle-0 artifacts, or any policy document. P1-T16 only appends to the cycle-0 qa-gate doc without altering the original timestamp line.
9. **File-size cap:** P1-T15 explicitly verifies the new test file is under 500 lines and fails the plan if not.
10. **AC mapping:** The two cycle-1 findings (AC#9 file-level coverage; AC#6 enforcement-channel documentation) each have a final-QA verification task pairing them to their closing evidence path (P2-T5/P2-T6 for AC#9; P2-T7 for AC#6).

All checks above resolved positively against the plan body.

**PREFLIGHT: ALL CLEAR**
