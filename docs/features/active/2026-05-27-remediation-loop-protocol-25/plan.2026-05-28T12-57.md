# Atomic Plan — Issue #25 (Bug: remediation-loop-protocol)

- **Issue:** #25
- **Issue URL:** https://github.com/drmoisan/mix-calculator/issues/25
- **Work Mode:** full-bug (source precedence: `docs/features/active/remediation-loop-protocol-25/issue.md` line 5)
- **Feature folder:** `docs/features/active/remediation-loop-protocol-25/`
- **Plan timestamp:** 2026-05-28T12-57
- **Languages in scope:** PowerShell (hook), Pester (tests), Markdown (agent/skill/memory/walkthrough), JSON (schema)
- **Languages NOT in scope:** Python, TypeScript, C#

**Fail-closed evidence rule:** Each evidence-producing task names the artifact path that closes it. Missing baseline, final-QA, or AC-verification artifacts force a BLOCKED or INCOMPLETE verdict; PASS is not permitted in their absence.

**Evidence accounting rule:** Every command-step artifact under `docs/features/active/remediation-loop-protocol-25/evidence/<kind>/` carries `Timestamp:`, `Command:`, `EXIT_CODE:`, and `Output Summary:` per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`.

## Evidence-Path Substitution Notice

This planner inspected the delegation prompt. The prompt names canonical evidence paths under `docs/features/active/remediation-loop-protocol-25/evidence/<kind>/` only. No non-canonical evidence path was supplied; no substitution was required. The canonical scheme defined in `.claude/skills/evidence-and-timestamp-conventions/SKILL.md` is in force for every artifact below.

## Naming Decision (surfaced for policy-audit reviewer)

The user verbally referenced `feature-review.<ts>.md` as one of the three reaudit artifacts. The verified repository convention under `docs/features/active/**` is `feature-audit.<ts>.md`. This plan uses `feature-audit.<ts>.md` everywhere (including in `.claude/agents/orchestrator.md` edits and the reference walkthrough). The policy-audit reviewer must confirm this naming choice during the end-of-cycle `feature-review`. The `.claude/agents/orchestrator.md` edits include an explicit footnote citing this decision so it surfaces in audit.

## Requirements Source

Primary requirements source for this `full-bug` plan is `docs/features/active/remediation-loop-protocol-25/issue.md`. The spec scaffold at `docs/features/active/remediation-loop-protocol-25/spec.md` is empty and is not used as the requirements source. No fill-in of `spec.md` is required by this plan.

## Acceptance Criteria → Task Mapping

The ten acceptance criteria checkboxes in `issue.md` map to plan tasks as follows. Every AC is closed by at least one final-QA verification task.

- AC#1 — `.claude/agents/orchestrator.md` "Remediation Loop Protocol" section names exactly `atomic-planner`, `atomic-executor`, `feature-review` as allowed delegates; prohibits direct typed-engineer worker calls; lists five required artifacts; describes preflight sub-state semantics including `changes_requested` routing back to `atomic-planner`. **Tasks:** P1-T1, P1-T2, P1-T3, P3-T1.
- AC#2 — `.claude/agents/orchestrator.md` documents the scope-change rule (new findings during execution start a new cycle). **Tasks:** P1-T4, P3-T1.
- AC#3 — `.claude/agents/orchestrator.md` extends step 9 CI monitoring to enter the remediation loop on a failed required check, including workflow-file changes. **Tasks:** P1-T5, P3-T1.
- AC#4 — `.claude/agents/orchestrator.md` specifies that the exit gate reads the latest audit's `blocking_count` and only then sets `exit_condition_met = true`. **Tasks:** P1-T6, P3-T1.
- AC#5 — `.claude/skills/remediation-handoff-atomic-planner/SKILL.md` documents the full handoff chain and five required artifacts with the entry-ts / exit-ts timestamp rule and cites `atomic-plan-contract`. **Tasks:** P1-T10, P1-T11, P1-T12, P3-T2.
- AC#6 — schema validated by `mcp__drm-copilot__validate_orchestration_artifacts` requires the `remediation_loop` shape; malformed cycles rejected. Enforcement via repo-local JSON Schema + extended hook validator. **Tasks:** P2-T1, P2-T2, P2-T3, P2-T4, P2-T5, P3-T6.
- AC#7 — orchestrator memory at `.claude/agent-memory/orchestrator/remediation-loop-strict-handoff.md` cited from `.claude/agents/orchestrator.md`. **Tasks:** P1-T7, P1-T13, P1-T14, P3-T7.
- AC#8 — reference walkthrough under `docs/` demonstrates a fully conformant cycle. **Tasks:** P1-T15, P3-T8.
- AC#9 — fixture-based Pester tests accept the conformant state and reject malformed cycles (fixture-fallback path explicitly chosen per research note section 4). **Tasks:** P2-T6, P2-T7, P2-T8, P3-T9.
- AC#10 — the retroactive PR #24 / issue #19 folder under `docs/features/active/2026-05-27-mix-pipeline-gui-19/` is not edited by this bug fix; the missing historical `remediation-plan.2026-05-28T12-17.md` is documented as a known historical gap rather than backfilled. **Tasks:** P3-T10.

---

### Phase 0 — Baselines and Policy Capture

- [x] [P0-T1] Read `.claude/rules/general-code-change.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T2] Read `.claude/rules/powershell.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T3] Read `.claude/rules/tonality.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T4] Read `.claude/rules/ci-workflows.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T5] Read `.claude/rules/general-unit-test.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T6] Read `.claude/rules/quality-tiers.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T7] Read `.claude/skills/atomic-plan-contract/SKILL.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T8] Read `.claude/skills/policy-compliance-order/SKILL.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T9] Read `.claude/skills/evidence-and-timestamp-conventions/SKILL.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T10] Read `docs/features/active/remediation-loop-protocol-25/issue.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T11] Read `artifacts/research/2026-05-28-remediation-loop-protocol-mapping.md` and append its absolute path to the `Policy Order:` list in `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T12] Write the Phase 0 evidence header (`Timestamp:`, `Policy Order:` list, and explicit list of files read) to `docs/features/active/remediation-loop-protocol-25/evidence/phase0/phase0-instructions-read.md`.
- [x] [P0-T13] Capture a baseline PoshQC format result by invoking `mcp__drm-copilot__run_poshqc_format` and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline/poshqc-format.2026-05-28T12-57.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T14] Capture a baseline PoshQC analyze result by invoking `mcp__drm-copilot__run_poshqc_analyze` and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline/poshqc-analyze.2026-05-28T12-57.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T15] Capture a baseline PoshQC Pester test result by invoking `mcp__drm-copilot__run_poshqc_test` and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline/poshqc-test.2026-05-28T12-57.md` with fields `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (the Output Summary must include numeric line-coverage and branch-coverage headline values per `.claude/rules/general-unit-test.md`).
- [x] [P0-T16] Read the current `.claude/hooks/validate-orchestrator-output.ps1` to record its current line count and remaining budget against the 500-line cap, and write the artifact to `docs/features/active/remediation-loop-protocol-25/evidence/baseline/file-sizes.2026-05-28T12-57.md` with `Timestamp:`, `Command: Read .claude/hooks/validate-orchestrator-output.ps1`, `EXIT_CODE: 0`, `Output Summary:` listing current line count and remaining budget.
- [x] [P0-T17] Confirm `.claude/agent-memory/orchestrator/remediation-loop-strict-handoff.md` is absent (per research note section 1) and record the negative finding in `docs/features/active/remediation-loop-protocol-25/evidence/baseline/memory-absence.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, `SearchScope: .claude/agent-memory/orchestrator/`, `SearchPatterns: remediation-loop-strict-handoff.md`, `SearchResult: none`.
- [x] [P0-T18] Confirm `.claude/schemas/orchestrator-state.schema.json` is absent (per research note section 2) and record the negative finding in `docs/features/active/remediation-loop-protocol-25/evidence/baseline/schema-absence.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`, `SearchScope: .claude/schemas/`, `SearchPatterns: *.schema.json`, `SearchResult: none`.

### Phase 1 — Authoring Edits (Markdown + Memory)

- [x] [P1-T1] Append a top-level `## Remediation Loop Protocol` section to `.claude/agents/orchestrator.md` that names exactly `atomic-planner`, `atomic-executor`, and `feature-review` as the only allowed delegates during a remediation cycle.
- [x] [P1-T2] In the same `## Remediation Loop Protocol` section in `.claude/agents/orchestrator.md`, add a paragraph explicitly prohibiting direct invocation of `python-typed-engineer`, `typescript-engineer`, `csharp-typed-engineer`, and `powershell-typed-engineer` inside a remediation cycle, stating that workers are invoked by `atomic-executor` only.
- [x] [P1-T3] In the same `## Remediation Loop Protocol` section in `.claude/agents/orchestrator.md`, add a subsection listing the five required artifacts per cycle (`remediation-inputs.<entry-ts>.md`, `remediation-plan.<entry-ts>.md`, `code-review.<exit-ts>.md`, `feature-audit.<exit-ts>.md`, `policy-audit.<exit-ts>.md`) and a subsection describing preflight sub-state semantics where `preflight.outcome in {clear, changes_requested}` and `changes_requested` routes back to `atomic-planner` (never to the orchestrator or to execution).
- [x] [P1-T4] In the same `## Remediation Loop Protocol` section in `.claude/agents/orchestrator.md`, add a `Scope-change rule` paragraph stating that any new finding surfaced during execution must trigger a new cycle with a follow-up `remediation-inputs.<new-ts>.md`, and that the orchestrator does not re-prompt the same worker.
- [x] [P1-T5] Extend the existing step-9 / CI-monitoring guidance in `.claude/agents/orchestrator.md` (under `## Completion Requirements` or the equivalent existing section) to state that a failed required check after the PR is open transitions to `remediation.cycle_N+1.inputs` and runs the full loop, and that workflow-file changes are implemented through the loop and trigger the `modified-workflow-needs-green-run` rule.
- [x] [P1-T6] In the `## Remediation Loop Protocol` section in `.claude/agents/orchestrator.md`, add an `Exit gate` paragraph stating that the orchestrator reads the latest cycle's audit artifacts, computes `blocking_count == 0`, and only then sets `exit_condition_met = true` and marks the loop complete.
- [x] [P1-T7] In `.claude/agents/orchestrator.md`, add a citation block at the end of the `## Remediation Loop Protocol` section that references `.claude/skills/remediation-handoff-atomic-planner/SKILL.md` and `.claude/agent-memory/orchestrator/remediation-loop-strict-handoff.md` so both are surfaced at startup.
- [x] [P1-T8] In `.claude/agents/orchestrator.md`, extend the `## Checkpoint Persistence` section with a paragraph describing the `remediation_loop` object shape (citing the JSON Schema file `.claude/schemas/orchestrator-state.schema.json` as the canonical contract) and stating that `next_step` uses cycle-aware names of the form `remediation.cycle_N.{plan,preflight,execute,reaudit,exit_check}` while in the loop.
- [x] [P1-T9] In `.claude/agents/orchestrator.md`, append a single footnote at the end of the `## Remediation Loop Protocol` section calling out the naming decision `feature-audit.<ts>.md` (not `feature-review.<ts>.md`) for policy-audit reviewer confirmation.
- [x] [P1-T10] Rewrite `.claude/skills/remediation-handoff-atomic-planner/SKILL.md` to document the full chain orchestrator -> atomic-planner -> atomic-executor (preflight) -> atomic-planner (revise loop) -> atomic-executor (execute) -> feature-review, including the preflight revision sub-loop.
- [x] [P1-T11] In `.claude/skills/remediation-handoff-atomic-planner/SKILL.md`, add a `## Required Artifacts` section listing the five filenames with the entry-ts / exit-ts timestamp rule explicitly stated (entry-ts applies to `remediation-inputs` and `remediation-plan`; exit-ts applies to `code-review`, `feature-audit`, `policy-audit`).
- [x] [P1-T12] In `.claude/skills/remediation-handoff-atomic-planner/SKILL.md`, add a `## Plan Shape` paragraph citing `.claude/skills/atomic-plan-contract/SKILL.md` as the schema for `remediation-plan.<ts>.md`.
- [x] [P1-T13] Write the orchestrator memory file at `.claude/agent-memory/orchestrator/remediation-loop-strict-handoff.md` using the standard memory frontmatter (`name: remediation-loop-strict-handoff`, `description: <one-line>`, `metadata.type: feedback`) with body recording the strict delegation chain, a `Why:` line, and a `How to apply:` line.
- [x] [P1-T14] Append a one-line index entry for `remediation-loop-strict-handoff.md` to `.claude/agent-memory/orchestrator/MEMORY.md` in the form `- [Remediation loop strict handoff](remediation-loop-strict-handoff.md) — <one-line hook>`.
- [x] [P1-T15] Create the reference walkthrough document at `docs/orchestrator-remediation-loop-reference.md` demonstrating one fully conformant cycle (every artifact present, every state transition recorded, including a sample `orchestrator-state.json` snippet showing the `remediation_loop` object with one conformant cycle).

### Phase 2 — Schema, Validator, and Tests (JSON + PowerShell + Pester)

- [x] [P2-T1] Create `.claude/schemas/orchestrator-state.schema.json` declaring `$schema: https://json-schema.org/draft/2020-12/schema`, top-level type `object`, and a `remediation_loop` property whose shape matches the issue body (Proposed Fix item 3).
- [x] [P2-T2] In `.claude/schemas/orchestrator-state.schema.json`, define `remediation_loop.current_cycle` as an integer and `remediation_loop.cycles` as an array of cycle objects, each requiring `entry_timestamp`, `inputs_path`, `plan_path`, `preflight`, `execution_status`, `audit_paths`, `blocking_count`, and `exit_condition_met` per the issue's Proposed Fix item 3.
- [x] [P2-T3] In `.claude/schemas/orchestrator-state.schema.json`, define the `preflight` sub-object as requiring `iterations` (integer) and `final_status` (enum `clear|changes_requested|pending`), and define `execution_status` as enum `not_started|in_progress|complete|failed`.
- [x] [P2-T4] In `.claude/schemas/orchestrator-state.schema.json`, add a conditional constraint (`if/then` or `allOf`) that rejects any cycle where `exit_condition_met == true` and `blocking_count != 0`, and a constraint that rejects any cycle where `execution_status` is set to `in_progress`/`complete`/`failed` while `preflight.final_status != "clear"`.
- [x] [P2-T5] Extend `Invoke-OrchestratorOutputValidation` in `.claude/hooks/validate-orchestrator-output.ps1` to detect the presence of a `remediation_loop` field in the parsed checkpoint and, when present, validate the object shape against the constraints stated in the JSON Schema (missing `plan_path` -> reject; `exit_condition_met: true` with `blocking_count != 0` -> reject; `execution_status` set with `preflight.final_status != "clear"` -> reject); total file length after the edit must remain below 500 lines.
- [x] [P2-T6] Create `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1` with a `Describe` block dot-sourcing `.claude/hooks/validate-orchestrator-output.ps1` and a fixture-builder helper that produces a baseline conformant `orchestrator-state.json` object containing one well-formed cycle.
- [x] [P2-T7] In `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1`, add three positive-path `It` cases: (a) accept a state with no `remediation_loop` field, (b) accept a state with a single conformant cycle, (c) accept a state with multiple sequential conformant cycles.
- [x] [P2-T8] In `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1`, add three negative-path `It` cases: (a) reject a cycle whose `plan_path` is missing, (b) reject a cycle where `exit_condition_met: true` and `blocking_count != 0`, (c) reject a cycle where `execution_status: in_progress` and `preflight.final_status: pending`; the test file must remain below 500 lines.

### Phase 3 — Final QA Loop and Acceptance-Criterion Verification

- [x] [P3-T1] Verify AC#1, AC#2, AC#3, AC#4 by `Grep`/`Read` inspection of `.claude/agents/orchestrator.md` and write the AC verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-1-2-3-4-orchestrator-md.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing each AC and the verified anchor text.
- [x] [P3-T2] Verify AC#5 against `.claude/skills/remediation-handoff-atomic-planner/SKILL.md` and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-5-skill-handoff.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` confirming the chain text, the five-artifact list with the entry-ts/exit-ts rule, and the citation of `atomic-plan-contract`.
- [x] [P3-T3] Run `mcp__drm-copilot__run_poshqc_format` on the working tree and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/poshqc-format-final.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If files are auto-modified, restart the loop from this task.
- [x] [P3-T4] Run `mcp__drm-copilot__run_poshqc_analyze` and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/poshqc-analyze-final.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If failures occur or files are auto-modified, restart the loop from P3-T3.
- [x] [P3-T5] Run `mcp__drm-copilot__run_poshqc_test` (must include the new Pester test file `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1`) with coverage enabled per repo policy, and write the result to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/poshqc-test-final.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric line-coverage and branch-coverage values (line >= 85%, branch >= 75% per `.claude/rules/general-unit-test.md` and `.claude/rules/quality-tiers.md`). If failures occur or files are auto-modified, restart from P3-T3.
- [x] [P3-T6] Verify AC#6 by `Read`-inspecting `.claude/schemas/orchestrator-state.schema.json` for the required `remediation_loop` shape and conditional constraints, and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-6-schema-shape.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing each required field, each enum, and each `if/then` constraint.
- [x] [P3-T7] Verify AC#7 by `Grep`-confirming that `.claude/agents/orchestrator.md` cites `.claude/agent-memory/orchestrator/remediation-loop-strict-handoff.md` and that the memory file exists with valid frontmatter, and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-7-memory-citation.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P3-T8] Verify AC#8 by `Read`-inspecting `docs/orchestrator-remediation-loop-reference.md` for the conformant-cycle walkthrough, and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-8-walkthrough.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing each artifact reference and each recorded state transition.
- [x] [P3-T9] Verify AC#9 by inspecting the Pester test output recorded in the P3-T5 artifact, confirming that all six fixture-based `It` cases passed, and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/ac-9-pester-fixtures.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` listing each test case name and result and documenting the fixture-fallback choice per AC#9 second sentence.
- [x] [P3-T10] Verify that `docs/features/active/2026-05-27-mix-pipeline-gui-19/` was not modified by running `git diff --name-only origin/main...HEAD` (or equivalent) and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/no-historical-edits.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` showing the diff list and confirming the historical path is absent.
- [x] [P3-T11] Verify file-size compliance for `.claude/hooks/validate-orchestrator-output.ps1` and `tests/claude/hooks/validate-orchestrator-output.remediation-loop.Tests.ps1` (both < 500 lines) and write the verification artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/file-size-compliance.2026-05-28T12-57.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` reporting each file's line count.
- [x] [P3-T12] Write the end-of-plan summary artifact to `docs/features/active/remediation-loop-protocol-25/evidence/qa-gates/plan-complete.2026-05-28T12-57.md` with `Timestamp:`, `Command: (n/a)`, `EXIT_CODE: 0`, `Output Summary:` listing each AC (AC#1 through AC#10) paired with the verification artifact path that closes it (AC#10's closing artifact is `no-historical-edits.2026-05-28T12-57.md` produced by P3-T10).
