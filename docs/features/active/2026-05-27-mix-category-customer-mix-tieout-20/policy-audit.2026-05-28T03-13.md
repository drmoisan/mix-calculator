# Policy Compliance Audit: mix-category-customer-mix-tieout (Issue #20) — R4 Re-audit

**Audit Date:** 2026-05-28
**Audit Type:** Remediation pass 1 (R4) re-audit after test-module split
**Code Under Test:** `src/mix_rollups.py`, `src/_mix_rollups_helpers.py`, `src/mix_pipeline_run.py`, `tests/test_mix_rollups.py`, `tests/test_mix_rollups_tieout.py`, `tests/_mix_rollups_fixtures.py`, `tests/mix_bottomsup_fixtures.py`
**Base branch:** `main` (resolved `origin/main`)
**Merge-base SHA:** `b0e048fe9f6d5004e158cac03c3c3261cbba0eb8`
**Branch head SHA:** `528dcbc37a0422cced21b70c61e0ce34ec1a120f`
**Work mode:** `full-bug`

**Coverage Metrics by Language:**

| Language | Files Changed | Tests | Test Result | Baseline Coverage | Post-Change Coverage | New Code Coverage |
|----------|--------------|-------|-------------|-------------------|---------------------|-------------------|
| Python | 7 files (3 src, 4 test) | 220 tests | PASS — 220 pass, 0 fail | 100% lines, 100% branch | 100% lines, 100% branch | 100% line, 100% branch |

**Note:** No TypeScript, PowerShell, C#, Bash, or JSON source files are changed in the branch diff (`b0e048f..528dcbc`). Coverage verdicts for those languages are `N/A - zero changed files on branch`, which is the only context in which N/A is acceptable.

### Coverage Evidence Checklist

- Python baseline coverage artifact: `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/remediation-baseline/pytest-baseline.2026-05-27T22-40.md` (post-split-start baseline; 100/100)
- Python post-change coverage artifact: `coverage/lcov.info` (regenerated by reviewer 2026-05-28T03-13; 100% line / 100% branch) and `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/qa-gates/coverage-delta.2026-05-27T22-40.md`
- TypeScript baseline coverage artifact: `N/A - out of scope` (zero TypeScript files changed on branch)
- TypeScript post-change coverage artifact: `N/A - out of scope` (zero TypeScript files changed on branch)
- PowerShell baseline coverage artifact: `N/A - out of scope` (zero PowerShell files changed on branch)
- PowerShell post-change coverage artifact: `N/A - out of scope` (zero PowerShell files changed on branch)
- Per-language comparison summary: see Section 1.2.1 below.

### 1.2.1 Per-Language Coverage Comparison

- Python: Baseline: 100% line (100% branch) -> Post-change: 100% line (100% branch). Change: +0% line (+0% branch). New/changed-code coverage: 100%. Disposition: PASS. Evidence: `evidence/remediation-baseline/pytest-baseline.2026-05-27T22-40.md`, `evidence/qa-gates/coverage-delta.2026-05-27T22-40.md`, `coverage/lcov.info` (reviewer rerun 2026-05-28T03-13).
- TypeScript: Baseline: N/A - out of scope -> Post-change: N/A - out of scope. Change: N/A. New/changed-code coverage: `N/A - out of scope`. Disposition: N/A. Evidence: `N/A - out of scope` (zero changed files on branch).
- PowerShell: Baseline: N/A - out of scope -> Post-change: N/A - out of scope. Change: N/A. New/changed-code coverage: `N/A - out of scope`. Disposition: N/A. Evidence: `N/A - out of scope` (zero changed files on branch).

### 1.2.2 Verdict Rule

**Non-negotiable verdict rule:** This audit reports numeric baseline and post-change coverage for Python (the only in-scope language). The verdict is PASS; see Section 10.

---

## Executive Summary

The R4 remediation cleanly addresses the one Blocking finding from R3 (`tests/test_mix_rollups.py` at 562 lines). The cohesive split moved fixture helpers into `tests/_mix_rollups_fixtures.py` (225 lines, with an explicit `__all__` export list) and the AC8 four-layer tie-out and single-scenario regression tests into `tests/test_mix_rollups_tieout.py` (167 lines), leaving `tests/test_mix_rollups.py` at 204 lines. Production code is unchanged from R3; no test added, removed, renamed, or skipped.

This reviewer re-ran the full Python toolchain against the branch head: Black, Ruff, Pyright, and Pytest with branch coverage all pass in a single clean pass (220 tests, 100% line / 100% branch). All other R3 PASS findings (design, type safety, suppression discipline, confidentiality, evidence locations, scope discipline, determinism, docstrings) hold unchanged. Zero Blocking and zero Non-Blocking findings.

**Policy documents evaluated:**
- `.claude/rules/general-code-change.md`
- `.claude/rules/general-unit-test.md`
- `.claude/rules/quality-tiers.md`
- `.claude/rules/self-explanatory-code-commenting.md`
- `.claude/rules/tonality.md`

**Language-specific policies evaluated:**
- Python: `.claude/rules/python.md` + `.claude/rules/python-suppressions.md`
- N/A PowerShell (zero changed files on branch)
- N/A TypeScript (zero changed files on branch)
- N/A C# (zero changed files on branch)

**Temporary artifacts cleanup:**
- Reviewer captured one scratch git-diff file (`evidence_diff_tmp.txt`) for a confidentiality scan; deleted before completion.

---

## Rejected Scope Narrowing

The caller prompt explicitly states "Do not narrow scope" and supplies merge-base, head SHA, base branch, and AC source — all legitimate authoritative scope inputs. No instruction attempts to narrow scope to a plan, task, phase, or file subset, and no instruction marks any language with changed files as out of scope. Nothing to reject.

---

## Evidence Location Compliance

- Scan command: `git diff --name-only b0e048fe9f6d5004e158cac03c3c3261cbba0eb8..528dcbc37a0422cced21b70c61e0ce34ec1a120f | grep -E '^artifacts/(baselines|qa|evidence|coverage)/'`
- Result: `NO_VIOLATIONS`. Zero matches under the forbidden prefixes.
- All evidence is correctly under `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/<kind>/` (subkinds: `baseline/`, `remediation-baseline/`, `qa-gates/`, `regression-testing/`, `issue-updates/`).
- `scripts/validate_evidence_locations.py` does not exist in this repo; the git-diff scan is the substitute. The PowerShell PreToolUse hook `enforce-evidence-locations.ps1` is the live enforcement mechanism.
- `EVIDENCE_LOCATION_OVERRIDE_REJECTED`: none. No caller instruction supplied a non-canonical evidence path.

---

## 1. General Unit Test Policy Compliance

### 1.1 Core Principles

| Requirement | Status | Evidence |
|---|---|---|
| Independence — tests run in any order | PASS | All 220 tests pass under `pytest-randomly` random ordering; each test builds its own fabricated DataFrames via the shared `_mix_rollups_fixtures` helpers; no shared mutable state. |
| Isolation — single behavior per test | PASS | Each `test_*` targets one builder or one invariant (e.g., `test_category_layer_retains_single_scenario_volume`, `test_layer_mix_equals_full_aggregation_minus_rollup`). |
| Fast execution | PASS | Full suite (220 tests) completed in 17.23s in the reviewer rerun. |
| Determinism | PASS | Fabricated in-memory DataFrames only; no clock, RNG, network, or filesystem dependence in any changed test. |
| Readability & Maintainability | PASS | All test files now under 500 lines (max 204 in `test_mix_rollups.py`); the new fixture module has a module docstring stating purpose and non-pytest nature; the new tie-out module has a module docstring stating purpose. The R3 file-size concern is closed. |

### 1.2 Coverage and Scenarios

| Requirement | Status | Evidence |
|---|---|---|
| Baseline coverage documented | PASS | 100% line / 100% branch baseline (pre-split). Source: `evidence/remediation-baseline/pytest-baseline.2026-05-27T22-40.md`. |
| No coverage regression | PASS | Post-change 100% line / 100% branch (reviewer rerun, this audit). Change: +0% / +0%. No regression on changed lines. |
| New-code coverage | PASS | The three changed source files (`mix_rollups.py`, `_mix_rollups_helpers.py`, `mix_pipeline_run.py`) each report 0 missed statements and 0 partial branches: 100/100. Above the 85%/75% uniform threshold. |
| Comprehensive coverage | PASS | The three regression tests in `tests/test_mix_rollups_tieout.py` cover single-scenario LE/AOP retention at category and customer granularity and the NPI-minus-rollup identity; existing tests in `tests/test_mix_rollups.py` cover the SKU layer, rollups, full-chain, country, and the nonzero-Lbs stage filter. |
| Positive flows | PASS | `test_build_mix_1_sku_produces_sku_mix_column`, `test_full_mix_chain_columns_and_fill_zero`. |
| Negative flows | PASS | `test_build_mix_stage_keeps_only_nonzero_lbs_lines` verifies zero-Lbs lines are dropped at the stage. |
| Edge cases | PASS | `_single_scenario_mix_base_fixture` exercises new-in-LE (zero AOP Lbs) and dropped-in-LE (zero LE Lbs) lines plus a cross-group leakage guard. |
| Error handling | N/A | Pure builders; no contract-relevant exceptions. |
| Concurrency | N/A | Pure synchronous transforms. |
| State transitions | N/A | Stateless transforms. |

### 1.3 Test Structure

PASS. All new and changed tests use Arrange-Act-Assert structure; descriptive `test_*` names; one behavior per test; behavioral assertions.

### 1.4 External Dependencies

PASS. No external services, no temp files, no real network, no real filesystem in the changed tests.

### 1.5 Documentation

PASS. New module docstrings state purpose. New test functions and fixtures have docstrings.

### 1.6 Test Categories

| Requirement | Status | Evidence |
|---|---|---|
| Unit tests | PASS | All changed tests are unit tests under `tests/`. |
| Property-based tests | N/A | Module rigor tier for `src/mix_rollups.py` is T2 in `quality-tiers.yml`; no new pure functions added in this R4 cycle that would extend the property-test density requirement. |
| Golden/snapshot | N/A | Not a classifier-output module. |
| Contract/schema | N/A | No host-service boundary changed. |
| Mutation | N/A | T2 module; mutation is trend-only, not gated. |
| Integration | PASS | End-to-end pipeline run captured in `evidence/regression-testing/e2e-run.2026-05-27T22-27.md`. |

### 1.7 Determinism Infrastructure

PASS. No use of `time.sleep`, `Thread.Sleep`, `Task.Delay`, or `setTimeout` in tests. No clock or RNG dependency in the changed tests.

---

## 2. General Code Change Policy Compliance

### 2.1 Design Principles

PASS. Simplicity first (mechanical split); reusability (shared fixtures consolidated); extensibility (no API change); separation of concerns (builders pure; fixture module is data-construction only).

### 2.2 Classes, Functions, and APIs

PASS. Helpers are pure stateless functions; no class abstraction added.

### 2.3 File-Size Limit (500 lines)

| File | Lines (`(Get-Content …).Count`) | Verdict |
|---|---|---|
| `tests/test_mix_rollups.py` | 204 | PASS |
| `tests/test_mix_rollups_tieout.py` | 167 | PASS |
| `tests/_mix_rollups_fixtures.py` | 225 | PASS |
| `tests/mix_bottomsup_fixtures.py` | 295 | PASS |
| `src/mix_rollups.py` | 343 | PASS |
| `src/_mix_rollups_helpers.py` | 182 | PASS |
| `src/mix_pipeline_run.py` | 137 | PASS |

The R3 finding (`tests/test_mix_rollups.py` at 562 lines) is **CLOSED**. All changed files are now within the 500-line limit. Measurement-consistency check: zero `Measure-Object -Line` patterns across the feature folder; this audit and the remediation cycle both use `(Get-Content …).Count` (= `wc -l +1`), the consistent measurement basis.

### 2.4 Mandatory Toolchain Loop

PASS. Single clean pass:
- Black: `46 files would be left unchanged.` (exit 0)
- Ruff: `All checks passed!` (exit 0)
- Pyright: `0 errors, 0 warnings, 0 informations` (exit 0)
- Pytest: `220 passed in 17.23s` (exit 0)

### 2.5 Error Handling and Logging

PASS. No new error paths; no broad excepts introduced.

### 2.6 Naming

PASS. `snake_case` functions, `_`-prefixed private helpers, accurate `__all__` lists.

### 2.7 Public APIs and Compatibility

PASS. Builder signature change is internal; `run_transforms` is the only in-repo caller and is updated. CLI surface unchanged.

### 2.8 Dependencies

PASS. No new dependencies.

### 2.9 I/O Boundaries

PASS. Builders are I/O-free; `main` remains the only I/O boundary.

---

## 3. Language-Specific Code Change Policy Compliance

### 3.1 Python — `.claude/rules/python.md`

| Requirement | Status | Evidence |
|---|---|---|
| Black formatted | PASS | `env -u VIRTUAL_ENV poetry run black --check .` exit 0. |
| Ruff clean | PASS | `env -u VIRTUAL_ENV poetry run ruff check .` exit 0. |
| Pyright clean | PASS | `env -u VIRTUAL_ENV poetry run pyright` exit 0. |
| Pytest passes | PASS | 220 passed in 17.23s; 100/100 coverage. |
| PEP 8 naming | PASS | Reviewed across all changed files. |
| Strong typing | PASS | All public/private function signatures fully annotated; no untyped `def` introduced. |
| Imports absolute | PASS | All changed files use absolute imports (`from src.…`, `from tests.…`). |
| Error handling specific | PASS | No new broad excepts. |
| No print statements | PASS | No `print(...)` introduced. |

### 3.2 Python Suppressions — `.claude/rules/python-suppressions.md`

PASS. Repo-wide grep across `src/**/*.py` and `tests/**/*.py` for `noqa`, `type:\s*ignore`, and `pyright:\s*ignore` returned **zero matches**. The `__all__` declaration in `tests/_mix_rollups_fixtures.py` is a justified mechanical fix that explicitly exports the six shared fixture helpers consumed by `tests/test_mix_rollups.py` and `tests/test_mix_rollups_tieout.py`; it is an export contract, not a suppression, and is not subject to the suppression-authorization policy (which governs `noqa`/`type:ignore`/`pyright:ignore` markers only).

### 3.3 Other languages

N/A — no changed files in TypeScript, PowerShell, or C#.

---

## 4. Language-Specific Unit Test Policy Compliance

### 4.1 Python — pytest rules

PASS. Per-test single-behavior; descriptive names; AAA structure; `pytest.mark.parametrize` not required for the new tests (fabricated scenarios are distinct and small); fixtures are function-scope by default; no `time.sleep`; tests mirror code structure (`tests/test_mix_rollups.py` ↔ `src/mix_rollups.py`; `tests/test_mix_rollups_tieout.py` is a peer test module for the same source); no external dependencies; coverage thresholds met.

### 4.2 Test placement after R4 split

PASS. The split keeps the primary `tests/test_mix_rollups.py` mirror module intact (204 lines, 8 tests for rollup/layer/stage behavior). The `tests/test_mix_rollups_tieout.py` peer module isolates the three AC8/regression tie-out tests behind a descriptive name; shared fixtures are in `tests/_mix_rollups_fixtures.py` (an underscore-prefixed non-pytest module).

---

## 5. Test Coverage Detail

### 5.1 Per-source-file coverage (post-change)

| Source file | Stmts | Miss | Branch | BrPart | Cover |
|---|---|---|---|---|---|
| `src/_mix_rollups_helpers.py` | 30 | 0 | 0 | 0 | 100% |
| `src/mix_pipeline_run.py` | 26 | 0 | 0 | 0 | 100% |
| `src/mix_rollups.py` | 59 | 0 | 0 | 0 | 100% |

### 5.2 Repo-wide totals

- Statements: 1085
- Missing: 0
- Branches: 206
- Partial branches: 0
- Line coverage: 100%
- Branch coverage: 100%

### 5.3 Baseline vs Post-change

| Metric | Baseline | Post-change | Disposition |
|---|---|---|---|
| Repo-wide line coverage | 100% | 100% | PASS — no regression |
| Repo-wide branch coverage | 100% | 100% | PASS — no regression |

---

## 6. Test Execution Metrics

| Metric | Value |
|---|---|
| Total tests collected | 220 |
| Passed | 220 |
| Failed | 0 |
| Skipped | 0 |
| Errors | 0 |
| Duration | 17.23s |
| Random order plugin active | yes (`pytest-randomly`) |
| Seed printed on failure | n/a (no failures) |

Source: reviewer rerun via `env -u VIRTUAL_ENV poetry run pytest --cov=src --cov-branch --cov-report=term` (this audit, 2026-05-28T03-13).

---

## 7. Code Quality Checks

| Tool | Command | Exit | Result |
|---|---|---|---|
| Black | `env -u VIRTUAL_ENV poetry run black --check .` | 0 | `46 files would be left unchanged.` |
| Ruff | `env -u VIRTUAL_ENV poetry run ruff check .` | 0 | `All checks passed!` |
| Pyright | `env -u VIRTUAL_ENV poetry run pyright` | 0 | `0 errors, 0 warnings, 0 informations` |
| Pytest+cov | `env -u VIRTUAL_ENV poetry run pytest --cov=src --cov-branch --cov-report=term` | 0 | `220 passed in 17.23s` (100/100) |

Cross-cutting:
- **`benchmark-baselines.md`** — N/A; no benchmark baselines added or modified.
- **`ci-workflows.md`** — N/A; no `pwsh` workflow steps with deliberately-failing nested commands added or modified.
- **`self-explanatory-code-commenting.md`** — PASS; module docstrings on new files, function docstrings present, no numbered notes detected.
- **`tonality.md`** — PASS; measured, factual evidence and audit phrasing.

### 7.1 Confidentiality

Scanned the full branch diff (source, tests, and all evidence artifacts) for comma-grouped multi-thousand numeric tokens, dollar-amounts, and multi-digit decimal aggregates. **Zero matches** for those patterns. The string `LE v AOP Gross to Net Decomp.xlsx` appears only as the documented command argument (was already public in `issue.md` and `spec.md`). AC8 tie-out evidence reports per-layer PASS/FAIL only. PASS.

---

## 8. Gaps and Exceptions

None. All R3 findings are closed and no new findings were identified in this R4 re-audit.

---

## 9. Summary of Changes

Changed files in the branch diff `b0e048fe..528dcbc3`:

- Production:
  - `src/mix_rollups.py` (+58/-32) — coarser builders now aggregate from unfiltered `mix_base` at their own granularity.
  - `src/_mix_rollups_helpers.py` (+21/-63) — removed unused `unstack_to_long`.
  - `src/mix_pipeline_run.py` (+7/-4) — `run_transforms` threads `mix_base` to the changed builders.
- Tests:
  - `tests/test_mix_rollups.py` (+17/-151) — 562 → 204 lines; eight rollup/layer/stage tests retained; imports six shared fixtures from `_mix_rollups_fixtures`.
  - `tests/_mix_rollups_fixtures.py` (+225/-0) — new shared fixture module with `__all__` export list.
  - `tests/test_mix_rollups_tieout.py` (+167/-0) — new module with the three AC8/single-scenario regression tests.
  - `tests/mix_bottomsup_fixtures.py` (+4/-2) — call-site reconciliation for the changed builder signatures.
- Documentation/evidence:
  - feature folder evidence under `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/**` (baseline, remediation-baseline, qa-gates, regression-testing, issue-updates).
  - R3 audit artifacts and remediation-inputs retained for traceability.

R4 cycle delta (since R3): three test files split, no production code change, 220 tests preserved (identical test names; verified by inventory).

## 10. Compliance Verdict

| ID | Severity | Status | Description |
|---|---|---|---|
| (none) | — | — | All prior R3 findings closed; no new findings. |

- **Blocking findings:** 0
- **Non-Blocking findings:** 0

**Verdict:** PASS. Zero remediation triggers. No `remediation-inputs.<timestamp>.md` is produced for this pass.

---

## Appendix A: Test Inventory

11 test functions across the two `tests/test_mix_rollups*.py` modules after the R4 split (identical to the pre-split set; no test added, removed, renamed, or skipped):

`tests/test_mix_rollups.py` (8):

- `test_build_mix_0_detail_composite_keys`
- `test_build_mix_1_sku_produces_sku_mix_column`
- `test_build_mix_rollup_1_groups_by_customer_country_category`
- `test_build_mix_rollup_4_returns_scalar_sum`
- `test_build_mix_stage_keeps_only_nonzero_lbs_lines`
- `test_full_mix_chain_columns_and_fill_zero`
- `test_mix_4_country_subtracts_scalar_rollup_4`
- `test_rollup_tie_out_customer_layer_sum_matches_scalar`

`tests/test_mix_rollups_tieout.py` (3):

- `test_category_layer_retains_single_scenario_volume`
- `test_customer_layer_retains_single_scenario_volume`
- `test_layer_mix_equals_full_aggregation_minus_rollup`

Full repo suite: 220 tests collected, 220 passed, 0 failed, 0 skipped.

## Appendix B: Toolchain Commands Reference

| Stage | Command |
|---|---|
| Format check | `env -u VIRTUAL_ENV poetry run black --check .` |
| Lint | `env -u VIRTUAL_ENV poetry run ruff check .` |
| Type check | `env -u VIRTUAL_ENV poetry run pyright` |
| Unit tests + coverage | `env -u VIRTUAL_ENV poetry run pytest --cov=src --cov-branch --cov-report=term` |
| File-size measurement | `pwsh -NoProfile -Command "(Get-Content <path>).Count"` |
| Test inventory | `pwsh -NoProfile -Command "Select-String -Path tests/test_mix_rollups.py,tests/test_mix_rollups_tieout.py -Pattern '^def (test_[A-Za-z0-9_]+)' | ForEach-Object { \$_.Matches[0].Groups[1].Value } | Sort-Object"` |
| Evidence-location scan | `git diff --name-only b0e048fe..528dcbc3 | grep -E '^artifacts/(baselines|qa|evidence|coverage)/'` |

---

## Appendix C — Coverage Detail

| Source file | Stmts | Miss | Branch | BrPart | Cover |
|---|---|---|---|---|---|
| `src/__init__.py` | 0 | 0 | 0 | 0 | 100% |
| `src/_load_aop_helpers.py` | 79 | 0 | 20 | 0 | 100% |
| `src/_mix_bottomsup_helpers.py` | 39 | 0 | 2 | 0 | 100% |
| `src/_mix_nrr_summary_helpers.py` | 41 | 0 | 10 | 0 | 100% |
| `src/_mix_rollups_helpers.py` | 30 | 0 | 0 | 0 | 100% |
| `src/_mix_transforms_helpers.py` | 97 | 0 | 26 | 0 | 100% |
| `src/calculator.py` | 4 | 0 | 2 | 0 | 100% |
| `src/etl_columns.py` | 48 | 0 | 22 | 0 | 100% |
| `src/etl_key.py` | 56 | 0 | 30 | 0 | 100% |
| `src/etl_totals.py` | 12 | 0 | 2 | 0 | 100% |
| `src/load_aop.py` | 89 | 0 | 26 | 0 | 100% |
| `src/load_skulu.py` | 32 | 0 | 4 | 0 | 100% |
| `src/mix_bottomsup.py` | 57 | 0 | 2 | 0 | 100% |
| `src/mix_lookups.py` | 43 | 0 | 4 | 0 | 100% |
| `src/mix_nrr_summary.py` | 82 | 0 | 8 | 0 | 100% |
| `src/mix_pipeline.py` | 68 | 0 | 4 | 0 | 100% |
| `src/mix_pipeline_run.py` | 26 | 0 | 0 | 0 | 100% |
| `src/mix_q1.py` | 20 | 0 | 4 | 0 | 100% |
| `src/mix_rate_impacts.py` | 21 | 0 | 0 | 0 | 100% |
| `src/mix_rollups.py` | 59 | 0 | 0 | 0 | 100% |
| `src/mix_transforms.py` | 43 | 0 | 14 | 0 | 100% |
| `src/normalize_le.py` | 124 | 0 | 26 | 0 | 100% |
| `src/pandas_io.py` | 15 | 0 | 0 | 0 | 100% |
| **TOTAL** | **1085** | **0** | **206** | **0** | **100%** |

### Coverage Checklist (Python toolchain)

- [x] Repo-wide line coverage parsed (100%).
- [x] Repo-wide branch coverage parsed (100%).
- [x] New-file coverage verified per file.
- [x] Modified-file coverage verified per file with no regression on changed lines.
