# Policy Compliance Audit: mix-category-customer-mix-tieout (Issue #20)

**Audit Date:** 2026-05-27
**Code Under Test:** `src/mix_rollups.py`, `src/_mix_rollups_helpers.py`, `src/mix_pipeline_run.py`, `tests/test_mix_rollups.py`, `tests/mix_bottomsup_fixtures.py`

**Coverage Metrics by Language:**

| Language | Files Changed | Tests | Test Result | Baseline Coverage | Post-Change Coverage | New Code Coverage |
|----------|--------------|-------|-------------|-------------------|---------------------|-------------------|
| Python | 5 files (3 src, 2 test) | 220 tests | ✅ 220 pass, 0 fail | 100% lines, 100% branch | 100% lines, 100% branch | 100% |

**Note:** No TypeScript, PowerShell, C#, Bash, or JSON source files are changed in the branch diff (`b0e048f..98399b6`). Coverage verdicts for those languages are `N/A - zero changed files on branch`, which is the only context in which N/A is acceptable.

### Coverage Evidence Checklist

- Python baseline coverage artifact: `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/baseline/pytest-baseline.2026-05-27T22-07.md`
- Python post-change coverage artifact: `artifacts/python/lcov.info` (regenerated by reviewer 2026-05-27T22-34; 100% line / 100% branch) and `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/qa-gates/coverage-delta.2026-05-27T22-19.md`
- TypeScript baseline coverage artifact: `N/A - out of scope` (zero TypeScript files changed on branch)
- TypeScript post-change coverage artifact: `N/A - out of scope` (zero TypeScript files changed on branch)
- PowerShell baseline coverage artifact: `N/A - out of scope` (zero PowerShell files changed on branch)
- PowerShell post-change coverage artifact: `N/A - out of scope` (zero PowerShell files changed on branch)
- Per-language comparison summary: Section 1.2.1 below.

**Non-negotiable verdict rule:** This audit reports numeric baseline and post-change coverage for Python (the only in-scope language). The verdict is not PASS; see Section 10.

---

## Executive Summary

This branch corrects the issue #20 mix tie-out defect: `build_mix_2_category`, `build_mix_3_customer`, and `build_mix_4_country` now aggregate volume/revenue from the unfiltered `mix_base` at their own granularity rather than re-aggregating the prior filtered layer, while the rollup-subtraction target remains the prior finer layer's summed `Calc Net Price Impact`. `unstack_to_long` and the `_STAGE_SCENARIOS` constant were removed from `src/_mix_rollups_helpers.py`. `src/mix_pipeline_run.py` was rewired to pass `mix_base` to the three builders. `mix_1_sku` is unchanged. A cross-feature reconciliation updated `tests/mix_bottomsup_fixtures.py::build_chain` to pass `mix_base` to the changed builders.

The Python toolchain (Black, Ruff, Pyright, Pytest with branch coverage) was rerun by the reviewer and passes in a single clean pass: 220 tests pass, 100% line / 100% branch coverage repo-wide and on all three changed source files. The end-to-end pipeline was rerun by the reviewer against the confidential workbook; the `nrr_summary` reconciliation `check` resolves to `CHECK`.

One Blocking policy violation was found: `tests/test_mix_rollups.py` is 562 lines, exceeding the 500-line file-size limit in `.claude/rules/general-code-change.md`, which explicitly applies to test code. This triggers remediation.

**Policy documents evaluated:**
- ✅ `.claude/rules/general-code-change.md`
- ✅ `.claude/rules/general-unit-test.md`
- ✅ `.claude/rules/quality-tiers.md`
- ✅ `.claude/rules/self-explanatory-code-commenting.md`
- ✅ `.claude/rules/tonality.md`

**Language-specific policies evaluated:**
- ✅ Python: `.claude/rules/python.md` + `.claude/rules/python-suppressions.md`
- N/A PowerShell (zero changed files on branch)
- N/A TypeScript (zero changed files on branch)
- N/A C# (zero changed files on branch)
- N/A Bash (zero changed files on branch)
- N/A JSON (zero changed files on branch)

**Temporary artifacts cleanup:**
- ✅ All temporary review probe scripts (`_review_check.py`, `_review_tieout.py`, `_review_inspect.py`, `_review_tieout2.py`) and the throwaway `artifacts/mix_review.db` created during this audit were deleted; the working tree is clean.
- ✅ No ongoing tooling scripts were created by this audit.

---

## Rejected Scope Narrowing

No caller instruction attempted to narrow the audit scope to a plan, task, phase, or file subset, and no instruction attempted to mark any language's coverage as out of scope, informational only, or not applicable for a language with changed files. The caller supplied the merge-base, head SHA, base branch, and AC source, which are legitimate authoritative scope sources. Nothing to reject.

---

## Evidence Location Compliance

The branch diff was scanned for evidence files written under the forbidden `artifacts/baselines/`, `artifacts/baseline/`, `artifacts/qa/`, `artifacts/qa-gates/`, `artifacts/evidence/`, `artifacts/coverage/`, `artifacts/regression-testing/`, or `artifacts/post-change/` paths.

- Scan command: `git diff --name-only b0e048fe9f6d5004e158cac03c3c3261cbba0eb8 98399b6b61efae4eb4fa9784925a8975dcb356ca | rg "artifacts/(baselines?|qa|qa-gates|evidence|coverage|regression-testing|post-change)/"`
- Result: `NONE`. No forbidden evidence paths appear in the diff.
- All evidence artifacts are correctly under `docs/features/active/2026-05-27-mix-category-customer-mix-tieout-20/evidence/<kind>/` (baseline, qa-gates, regression-testing, issue-updates).
- Note: `scripts/validate_evidence_locations.py` is not present in this repository; the git-diff scan above is the substitute. Only the PowerShell PreToolUse hook exists for evidence-location enforcement.
- `EVIDENCE_LOCATION_OVERRIDE_REJECTED`: none. No caller instruction supplied a non-canonical evidence path.

---

## 1. General Unit Test Policy Compliance

### 1.1 Core Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Independence** - Tests run in any order | ✅ PASS | Each test in `tests/test_mix_rollups.py` builds its own fabricated `mix_base`/rollup fixtures via module-level helper functions; no shared mutable state or ordering dependency. 220 tests passed in a single run. |
| **Isolation** - Each test targets single behavior | ✅ PASS | Tests target one builder or one invariant each (e.g. `test_category_layer_retains_single_scenario_volume`, `test_layer_mix_equals_full_aggregation_minus_rollup`). |
| **Fast Execution** - Tests complete quickly | ✅ PASS | Full suite (220 tests) ran in 20.48s; reviewer rerun. No per-test slowness observed. |
| **Determinism** - Consistent results | ✅ PASS | Fabricated in-memory DataFrames only; no clock, RNG, network, or filesystem dependency in the changed tests. |
| **Readability & Maintainability** - Clear structure | ⚠️ PARTIAL | Test names and AAA structure are clear and well-documented. However `tests/test_mix_rollups.py` is 562 lines, exceeding the 500-line maintainability limit (see Section 2.3). |

### 1.2 Coverage and Scenarios

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Baseline Coverage Documented** | ✅ PASS | Baseline 100% line / 100% branch. Command: `poetry run pytest --cov --cov-branch --cov-report=term-missing`. Source: `evidence/baseline/pytest-baseline.2026-05-27T22-07.md`. |
| **No Coverage Regression** | ✅ PASS | Post-change 100% line / 100% branch (reviewer rerun). Change: +0% / +0%. No regression on changed lines. Evidence: `evidence/qa-gates/coverage-delta.2026-05-27T22-19.md` and reviewer rerun. |
| **New Code Coverage** | ✅ PASS | The three changed source files (`mix_rollups.py`, `_mix_rollups_helpers.py`, `mix_pipeline_run.py`) each report 0 missed statements and 0 partial branches: 100% line / 100% branch, above the 85%/75% uniform threshold. |
| **Comprehensive Coverage** | ✅ PASS | New tests cover single-scenario LE/AOP retention at category and customer granularity, the NPI-minus-rollup identity (AC2), the country scalar subtraction, and the nonzero-Lbs stage filter. |
| **Positive Flows** | ✅ PASS | `test_build_mix_1_sku_produces_sku_mix_column`, `test_full_mix_chain_columns_and_fill_zero`. |
| **Negative Flows** | ✅ PASS | `test_build_mix_stage_keeps_only_nonzero_lbs_lines` verifies zero-Lbs lines are dropped at the stage. |
| **Edge Cases** | ✅ PASS | `_single_scenario_mix_base_fixture` exercises new-in-LE (zero AOP Lbs) and dropped-in-LE (zero LE Lbs) boundary lines plus a cross-group leakage guard. |
| **Error Handling** | N/A | The pure builders raise no contract-relevant exceptions; no error paths to test for this change. |
| **Concurrency** | N/A | Pure synchronous transforms. |
| **State Transitions** | N/A | Stateless transforms. |

### 1.2.1 Per-Language Coverage Comparison

- Python: Baseline: 100% line (100% branch) -> Post-change: 100% line (100% branch). Change: +0% line (+0% branch). New/changed-code coverage: 100%. Disposition: PASS. Evidence: `evidence/baseline/pytest-baseline.2026-05-27T22-07.md`, `evidence/qa-gates/coverage-delta.2026-05-27T22-19.md`, `artifacts/python/lcov.info` (reviewer rerun 2026-05-27T22-34).
- TypeScript: Baseline: N/A - out of scope -> Post-change: N/A - out of scope. Change: N/A. New/changed-code coverage: `N/A - out of scope`. Disposition: N/A. Evidence: `N/A - out of scope` (zero changed files on branch).
- PowerShell: Baseline: N/A - out of scope -> Post-change: N/A - out of scope. Change: N/A. New/changed-code coverage: `N/A - out of scope`. Disposition: N/A. Evidence: `N/A - out of scope` (zero changed files on branch).

### 1.3 Test Structure and Diagnostics

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clear Failure Messages** | ✅ PASS | Assertions compare against independently recomputed expected values (e.g. `_unfiltered_group_lbs_le`), so failures localize to the specific layer/group. |
| **Arrange-Act-Assert Pattern** | ✅ PASS | Every test in the changed file uses explicit Arrange/Act/Assert comment blocks. |
| **Document Intent** | ✅ PASS | Descriptive `test_...` names and docstrings explain each scenario and expected outcome. |

### 1.4 External Dependencies and Environment

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Avoid External Dependencies** | ✅ PASS | No network, DB, external process, or runtime temp files in the changed tests; in-memory pandas DataFrames only. |
| **Use Mocks/Stubs** | ✅ PASS | No mocks needed; pure code paths are exercised directly. |
| **Environment Stability** | ✅ PASS | No global mutable state, config, or temp files. |

### 1.5 Policy Audit Requirement

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Pre-submission Review** | ✅ PASS | This audit serves as the required policy review. |

---

## 2. General Code Change Policy Compliance

### 2.1 Before Making Changes

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Clarify the objective** | ✅ PASS | Objective documented in `issue.md` and `spec.md` (issue #20) with a confirmed root-cause analysis. |
| **Read existing change plans** | ✅ PASS | `plan.2026-05-27T21-54.md` present in the feature folder. |
| **Document the plan** | ✅ PASS | Plan and spec capture the design summary and AC1-AC9. |

### 2.2 Design Principles

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Simplicity first** | ✅ PASS | The fix removes the `unstack_to_long` reshape indirection and sources each layer directly from `mix_base`, simplifying the cascade. |
| **Reusability** | ✅ PASS | The shared `build_mix_stage` / `join_rollup_mix` / `group_net_price_impact` helpers are reused by all layers; the country layer adds a synthetic `_all` key to reuse the same stage path. |
| **Extensibility** | ✅ PASS | Builders take keyword-style `mix_base` plus the rollup target; output schemas unchanged. |
| **Separation of concerns** | ✅ PASS | Builders remain pure (I/O-free); `run_transforms` performs no I/O; `main` remains the only I/O boundary. |

### 2.3 Module & File Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Cohesive modules** | ✅ PASS | `mix_rollups.py` holds the layer builders; `_mix_rollups_helpers.py` holds the shared reshape body; `mix_pipeline_run.py` is the runner. |
| **Under 500 lines** | ❌ FAIL | `tests/test_mix_rollups.py` is **562 lines** (was 338 at merge-base), exceeding the 500-line limit. Test code is explicitly within the limit per `general-code-change.md`; none of the exceptions (throwaway scripts, raw text fixtures, Markdown) apply. Source files are within limit: `mix_rollups.py` 343, `_mix_rollups_helpers.py` 182, `mix_pipeline_run.py` 137, `tests/mix_bottomsup_fixtures.py` 295. |
| **Public vs internal** | ✅ PASS | `__all__` is declared in all three changed source modules; the removed `unstack_to_long` was an internal helper with no remaining references (`rg unstack_to_long` returns nothing). |
| **No circular dependencies** | ✅ PASS | `_mix_rollups_helpers.py` imports nothing from `mix_rollups.py`; the `pandas` import moved into a `TYPE_CHECKING` block. |

### 2.4 Naming, Docs, and Comments

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Descriptive names** | ✅ PASS | `snake_case` functions/variables, descriptive builder names; matches `.claude/rules/python.md`. |
| **Docs/docstrings** | ✅ PASS | Every changed function carries a Google-style docstring with Args/Returns; module docstrings updated to describe the issue #20 sourcing change. |
| **Comment why, not what** | ✅ PASS | Inline comments explain intent (e.g. why `mix_base` is aggregated at the layer granularity, why the `_all` synthetic key is added) rather than narrating obvious lines. |

### 2.5 After Making Changes - Toolchain Execution

| Requirement | Status | Evidence |
|------------|--------|----------|
| **1. Formatting** | ✅ PASS | **Command:** `poetry run black --check <5 changed files>`<br>**Result:** "5 files would be left unchanged" (reviewer rerun, exit 0). |
| **2. Linting** | ✅ PASS | **Command:** `poetry run ruff check <5 changed files>`<br>**Result:** "All checks passed!" (reviewer rerun, exit 0). |
| **3. Type checking** | ✅ PASS | **Command:** `poetry run pyright <5 changed files>`<br>**Result:** "0 errors, 0 warnings, 0 informations" (reviewer rerun, exit 0). |
| **4. Testing** | ✅ PASS | **Command:** `poetry run pytest --cov=src --cov-branch --cov-report=term-missing`<br>**Result:** "220 passed in 20.48s", 100% line / 100% branch (reviewer rerun, exit 0). |
| **Full toolchain loop** | ✅ PASS | All four stages passed in a single reviewer pass with no auto-fixes. |
| **Explicit reporting** | ✅ PASS | Commands and results are recorded here and in `evidence/qa-gates/`. |

### 2.6 Summarize and Document

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Summarize changes** | ✅ PASS | Summarized in this audit and the feature spec. |
| **Design choices explained** | ✅ PASS | Spec "Proposed Fix" and module docstrings explain the unfiltered-`mix_base` sourcing decision. |
| **Update supporting documents** | ✅ PASS | Module docstrings updated. No README change was required for this value-correcting fix (AC9 scope). |
| **Provide next steps** | ✅ PASS | This audit and the remediation inputs define the next step (split the oversized test file). |

---

## 3. Language-Specific Code Change Policy Compliance

### Section 3A: Python Code Change Policy Compliance

#### 3A.1 Tooling & Baseline

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Formatting with Black** | ✅ PASS | `poetry run black --check` clean (reviewer rerun). |
| **Linting with Ruff** | ✅ PASS | `poetry run ruff check` clean (reviewer rerun). |
| **Type checking with Pyright** | ✅ PASS | `poetry run pyright` 0 errors (reviewer rerun). |
| **Testing with Pytest** | ✅ PASS | `poetry run pytest` 220 passed (reviewer rerun). |

#### 3A.2 Python Design & Typing

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Strong typing** | ✅ PASS | Full type hints on all changed signatures (`mix_base: pd.DataFrame`, `mix_rollup_4: float`, etc.); no `Any`. Pyright clean. |
| **Dataclasses for value objects** | N/A | No value objects introduced; pandas DataFrames are the transform medium. |
| **Protocols/ABCs for interfaces** | N/A | Single implementation; no interface needed. |
| **Avoid utility classes** | ✅ PASS | All changed code is module-level functions, no static-only utility classes. |

#### 3A.3 Python Error Handling

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Specific exceptions** | ✅ PASS | No broad `except` in changed source; builders fail fast on missing columns via pandas KeyError. |
| **Logging over print** | ✅ PASS | No `print` added to changed source; the pipeline uses the established logging pattern. |
| **Invariants at construction** | N/A | Pure functions; no constructors introduced. |

#### 3A.4 Suppressions

No `# noqa` or `# type: ignore` suppressions were added to any changed source or test file (verified by inspection of the diff). No suppression authorization is required.

---

## 4. Language-Specific Unit Test Policy Compliance

### Section 4A: Python Unit Test Policy Compliance

#### 4A.1 Framework and Scope

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Use Pytest** | ✅ PASS | Pytest only; function-style tests. |
| **Coverage expectation** | ✅ PASS | 100% line / 100% branch on changed source; >= 85%/75% uniform threshold met; no regression. |

#### 4A.2 Test Style and Structure

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Focused unit tests** | ✅ PASS | One behavior per test. |
| **Mocking sparingly** | ✅ PASS | No mocks; real pure code paths. |
| **Organization** | ⚠️ PARTIAL | `tests/test_mix_rollups.py` mirrors `src/mix_rollups.py` correctly, but the file exceeds 500 lines (see Section 2.3). |

#### 4A.3 Naming and Readability

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Naming conventions** | ✅ PASS | Descriptive `test_...` names. |
| **Docstrings/comments** | ✅ PASS | Each test has a docstring summarizing scenario and expected outcome. |

#### 4A.4 Running the Toolchain

| Requirement | Status | Evidence |
|------------|--------|----------|
| **Use Pytest** | ✅ PASS | `poetry run pytest` 220 passed. |
| **No Alternative Test Runners** | ✅ PASS | Pytest only. |

---

## 5. Test Coverage Detail

### Changed source modules (reviewer rerun, term-missing)

| Module | Stmts | Miss | Branch | BrPart | Cover |
|--------|-------|------|--------|--------|-------|
| `src/mix_rollups.py` | 59 | 0 | 0 | 0 | 100% / 100% |
| `src/_mix_rollups_helpers.py` | 30 | 0 | 0 | 0 | 100% / 100% |
| `src/mix_pipeline_run.py` | 26 | 0 | 0 | 0 | 100% / 100% |

**Not covered:** None. Every changed line and branch is exercised.

---

## 6. Test Execution Metrics

| Metric | Value | Status |
|--------|-------|--------|
| Total Tests | 220 | ✅ |
| Tests Passed | 220 (100%) | ✅ |
| Tests Failed | 0 | ✅ |
| Execution Time | 20.48s total | ✅ Fast |
| Code Coverage | 100% lines, 100% branches | ✅ |
| BottomsUp cross-feature tests | 19 passed | ✅ |
| Test File Size (`test_mix_rollups.py`) | 562 lines | ❌ Exceeds 500-line limit |

---

## 7. Code Quality Checks

**For Python:**

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Black Formatting | `poetry run black --check <changed files>` | 5 files unchanged | ✅ |
| Ruff Linting | `poetry run ruff check <changed files>` | All checks passed | ✅ |
| Pyright Type Checking | `poetry run pyright <changed files>` | 0 errors | ✅ |
| Pytest Tests | `poetry run pytest --cov=src --cov-branch` | 220 passed, 100%/100% | ✅ |

**Notes:** No pre-existing unrelated failures. The reviewer reran all four stages independently of the executor's `evidence/qa-gates/` artifacts and reproduced clean results.

---

## 8. Gaps and Exceptions

### Identified Gaps

- **Under 500 lines (file-size limit):** `tests/test_mix_rollups.py` is 562 lines, 62 lines over the limit. Plan: split the single-scenario regression and identity tests into a sibling test module (e.g. `tests/test_mix_rollups_tieout.py`) so each file is under 500 lines, preserving all assertions. Tracked in `remediation-inputs.2026-05-27T22-34.md`.

### Approved Exceptions

- **None.** No exceptions were approved. The file-size limit applies to test code with no applicable exception.

### Removed/Skipped Tests

- The `unstack_to_long` helper and its dedicated test were removed because the helper became unused after the layers were re-sourced from `mix_base`. The removed lines were fully covered before removal; no behavior coverage is lost (the layer behavior is now covered by the chain and single-scenario tests). This is a legitimate removal of dead code, not a weakening of coverage.

---

## 9. Summary of Changes

### Files Modified (branch `fix/mix-category-customer-mix-tieout-20`, `b0e048f..98399b6`)

1. **`src/mix_rollups.py`** (MODIFIED) — `build_mix_2_category`, `build_mix_3_customer`, `build_mix_4_country` re-sourced from unfiltered `mix_base`; docstrings updated. 343 lines.
2. **`src/_mix_rollups_helpers.py`** (MODIFIED) — removed `unstack_to_long` and `_STAGE_SCENARIOS`; moved `pandas` import to `TYPE_CHECKING`; docstring updated. 182 lines.
3. **`src/mix_pipeline_run.py`** (MODIFIED) — pass `mix_base` to the three changed builders in `run_transforms`. 137 lines.
4. **`tests/test_mix_rollups.py`** (MODIFIED) — added single-scenario retention and NPI-minus-rollup identity tests; updated chain calls. 562 lines (over limit).
5. **`tests/mix_bottomsup_fixtures.py`** (MODIFIED) — `build_chain` passes `mix_base` to the changed builders (cross-feature reconciliation with merged PR #22). 295 lines.

Non-code changes: feature docs (issue.md rename, spec.md, plan), evidence artifacts under the canonical `evidence/<kind>/` paths, and one agent-memory note.

---

## 10. Compliance Verdict

### Overall Status: ⚠️ PARTIALLY COMPLIANT

The functional fix is correct, fully covered, and passes the entire Python toolchain in a single clean pass with an independently reproduced end-to-end `CHECK` result. One Blocking policy violation prevents a clean PASS: `tests/test_mix_rollups.py` exceeds the 500-line file-size limit. Remediation is required to split the file before merge.

**Fail-closed reminder:** No required baseline, QA, or coverage artifact is missing; the non-PASS verdict is driven solely by the file-size violation, not by missing evidence.

---

### Policy-by-Policy Summary

#### General Code Change Policy (Section 2)
- ✅ Before Making Changes: documented in issue/spec/plan.
- ✅ Design Principles: simpler, pure, reusable.
- ❌ Module & File Structure: `test_mix_rollups.py` over 500 lines.
- ✅ Naming, Docs, Comments: compliant.
- ✅ Toolchain Execution: clean single pass.
- ✅ Summarize & Document: complete.

#### Language-Specific Code Change Policy (Section 3)
**For Python:**
- ✅ Tooling & Baseline: Black/Ruff/Pyright/Pytest clean.
- ✅ Python Design & Typing: strong typing, no `Any`, no suppressions.
- ✅ Error Handling: fail-fast, no broad catches.

#### General Unit Test Policy (Section 1)
- ⚠️ Core Principles: all met except maintainability file-size.
- ✅ Coverage & Scenarios: 100%/100%, comprehensive scenarios.
- ✅ Test Structure: clear AAA, good diagnostics.
- ✅ External Dependencies: none; no temp files.
- ✅ Policy Audit: completed.

#### Language-Specific Unit Test Policy (Section 4)
**For Python:**
- ✅ Framework & Scope: Pytest, coverage met.
- ⚠️ Test Style & Structure: focused/isolated, but oversized file.
- ✅ Naming & Readability: descriptive, documented.
- ✅ Toolchain: Pytest only.

---

### Metrics Summary

- ✅ 220/220 tests passing (100%)
- ✅ 100% line coverage, 100% branch coverage (repo-wide and changed source)
- ✅ No coverage regression on changed lines
- ✅ All four Python toolchain stages clean in a single pass
- ✅ End-to-end `nrr_summary` `check` = `CHECK` (reviewer rerun)
- ❌ `tests/test_mix_rollups.py` 562 lines (over 500-line limit)

---

### Recommendation

**Needs revision** — Split `tests/test_mix_rollups.py` to bring every file under the 500-line limit, then re-run the Python toolchain. All other policy requirements and acceptance criteria are satisfied. Once the file-size violation is resolved this change is ready for normal PR flow.

---

## Appendix A: Test Inventory

Changed/added tests in `tests/test_mix_rollups.py`:

- `test_build_mix_rollup_1_groups_by_customer_country_category`
- `test_build_mix_rollup_4_returns_scalar_sum`
- `test_build_mix_1_sku_produces_sku_mix_column`
- `test_full_mix_chain_columns_and_fill_zero`
- `test_mix_4_country_subtracts_scalar_rollup_4`
- `test_build_mix_0_detail_composite_keys`
- `test_build_mix_stage_keeps_only_nonzero_lbs_lines`
- `test_rollup_tie_out_customer_layer_sum_matches_scalar`
- `test_category_layer_retains_single_scenario_volume` (issue #20 regression)
- `test_customer_layer_retains_single_scenario_volume` (issue #20 regression)
- `test_layer_mix_equals_full_aggregation_minus_rollup` (AC2 identity)

Cross-feature: `tests/mix_bottomsup_fixtures.py::build_chain` updated (19 BottomsUp tests pass).

Full suite: 220 tests collected and passed.

---

## Appendix B: Toolchain Commands Reference

**For Python (run with `env -u VIRTUAL_ENV` per the repo Poetry quirk):**
```bash
# Formatting
env -u VIRTUAL_ENV poetry run black --check src/mix_rollups.py src/_mix_rollups_helpers.py src/mix_pipeline_run.py tests/test_mix_rollups.py tests/mix_bottomsup_fixtures.py

# Linting
env -u VIRTUAL_ENV poetry run ruff check src/mix_rollups.py src/_mix_rollups_helpers.py src/mix_pipeline_run.py tests/test_mix_rollups.py tests/mix_bottomsup_fixtures.py

# Type checking
env -u VIRTUAL_ENV poetry run pyright src/mix_rollups.py src/_mix_rollups_helpers.py src/mix_pipeline_run.py tests/test_mix_rollups.py tests/mix_bottomsup_fixtures.py

# Testing with branch coverage
env -u VIRTUAL_ENV poetry run pytest --cov=src --cov-branch --cov-report=term-missing

# End-to-end (confidential workbook present in environment)
env -u VIRTUAL_ENV poetry run python -m src.mix_pipeline --input "artifacts/LE v AOP Gross to Net Decomp.xlsx" --output artifacts/mix.db
```

---

**Audit Completed By:** feature-review agent
**Audit Date:** 2026-05-27
**Policy Version:** Current (as of audit date)
