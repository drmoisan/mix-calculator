# Remediation Plan — Issue #62, Cycle 1 (Edit Schema source pool + Columns scroll + window controls)

- Entry timestamp: 2026-06-10T12-24
- Issue: #62
- Branch: fix/edit-schema-columns-assignment
- PR: #63 (open, not merged)
- Work Mode: minor-audit (per `issue.md` metadata)
- Plan type: remediation (cycle 1)
- Requirements source: `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/issue.md` (`## Acceptance Criteria`, AC-1..AC-9)
- Root-cause / seams source: `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/remediation-inputs.2026-06-10T12-24.md`

## Scope summary

Cycle 0 delivered AC-1..AC-4 (alias-seeding in `ColumnsTabPresenter.prepopulate()`); that change is a valid fallback and is retained. Cycle 1 delivers AC-5..AC-9 without regressing AC-1..AC-4:

- Fix A (AC-5 / AC-6 / AC-9): the per-tab "Edit Schema" wiring builds a `preview_slice` from the selected worksheet's real headers (via the same reader + best-header-row path the discovery flow uses) and passes it into `open_schema_builder(..., preview_slice=...)`, so `prepopulate()` fuzzy-matches canonical columns to the real source columns. No file/worksheet selected → no reader call, empty pool, no crash (#50 seam preserved).
- Fix B (AC-7): wrap the Columns-tab content in a `QScrollArea` (`setWidgetResizable(True)`) in `build_columns_tab()`.
- Fix C (AC-8): set resizable + minimize/maximize/close window flags and a default size on `SchemaBuilderDialog`.

## Evidence location invariant

All evidence artifacts produced by this plan are written under
`docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/<kind>/`
per `.claude/skills/evidence-and-timestamp-conventions/SKILL.md`. No `artifacts/*` evidence paths are used. Any caller-supplied non-canonical evidence path is rejected and replaced with the canonical path, recorded as `EVIDENCE_LOCATION_OVERRIDE_REJECTED:`.

## Confidentiality invariant

The `preview_slice` built in Fix A is derived from a real worksheet and is used only at runtime in the live GUI. It MUST NOT be written to any committed file or test fixture. All tests use synthetic headers only (no real workbook column values). This mirrors the `PreviewSlice` and `CallerBuildSpec` confidentiality constraints already documented in `src/gui/presenters/_schema_builder_state.py` and `src/gui/_schema_build_specs.py`.

## File-size watch (500-line cap)

Baseline line counts captured at planning time (to be re-verified in Phase 0 and re-scanned in Phase 2):

- `src/gui/_schema_discovery_wiring.py` — ~246 lines (will grow with Fix A wiring; at risk).
- `src/gui/widgets/schema_builder_dialog.py` — ~494 lines (Fix C adds imports + flags; closest to cap; high risk — extract a helper if it would exceed 500).
- `src/gui/widgets/_schema_builder_tabs.py` — ~276 lines (Fix B adds a `QScrollArea` wrap; low risk).
- `src/gui/presenters/source_selection_presenter.py` — ~380 lines (Fix A may extract a Qt-free header-columns helper here; medium risk).
- Do NOT touch `src/gui/widgets/source_input_widget.py` (near cap).

---

### Phase 0 — Baseline capture

- [x] [P0-T1] Read repository policy files in the required order and record an evidence artifact. Read, in this order: `CLAUDE.md`; `.claude/rules/general-code-change.md`; `.claude/rules/general-unit-test.md`; `.claude/rules/quality-tiers.md`; `.claude/rules/python.md`; `.claude/rules/python-suppressions.md`; `.claude/rules/self-explanatory-code-commenting.md`; `.claude/rules/benchmark-baselines.md` (N/A confirmation); `.claude/rules/tonality.md`. Write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/baseline/phase0-instructions-read.md` containing `Timestamp:`, `Policy Order:`, and the explicit list of files read. AC: all listed files read; artifact exists with required fields. (Maps to: process gate for all ACs.)
- [x] [P0-T2] Capture the Black baseline. Run `poetry run black --check .` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/baseline/black.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. AC: artifact exists with all four fields and the pass/fail signal. (Maps to: toolchain gate.)
- [x] [P0-T3] Capture the Ruff baseline. Run `poetry run ruff check .` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/baseline/ruff.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. AC: artifact exists with all four fields. (Maps to: toolchain gate.)
- [x] [P0-T4] Capture the Pyright baseline. Run `poetry run pyright` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/baseline/pyright.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (error/warning counts). AC: artifact exists with all four fields. (Maps to: toolchain gate.)
- [x] [P0-T5] Capture the Pytest + coverage baseline. Run `$env:QT_QPA_PLATFORM='offscreen'; poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/baseline/pytest-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric baseline total line-coverage percent and branch-coverage percent and the pass count. AC: artifact records numeric baseline line% and branch% (not placeholders). (Maps to: coverage no-regression gate for all ACs.)
- [x] [P0-T6] Capture the file-size baseline for the at-risk files. Record the current line count of `src/gui/_schema_discovery_wiring.py`, `src/gui/widgets/schema_builder_dialog.py`, `src/gui/widgets/_schema_builder_tabs.py`, and `src/gui/presenters/source_selection_presenter.py` into `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/baseline/file-size-baseline.md` with `Timestamp:` and `Output Summary:` (one line count per file). AC: artifact lists all four files with numeric line counts. (Maps to: 500-line cap gate.)

---

### Phase 1 — Implementation

#### Fix A — Edit builder source pool from real worksheet headers (AC-5 / AC-6 / AC-9)

- [x] [P1-T1] Expose a Qt-free, reusable worksheet-header-columns helper so Fix A and `on_schema_discovery` share one path. In `src/gui/presenters/source_selection_presenter.py`, rename the module-private `_best_header_row` to a public `best_header_row` (keep behavior identical: LE/SKU_LU header row 0 unaffected; AOP1 detected header row honored; fallback to `rows[0]` when no row matches) and update its internal reference in `on_schema_discovery`. Add a new public module-level function `read_worksheet_header_columns(reader, service, path, sheet, *, max_rows=_HEADER_PREVIEW_ROWS) -> tuple[str, ...]` that: returns `()` when `path` or `sheet` is blank/whitespace-only (no reader call — #50 / AC-9 seam); calls `reader.read_sheet_preview(path, sheet, max_rows=max_rows)`; returns `()` when the preview is empty; otherwise returns `tuple(best_header_row(rows, service))`. Add a complete docstring per the commenting policy. AC: `best_header_row` is public and behavior-identical; `read_worksheet_header_columns` exists with the blank-guard, empty-preview guard, and best-header-row delegation; `on_schema_discovery` calls `best_header_row` and still passes its existing tests. (Maps to: AC-5, AC-9.)
- [x] [P1-T2] Add a unit test for `read_worksheet_header_columns` covering the populated and guard paths. In a new file `tests/gui/test_worksheet_header_columns.py`, using `FakeWorkbookReader` (synthetic headers only) and `FakeSchemaService`, assert: (a) a worksheet whose header is on row 0 returns those synthetic header tokens; (b) a multi-row preview with stray leading rows returns the best-matching header row (AOP1-style, synthetic); (c) a blank/whitespace path returns `()` and the reader is NOT called (record call count on the fake); (d) a blank/whitespace sheet returns `()` and the reader is NOT called; (e) an empty preview returns `()`. No `QApplication`, no temp files, deterministic. AC: all five cases pass; the fake records zero `read_sheet_preview` calls for the blank-path/blank-sheet cases. (Maps to: AC-5, AC-9.)
- [x] [P1-T3] Pass a header-provider into the Edit wiring so the edit open path seeds a real-header `preview_slice`. In `src/gui/_schema_discovery_wiring.py`, change `wire_edit_schema_buttons` to accept the three per-tab presenters (or, to keep the open-path signature small, a per-widget header-provider derived from them) without changing the external entry point `wire_schema_discovery_and_gating`'s public contract: update the `wire_edit_schema_buttons(window, service, ...)` call site inside `wire_schema_discovery_and_gating` to also pass `le_presenter`/`aop_presenter`/`skulu_presenter` (each already in scope and carrying `.reader`). In `_make_edit`/`_open_edit`, after the existing schema-name placeholder guard, read `path = widget.current_path()` and `sheet = widget.current_sheet()`, build `header = read_worksheet_header_columns(presenter.reader, service, path, sheet)`, construct `preview_slice = PreviewSlice(header=header) if header else None`, and pass `preview_slice=preview_slice` into the `open_schema_builder(...)` call. Keep the optional `dialog_factory`/`presenter_factory` test seams. Import `PreviewSlice` and `read_worksheet_header_columns` under `TYPE_CHECKING`/runtime as appropriate (runtime import for `PreviewSlice` constructor and the helper call). AC: the edit handler computes a header-derived `preview_slice` and forwards it to `open_schema_builder`; when no file/sheet is selected the helper returns `()` and `preview_slice` is `None` (empty pool, no crash); existing edit-wiring tests still pass. (Maps to: AC-5, AC-6, AC-9.)
- [x] [P1-T4] Add wiring tests proving the Edit path populates the source pool and renders matched canonical rows as assigned. In `tests/gui/test_edit_schema_wiring.py`, add tests that: (a) wire `wire_edit_schema_buttons` with a `SourceSelectionPresenter` carrying a `FakeWorkbookReader` that returns synthetic headers matching the loaded schema's canonical names, set the widget's current path/sheet to non-blank synthetic values, emit `edit_schema_requested`, and assert the `open_schema_builder` path received a non-empty `preview_slice` whose `header` equals the synthetic headers (assert via a recording dialog/presenter factory or by inspecting the opened dialog's columns/source pool); (b) assert that with that pool present, the loaded schema's matching canonical rows render assigned (drive through the real `SchemaBuilderDialog` + presenter under offscreen Qt, or assert `prepopulate`-equivalent assignment on the bound columns presenter) — synthetic data only. AC: test (a) asserts a populated `preview_slice.header` equal to synthetic headers; test (b) asserts at least one canonical row is assigned (not `None`) for a name-matching synthetic header. (Maps to: AC-5, AC-6.)
- [x] [P1-T5] Add a wiring test for the no-file/no-sheet Edit path (AC-9). In `tests/gui/test_edit_schema_wiring.py`, add a test that wires the edit button with a `FakeWorkbookReader` that records calls, sets the widget's current path/sheet to blank/whitespace, emits `edit_schema_requested` for a real selected schema name, and asserts: the reader's `read_sheet_preview` was NOT called; `open_schema_builder` received `preview_slice=None`; and no exception is raised. AC: reader call count is zero; `preview_slice` is `None`; handler completes without raising. (Maps to: AC-9.)

#### Fix B — Columns tab vertically scrollable (AC-7)

- [x] [P1-T6] Wrap the Columns-tab content in a `QScrollArea`. In `src/gui/widgets/_schema_builder_tabs.py`, modify `build_columns_tab()` to create a `QScrollArea`, set `setWidgetResizable(True)`, set the `ColumnsTabWidget` as its widget, and place the scroll area in the tab's `QVBoxLayout` (instead of placing `ColumnsTabWidget` directly). Keep `ColumnsTabControls.columns_widget` pointing at the real `ColumnsTabWidget` so binder wiring and existing tests are unaffected. Add `QScrollArea` to the `PySide6.QtWidgets` import. Update the `build_columns_tab` docstring to note the scroll-area wrap. AC: `build_columns_tab()` returns controls whose container hosts a `QScrollArea` with `widgetResizable == True`, and `columns_widget` is the same `ColumnsTabWidget` instance set as the scroll area's widget. (Maps to: AC-7.)
- [x] [P1-T7] Add a focused offscreen Qt test asserting the Columns tab hosts a resizable scroll area. In `tests/gui/test_columns_tab_widgets.py` (or `tests/gui/test_schema_builder_dialog.py` if more cohesive), add a test under offscreen Qt that builds the Columns tab, locates the `QScrollArea` in the container, and asserts `scroll_area.widgetResizable() is True` and `scroll_area.widget() is controls.columns_widget`. Deterministic; no temp files. AC: test passes asserting a `QScrollArea` with `widgetResizable True` wrapping the real columns widget. (Maps to: AC-7.)

#### Fix C — Schema Builder window resizable with min/max/restore (AC-8)

- [x] [P1-T8] Add resizable + minimize/maximize window flags and a default size to the dialog. In `src/gui/widgets/schema_builder_dialog.py`, import `Qt` from `PySide6.QtCore`, and in `SchemaBuilderDialog.__init__` after `setWindowTitle`, call `self.setWindowFlags(Qt.WindowType.Window | Qt.WindowType.WindowMinimizeButtonHint | Qt.WindowType.WindowMaximizeButtonHint | Qt.WindowType.WindowCloseButtonHint)` and set a sensible default size via `self.resize(<width>, <height>)` (for example 900x700) so all 26 AOP rows are reachable with the scroll area. Add a brief intent comment per the commenting policy. If this addition would push the file to 500 lines or more, extract the window-flag/size setup into a small private helper module or method to stay under the cap (re-scan in P1-T11). AC: `SchemaBuilderDialog` window flags include the minimize and maximize hints and the close hint, the dialog is a top-level `Qt.Window`, and a default size is set. (Maps to: AC-8.)
- [x] [P1-T9] Add a focused offscreen Qt test asserting the dialog exposes min/max window controls. In `tests/gui/test_schema_builder_dialog.py`, add a test under offscreen Qt that constructs `SchemaBuilderDialog` and asserts `dialog.windowFlags()` includes `Qt.WindowType.WindowMinimizeButtonHint` and `Qt.WindowType.WindowMaximizeButtonHint` (bitwise-and each hint and assert non-zero). Deterministic; no temp files. AC: test passes asserting both hints are present on the dialog's window flags. (Maps to: AC-8.)

#### Regression retention and file-size scan (Phase 1)

- [x] [P1-T10] Confirm the cycle-0 AC-1..AC-4 tests are retained and unweakened. Identify the existing tests covering AC-1..AC-4 (alias-seeding in `ColumnsTabPresenter.prepopulate()` and `load_existing` rendering — e.g. `tests/gui/test_columns_tab_presenter.py`, `tests/gui/test_edit_schema_wiring.py`, `tests/gui/test_schema_builder_presenter_*.py`) and confirm none of this cycle's edits remove or weaken their assertions (no deleted assertions, no broadened exception checks). AC: the AC-1..AC-4 tests still exist with their original assertion strength and pass in the Phase 2 run. (Maps to: AC-1, AC-2, AC-3, AC-4 — no regression.)
- [x] [P1-T11] Scan every changed production and test file against the 500-line cap and extract helpers if any reaches the limit. After P1-T1..P1-T9, record the line count of each changed file (at minimum `src/gui/_schema_discovery_wiring.py`, `src/gui/widgets/schema_builder_dialog.py`, `src/gui/widgets/_schema_builder_tabs.py`, `src/gui/presenters/source_selection_presenter.py`, and each changed/new test file) into `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/file-size-scan-phase1.md` with `Timestamp:` and `Output Summary:`. If any file is at or above 500 lines, extract a cohesive helper (e.g. move the dialog window-flag setup or the edit-wiring header-provider closure into a small `_`-prefixed helper module) and re-scan until all changed files are under 500. AC: artifact lists every changed file with a numeric line count, and every changed file is strictly under 500 lines. (Maps to: 500-line cap gate.)

---

### Phase 2 — Final QC loop, coverage comparison, file-size scan, and AC traceability

Run the full Python toolchain in order (Black → Ruff → Pyright → Pytest). If any step changes files or fails, restart from Black until a single clean pass completes. Tests run under `$env:QT_QPA_PLATFORM='offscreen'`.

- [x] [P2-T1] Final formatting gate. Run `poetry run black --check .` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/final-black.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If Black reformats files, restart the loop from this step. AC: `EXIT_CODE: 0` recorded; artifact has all four fields. (Maps to: toolchain gate.)
- [x] [P2-T2] Final lint gate. Run `poetry run ruff check .` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/final-ruff.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. No unauthorized suppressions added. AC: `EXIT_CODE: 0` recorded; artifact has all four fields. (Maps to: toolchain gate.)
- [x] [P2-T3] Final type-check gate. Run `poetry run pyright` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/final-pyright.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` (0 errors expected). AC: `EXIT_CODE: 0` and 0 errors recorded; artifact has all four fields. (Maps to: toolchain gate.)
- [x] [P2-T4] Final test + coverage gate. Run `$env:QT_QPA_PLATFORM='offscreen'; poetry run pytest --cov --cov-branch --cov-report=term-missing` and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/final-pytest-coverage.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including numeric post-change total line% and branch% and the pass count. AC: `EXIT_CODE: 0`; numeric post-change line% and branch% recorded; line% >= 85 and branch% >= 75. (Maps to: all ACs — verification; coverage gate.)
- [x] [P2-T5] Coverage delta / threshold verification. Compare the Phase 0 baseline (`evidence/baseline/pytest-coverage.md`) against the Phase 2 final (`evidence/qa-gates/final-pytest-coverage.md`) and write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/coverage-delta.md` recording: baseline line% and branch%, post-change line% and branch%, and changed-line coverage for the changed files (`src/gui/_schema_discovery_wiring.py`, `src/gui/presenters/source_selection_presenter.py`, `src/gui/widgets/_schema_builder_tabs.py`, `src/gui/widgets/schema_builder_dialog.py`). AC: artifact records baseline, post-change, and changed-code coverage; line% >= 85, branch% >= 75, and no regression on changed lines. If any value is unavailable, mark the outcome remediation-required (do not report PASS). (Maps to: coverage no-regression gate.)
- [x] [P2-T6] Final 500-line file-size scan of all changed files. Record the final line count of every changed production and test file into `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/file-size-scan-final.md` with `Timestamp:` and `Output Summary:`. AC: artifact lists every changed file with a numeric line count and confirms all are strictly under 500 lines; `src/gui/widgets/source_input_widget.py` is unchanged. (Maps to: 500-line cap gate.)
- [x] [P2-T7] AC-1..AC-9 traceability matrix. Write `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/ac-traceability.md` mapping each acceptance criterion (AC-1..AC-9) to the implementing change and the verifying test(s): AC-1..AC-4 → retained cycle-0 tests (P1-T10); AC-5 → P1-T1/P1-T3/P1-T4 + `read_worksheet_header_columns` tests (P1-T2); AC-6 → P1-T4; AC-7 → P1-T6/P1-T7; AC-8 → P1-T8/P1-T9; AC-9 → P1-T2/P1-T5. Include `Timestamp:` and the final pass/fail signal per AC. AC: every AC-1..AC-9 maps to a code change and a passing test with no gaps. (Maps to: AC-1..AC-9.)
- [x] [P2-T8] Confidentiality scan of committed artifacts. Confirm that no committed test fixture or evidence artifact contains real worksheet column values or proprietary headers (only synthetic/`masked_*`/test placeholders). Record the result in `docs/features/active/2026-06-09-edit-schema-columns-assignment-62/evidence/qa-gates/confidentiality-scan.md` with `Timestamp:` and `Output Summary:`. AC: artifact confirms no real worksheet content is committed; tests use synthetic headers only. (Maps to: confidentiality invariant for AC-5/AC-6.)

---

## Preflight

This plan is submitted for validation-only preflight via `atomic-executor` with the directive `DIRECTIVE: PREFLIGHT VALIDATION ONLY`, expecting `PREFLIGHT: ALL CLEAR` or `PREFLIGHT: REVISIONS REQUIRED`. The same target file
`docs/features/active/2026-06-09-edit-schema-columns-assignment-62/remediation-plan.2026-06-10T12-24.md`
is reused across all revision iterations. The orchestrator runs the `mcp__drm-copilot__validate_orchestration_artifacts` validator gate (`artifact_type: "plan"`); this plan is not treated as approved until that validator exits zero.
