# columns-tab-scroll-responsive-layout-68 (Plan)

- **Issue:** #68
- **Work Mode:** minor-audit
- **Owner:** drmoisan
- **Last Updated:** 2026-06-16T09-16
- **Status:** Draft
- **Version:** 0.2

**Fail-closed evidence rule:** Include explicit baseline artifact tasks, final-QA artifact tasks, and coverage-comparison tasks for each in-scope language when policy requires coverage. If any required baseline artifact, QA artifact, or coverage-comparison artifact is missing, the audit verdict must be BLOCKED or INCOMPLETE, never PASS.

**Evidence accounting rule:** Record the expected artifact path or location in each evidence-producing task. Do not mark evidence-backed work complete without the artifact.

**Non-overridable evidence path:** All evidence artifacts write to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/<kind>/`. No other path is valid.

---

### Phase 0 — Policy Baseline Capture

- [x] [P0-T1] Read `.claude/rules/general-code-change.md` and record file read in `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/phase0-instructions-read.md` with `Timestamp:`, `Policy Order:`, and explicit file list.
- [x] [P0-T2] Read `.claude/rules/general-unit-test.md` and append confirmation to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/phase0-instructions-read.md`.
- [x] [P0-T3] Read `.claude/rules/python.md` and `.claude/rules/python-suppressions.md` and append confirmation to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/phase0-instructions-read.md`.
- [x] [P0-T4] Read `.claude/rules/quality-tiers.md` and `.claude/rules/self-explanatory-code-commenting.md` and append confirmation to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/phase0-instructions-read.md`.
- [x] [P0-T5] Run `poetry run black . --check` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/black-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T6] Run `poetry run ruff check .` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/ruff-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T7] Run `poetry run pyright` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/pyright-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`.
- [x] [P0-T8] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/baseline/pytest-baseline.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including total line coverage %, total branch coverage %, and coverage for `src/gui/widgets/_columns_tab_drag.py`.

---

### Phase 1 — Implementation

- [x] [P1-T1] In `src/gui/widgets/_columns_tab_drag.py`: add `QScrollArea`, `QSplitter` to the `PySide6.QtWidgets` import block (no other import changes). Acceptance criterion: import block compiles without error and line count does not change beyond the import line itself.
- [x] [P1-T2] In `ColumnsTabWidget.__init__` in `src/gui/widgets/_columns_tab_drag.py`: add a `width_threshold: int = 700` constructor parameter and store it as `self._width_threshold`. Acceptance criterion: `ColumnsTabWidget(width_threshold=500)` constructs without error and `self._width_threshold == 500`.
- [x] [P1-T3] In `ColumnsTabWidget.__init__` in `src/gui/widgets/_columns_tab_drag.py`: build a `QWidget` named `_pool_panel` containing the "Source columns" `QLabel` and `_pool_box`, and a `QWidget` named `_rows_panel` containing the "Canonical columns" `QLabel` and `_rows_box` wrapped in a `QScrollArea` (with `setWidgetResizable(True)`). Replace the existing `outer = QVBoxLayout(self)` block with a `QSplitter` assigned to `self._splitter`. Acceptance criterion: `ColumnsTabWidget()` constructs, `self._splitter` is a `QSplitter` instance, the scroll area is resizable (`widgetResizable() is True`).
- [x] [P1-T4] In `ColumnsTabWidget.__init__` in `src/gui/widgets/_columns_tab_drag.py`: set the initial splitter orientation and panel order to `Qt.Orientation.Horizontal` with `_rows_panel` first and `_pool_panel` second (wide-mode layout); add a top-level `QVBoxLayout(self)` that contains only `self._splitter`. Acceptance criterion: constructing the widget and calling `self._splitter.orientation()` returns `Horizontal`.
- [x] [P1-T5] In `ColumnsTabWidget` in `src/gui/widgets/_columns_tab_drag.py`: add `resizeEvent(self, e: QResizeEvent) -> None` that compares `e.size().width()` to `self._width_threshold`; when crossing the threshold, calls `setParent(None)` on both panels and re-adds them with `addWidget` in the new correct order (wide: rows left, pool right; narrow: pool top, rows bottom), then changes `self._splitter.setOrientation(...)`. Acceptance criterion: the method exists, is type-annotated, and passes Pyright; the two orientation/order branches are distinct code paths.
- [x] [P1-T6] Verify `src/gui/widgets/_columns_tab_drag.py` line count does not exceed 500. If it does, extract the `resizeEvent` body helper into `src/gui/widgets/_columns_tab_layout.py` as a standalone function `apply_splitter_orientation(splitter: QSplitter, pool_panel: QWidget, rows_panel: QWidget, wide: bool) -> None` and call it from `resizeEvent`. Acceptance criterion: `_columns_tab_drag.py` line count <= 500; if the helper file was created, it has a module docstring and full type annotations.
- [x] [P1-T7] In `tests/gui/test_columns_tab_widgets.py`: add `test_columns_tab_scroll_area_wraps_rows_panel` — construct `ColumnsTabWidget()`, call `set_rows([("Col1","",None),("Col2","",None)])`, find the `QScrollArea` via `findChildren`, assert `widgetResizable() is True` and that at least one `ColumnDropRow` is a descendant of the scroll area's inner widget. Acceptance criterion: the test function exists, is syntactically valid, and exercises AC-1.
- [x] [P1-T8] In `tests/gui/test_columns_tab_widgets.py`: add `test_columns_tab_wide_layout_rows_left_pool_right` — construct `ColumnsTabWidget(width_threshold=400)`, call `set_source_pool(["a"])` and `set_rows([("X","",None)])`, simulate a wide resize by calling `widget.resize(500, 300)` and `widget.resizeEvent` directly with a stub `QResizeEvent` carrying width=500, assert the splitter's first widget contains a `ColumnDropRow` descendant and the second widget contains a `SourceColumnToken` descendant. Acceptance criterion: the test function exists and exercises AC-3.
- [x] [P1-T9] In `tests/gui/test_columns_tab_widgets.py`: add `test_columns_tab_narrow_layout_pool_top_rows_bottom` — construct `ColumnsTabWidget(width_threshold=400)`, simulate a narrow resize with width=300, assert the splitter's first widget contains a `SourceColumnToken` descendant and the second widget contains a `ColumnDropRow` descendant. Acceptance criterion: the test function exists and exercises AC-4.
- [x] [P1-T10] In `tests/gui/test_columns_tab_widgets.py`: add `test_columns_tab_derived_rows_appear_after_regular_rows` — call `set_rows` with a list of 3 regular rows followed by 2 derived rows (tuples with the derived canonical names), assert `row_canonicals()` returns all 5 in order with the 2 derived at the end, assert all 5 are accessible via `row_label_text`. Acceptance criterion: the test verifies AC-2 ordering of derived rows is preserved.
- [x] [P1-T11] In `tests/gui/test_columns_tab_widgets.py`: add `test_columns_tab_drag_behaviors_survive_orientation_switch` — construct `ColumnsTabWidget(width_threshold=400)`, call `set_source_pool(["c"])` and `set_rows([("D","",None)])`, simulate a narrow resize, then invoke `assign_column("c", "D")` through `on_assign` mock and assert the recorded call matches; then simulate a wide resize and repeat. Acceptance criterion: AC-5 drag-callback routing is unaffected by orientation changes.
- [x] [P1-T12] Verify that existing test seams `token_names()`, `row_canonicals()`, `row_label_text()`, `row_assignment_text()` still return correct values after the layout changes by reading their implementations in `src/gui/widgets/_columns_tab_drag.py` and confirming they reference `self._tokens` and `self._rows` (which are unchanged by the splitter refactor). Acceptance criterion: no modification needed; or if the internal widget hierarchy change makes any seam non-functional, update the seam implementation and document the change in this task's notes.
- [x] [P1-T13] If `src/gui/widgets/_columns_tab_layout.py` was created in P1-T6, add `QResizeEvent` import to `QResizeEvent`'s origin module in `src/gui/widgets/_columns_tab_drag.py` under `TYPE_CHECKING` to satisfy Pyright. Acceptance criterion: `poetry run pyright` exits 0 on both files.

---

### Phase 2 — Final QA Loop

- [x] [P2-T1] Run `poetry run black .` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/qa-gates/black-qa.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If exit code is non-zero or files are changed, restart the QA loop from P2-T1 after fixing all formatting issues.
- [x] [P2-T2] Run `poetry run ruff check .` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/qa-gates/ruff-qa.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If any lint errors remain, fix them and restart from P2-T1.
- [x] [P2-T3] Run `poetry run pyright` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/qa-gates/pyright-qa.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:`. If any type errors remain, fix them and restart from P2-T1.
- [x] [P2-T4] Run `poetry run pytest --cov --cov-branch --cov-report=term-missing` and write result to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/qa-gates/pytest-qa.md` with `Timestamp:`, `Command:`, `EXIT_CODE:`, `Output Summary:` including total line coverage %, total branch coverage %, and coverage for `src/gui/widgets/_columns_tab_drag.py`. If exit code is non-zero, fix failing tests and restart from P2-T1.
- [x] [P2-T5] Write coverage delta comparison to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/qa-gates/coverage-delta.md` recording: baseline line coverage % (from P0-T8), post-change line coverage % (from P2-T4), baseline branch coverage % (from P0-T8), post-change branch coverage % (from P2-T4), and a PASS/FAIL verdict: PASS only when post-change line >= 85% AND post-change branch >= 75% AND neither value regressed from baseline.
- [x] [P2-T6] Verify all five AC items against test evidence: AC-1 (scroll area wraps rows — confirmed by P1-T7 test pass), AC-2 (derived rows appear after regular rows — confirmed by P1-T10 test pass), AC-3 (wide layout — confirmed by P1-T8 test pass), AC-4 (narrow layout — confirmed by P1-T9 test pass), AC-5 (drag behaviors — confirmed by P1-T11 test pass). Write verification summary to `docs/features/active/columns-tab-scroll-responsive-layout-68/evidence/qa-gates/ac-verification.md` with one line per AC citing the test name and result. Verdict must be PASS only when all five ACs have a passing test in P2-T4 output.
